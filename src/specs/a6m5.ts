import { DEG } from '../core/math'
import { makeHitBox } from '../world/hit'
import { A6M5_BATTERY } from '../weapons/a6m5'
import type { AircraftSpec, HistoricalReference } from './types'

/** 公制馬力（PS）。日方的出力一律以此為單位。 */
const PS = 735.5
const KMH = 1 / 3.6
const MPH = 0.44704

/**
 * A6M5a 零戰 五二型甲。
 *
 * ══ 史實表：**六項全在同一份文件的報告不存在** ═══════════════
 *
 * 這是三台裡來源最零碎的一台。兩份候選各缺一半：
 *
 * ```
 *   TAIC 102D（1945-03）    極速＋臨界高度、海面極速、海面爬升、升限、重量
 *                           —— 五項同源同重量（6,026 lb ＝ 2,733 kg）
 *                           缺失速；**而且它認定的發動機是 Sakae 31A**
 *
 *   TSFER-1973（1946-03）   失速（乾淨與著陸都有）、低空爬升、平飛速度
 *                           —— 同一架、同一重量 5,544 lb
 *                           缺升限、缺全開高度
 * ```
 *
 * **主幹走日方（栄二一型），理由不是精度是引擎：**
 *
 * ```
 *                          一速全開高度   二速全開高度
 *   栄二一型（日方）          2,850 m       6,000 m
 *   Sakae 31A（TAIC 認定）    2,835 m       6,584 m
 * ```
 *
 * 一速幾乎一致，**二速差 584 m**。L2 有一條「極速在臨界高度附近達到峰值」
 * 的斷言，整條中高空曲線的形狀就由這個高度決定。走 TAIC 那一組等於把一台
 * 裝栄二一型的五二型配上另一款發動機的增壓器 —— 那不是誤差，是換了一台
 * 飛機。而且**日方的 565 km/h 剛好落在栄二一型的二速全開高度 6,000 m 上**，
 * 這個自洽性本身是一個交叉驗證。
 *
 * **TAIC 102D 自己也承認那張表是推算**：同一頁的 GENERAL DATA 欄寫著
 * 「A maximum speed of only 340 mph has been obtained in flight tests」——
 * 表列 358 mph，實飛只到 340。
 *
 * ══ 選五二型甲而不是丙 ═════════════════════════════════════
 *
 * 性能那一組（栄二一型、2,733 kg）屬於基本型與甲型；丙型加了 8 mm 鋼板、
 * 55 mm 頭枕玻璃與兩挺翼內 13 mm，重量與性能都不同。參考模型雖然是丙，
 * 但**我們的網格分不出這兩者**（不畫槍管、不畫玻璃厚度）。
 */
export const A6M5: AircraftSpec = {
  id: 'a6m5',
  name: 'A6M5 零戰',
  faction: 'japan',
  role: 'fighter',

  /**
   * # 2,733 kg —— **本輪查證裡最硬的一個數字**
   *
   * 日方正規全備 2,733 kg ＝ TAIC 的 Gross Normal 6,026 lb，兩系分毫不差。
   *
   * 【順手解掉一個日文維基的自相矛盾】兩個條目掛同一個引用（野沢正
   * 『日本航空機総集』p.189）卻分別寫 2,733 與 2,607 kg。判定 2,733 ——
   * 因為 2,733 ÷ 21.30 ＝ 128.3 kg/m²，恰好等於同一張表自己列的翼面荷重
   * 128.31；2,607 會得到 122.4。
   */
  mass: 2733,

  /**
   * 慣量 —— 由 P-51D 的迴轉半徑比例外推（做法與 `specs/f6f5.ts` 相同）：
   *
   * ```
   *   滾轉  k/(b/2)      0.2536 × 5.500 = 1.395
   *   俯仰  k/L          0.1627 × 9.177 = 1.493
   *   偏航  k/((b+L)/4)  0.4085 × 5.044 = 2.061
   * ```
   *
   * 乘上 2,733 kg 得 5,320 / 6,090 / 11,600。翼展與全長用**量到的**值
   *（`test/tools/a6m5-glb.probe.ts`：11.000 / 9.177）。
   *
   * **這是全專案最小的一組慣量**，比 Bf 109 K-4 還小 —— 零戰滾得輕、
   * 轉得快，那是質量與尺寸一起小的結果。
   */
  inertia: { pitch: 6090, yaw: 11600, roll: 5320 },

  /**
   * 翼面積 21.30 m²（日方；TAIC 230 sq ft ＝ 21.37）、翼展 11.00 m
   *（兩系一致，我們量到 11.000）。
   *
   * **翼負荷 128.3 kg/m² 是全專案最低**，比第二低的 Ki-84（171.4）還低 25%。
   * 那正是零戰的招牌，也是它持續迴旋突出的直接原因。
   *
   * 【平均氣動弦 2.00 m 是估算值，不是量測值】**任何語言、任何來源都查不到
   * A6M5 的 MAC 絕對值** —— 日美文件一律只用「% MAC」表示重心。做法是
   * 「一個真實的錨點 × 一個算得出來的比值」（設計文件 5.3）：
   *
   * ```
   *   S/b ＝ 21.30 ÷ 11.00 ＝ 1.936
   *   × 四台戰鬥機的 MAC ÷ (S/b) 平均 1.031
   *     （P-51D 1.023、K-4 1.033、F6F-5 1.027、Ki-84 1.042）
   *   ＝ 1.997 → 2.00 m
   * ```
   *
   * **Ki-84 那一格是有文件的**（MATSC 手冊 1.948 m），所以這條規則在本輪的
   * 兩台戰鬥機之間是自洽的，不是憑空發明。
   */
  wing: { area: 21.30, span: 11.00, chord: 2.00, oswald: 0.78 },

  /**
   * 【CL_max 1.727 —— 全專案最高，而且它是這台最可疑的一個數字】
   *
   * 由 TSFER-1973 的實測失速反解：74 mph clean @ 5,544 lb ＝ 2,514.7 kg、
   * S 21.30 → CL_max ＝ 1.727。**CL_max 不隨重量變**，所以換到 2,733 kg
   * 之後它一樣是 1.727，只有失速速度按 √W 縮放到 77.15 mph。
   *
   * 推導值 clAlpha × (alphaCrit − alphaZero) = 4.65 × 20.0° = 1.623，
   * 對 1.727 是 −6.0%，在 `specs.test.ts` 的 ±8% 內。
   *
   * 【alphaCrit 18.0° 是全專案最高，要說清楚為什麼接受】
   *
   * ```
   *   P-51D      15.5°     層流翼
   *   Ki-84      15.3°     層流化的中島系
   *   F6F-5      17.0°     NACA 230 系列厚翼
   *   A6M5       18.0°     ← 這裡
   * ```
   *
   * 支持它的：零戰的翼是三菱 118 番翼型（B-9 的肉付 ＋ NACA 23012 系的
   * 矢高線），**翼端捩り下げ 2.5°** 延後翼尖失速，翼負荷又是全專案最低。
   * 低速升力好本來就是這台的招牌。
   *
   * 反對它的：**那份報告的平飛數據自己註明「corrected for instrument error
   * only」，沒有做位置誤差修正**（同頁另有一張位置修正表，300 mph 處是
   * +12.6 mph，但沒有套用）。失速速度若也帶著位置誤差，真值會更高、
   * CL_max 會更低。
   *
   * **所以這是本台第一個該被推翻的數字。** 若日後找到做過位置修正的失速值，
   * 先動這裡。
   */
  lift: {
    clAlpha: 4.65,
    alphaZero: -2.0 * DEG,
    alphaCrit: 18.0 * DEG,
    stallBlend: 9 * DEG,
    postStallFactor: 0.65,   // 翼端捩り下げ 2.5°，失速後崩得比別台緩
    slatAlphaBonus: 0,
    slatDeployAlpha: Infinity,   // 無縫翼
    slatRetractAlpha: Infinity,
  },

  /**
   * 【調參掃描】`npx vite-node test/tools/japan-tune.probe.ts`
   */
  drag: { cd0: 0.019, cdBeta: 0.6, machCrit: 0.66, machDragFactor: 60 },

  side: { cyBeta: -0.72 },

  moments: {
    cm0: 0.02, cmAlpha: -0.80, cmQ: -12, cmDe: 1.20,
    clBeta: -0.085, clP: -0.42, clDa: 0.036,
    cnBeta: 0.10, cnR: -0.13, cnDr: 0.075,
  },

  /**
   * 【副翼在高速下重得離譜，這是零戰最重要的一個戰術特徵】
   * 美方對繳獲機的評語一致：低速滾轉極輕，但速度一上去副翼就鎖死。
   * `aileronK` 取 0.55 —— **全專案最高**，比 Bf 109 K-4 的 0.45 還重。
   * 這一項與低 `vne` 一起決定了「不要跟零戰纏鬥、用速度脫離」這條戰術。
   */
  controlStiffening: { qRef: 9000, aileronK: 0.55, elevatorK: 0.22, rudderK: 0.22 },

  engine: {
    /**
     * 栄二一型（中島 NK1F／ハ35-21），14 缸雙排星形，**單級二速**增壓
     *（⚠️ 英文資料常誤譯成 two-stage；日方寫的是「1段2速」）。
     *
     * ```
     *   離昇        1,130 PS   2,750 rpm  +300 mmHg   S.L.
     *   公称一速    1,100 PS   2,700 rpm  +200 mmHg   2,850 m   ← 填這個
     *   公称二速      980 PS   2,700 rpm  +200 mmHg   6,000 m   ← 與這個
     * ```
     *
     * 【沒有水甲醇噴射】水メタ是**栄三一型**才有的。所以這台**沒有戰鬥出力
     * 可填** —— 照 `specs/f6f5.ts` 的前例填公称出力，並在這裡寫明它與 P-51D
     * 的 throttle 1.1（WEP）不對等。這是平衡取捨，要不要改是負責人的決定。
     *
     * 【出處等級要誠實】這組 ratings 出自 ja.wikipedia〈栄 (エンジン)〉的
     * 規格表（三次來源），獨立佐證是 WarbirdPerformanceBlog 的火星／栄專頁
     *（1,130／1,100 @2,850／980 @6,000 三個值全對上）。真正的一次件
     *『榮發動機二〇型取扱説明書』（中島飛行機多摩製作所，昭和18年3月）
     * 沒有上網。
     *
     * 【一速的海平面出力沒有直接來源】1,080 PS 是由離昇 1,130（+300 mmHg）
     * 退到公称進氣壓（+200）推的；二速的 880 純粹是換檔曲線的形狀參數。
     */
    gears: [
      { powerSeaLevel: 1080 * PS, powerCritical: 1100 * PS, altCritical: 2850 },
      { powerSeaLevel: 880 * PS, powerCritical: 980 * PS, altCritical: 6000 },
    ],
    /**
     * 【0.15 —— 進氣衝壓完全沒有把峰值往上抬】飛機的極速峰值在 6,000 m，
     * 發動機的二速全開高度**也是 6,000 m**。對照 P-51D 的機腹進氣道抬了
     * 1,700 m（吃 0.90）、F6F-5 抬了 490 m（0.50）、Ki-84 抬了 300 m（0.35）。
     * 零戰的化油器進氣口在引擎罩下緣、又小又鈍，總壓恢復本來就差。
     */
    ramEfficiency: 0.15,
  },

  /**
   * 住友ハミルトン恒速式，**3.05 m 三葉**（實物銘板 M3-P1、発動機名称 NK1F、
   * 直径 3.050 m、ピッチ 高 49°/低 29°）。
   *
   * 【etaMax 0.87：槳盤載荷是全專案最低】
   *
   * ```
   *   A6M5     967 hp / (π × 1.525²) = 132 hp/m²   ← 這裡
   *   F6F-5  2,000 hp / (π × 1.995²) = 160 hp/m²   etaMax 0.88
   *   P-51D  1,720 hp / (π × 1.700²) = 189 hp/m²   etaMax 0.90
   *   Ki-84  1,598 hp / (π × 1.525²) = 219 hp/m²   etaMax 0.85
   * ```
   *
   * 載荷最低卻不是效率最高，因為它只有三葉而且槳徑相對機體偏小 ——
   * 0.87 介於 F6F-5 與 Ki-84 之間。
   */
  prop: { diameter: 3.05, etaMax: 0.87, vRef: 45, figureOfMerit: 0.8 },

  /**
   * 【vne 523 km/h 是實測的紅線，而且它是這台最重要的弱點】
   * TAIC Report No. 38（Eglin Field，1945-04）對繳獲機的實測：
   * **紅線 325 mph IAS**。
   *
   * 這比 P-51D 的 810、Bf 109 K-4 的 750 低得多，而那正是史實 ——
   * 盟軍對付零戰的標準戰術就是「不要纏鬥，俯衝脫離」，因為零戰跟不下來。
   * **這一項與 `controlStiffening.aileronK` 0.55 是同一個戰術的兩半。**
   *
   * 【過載限制】gPositive 7.0：零戰為了減重把結構做到極限，比 P-51D 的 8.0
   * 與 F6F-5 的 7.5 低。gNegative −3.0 同理。
   */
  limits: { gPositive: 7.0, gNegative: -3.0, vne: 325 * MPH },

  hp: 600,

  /**
   * 各部位的防護力。**這是全專案最脆的一張表**，而且有一手史料直接坐實。
   *
   * ```
   *   TAIC Report No. 38, Inclosure 4（1945-04）逐字：
   *   "The airplane has no protective armor plate, and no provision for
   *    leak proofing of internal fuel tanks."
   * ```
   *
   * TAIC 102D-3 的 VULNERABILITY 圖獨立佐證：兩具翼內油箱、胴體油箱、
   * 可拋副油箱**全部**畫成「Fuel tanks, unprotected」的細點網；
   * 整張圖沒有任何 armor plate 標註。
   *
   * **而且驗過對照組** —— 同一本手冊的三式戦（TONY）那一頁明寫
   * 「is equipped with leak proof internal tanks and armor」，另一型寫
   * 「12 mm armor plate behind…」。所以零戰那一頁沒標是**實質判斷，
   * 不是樣板漏印**。沒做對照組的話，「圖上沒有」與「圖沒畫」分不出來。
   *
   * ```
   *   cockpit   0.65   沒有鋼板、沒有防彈玻璃。**全專案唯一一台兩樣都沒有**
   *                    （P-51D 與 K-4 都是 1.15、F6F-5 是 1.25）
   *   wingL/R   0.65   翼內整體式油箱、無自封層 —— 打中機翼就是打中油箱
   *   fuselage  0.75   胴體油箱同樣無防護（對照 P-51D 的 0.80 是機腹散熱器）
   *   engine    1.10   栄是氣冷星形，沒有冷卻液可流失。這是它唯一的優點；
   *                    對照液冷的 P-51D 與 K-4 都是 0.85
   *   tail      1.00   沒有特別之處
   * ```
   *
   * 【護欄的下限是 0.60，為這一台放寬的】取 0.65 而不是 0.60，是為了
   * **留 0.05 的餘裕不坐在界限上**（Task 14 的
   * 教訓：三個出貨值恰好坐在界限上，之後沒有人知道那是量出來的還是被夾住的）。
   *
   * 【打起來差多少】座艙倍率 2.5，同一發 M2 .50 命中座艙：A6M5 吃
   * 18 × 2.5 ÷ 0.65 ＝ **69 點**，P-51D 吃 18 × 2.5 ÷ 1.15 ＝ 39 點。
   * 再配上血量 600 對 1,000，**打穿零戰座艙的成本是打穿野馬的 29%**。
   *
   * 【一個斷代結論】美軍 TAIC 直到 1945 年 3–4 月，實際飛測過的「Zeke 52」
   * 都還停在無防護的基本型／甲型階段；美方文件**從未記錄到乙型的 45 mm
   * 防彈玻璃與 CO₂ 滅火器，也沒記錄到丙型的鋼板**。這就是「零戰＝紙糊」
   * 在盟軍文件裡被固定下來的原因 —— 它對前期的零戰是準確的。
   */
  protection: {
    cockpit: 0.65, engine: 1.10, tail: 1.00,
    fuselage: 0.75, wingLeft: 0.65, wingRight: 0.65,
  },

  /**
   * 命中盒 —— 機翼與尾段由 `hitbox-emit.probe.ts` 產生，機首／座艙／機身
   * 三個由 `japan-hit.measure.ts` 的逐 z 帶幅度手切。
   *
   * 【引擎罩是一個獨立的黑殼】`A6M5_Cowl`（z −1.840…−0.780）與機身
   *（z −0.800…6.650）不相連，這是零戰造型的特徵（見
   * `.claude/skills/aircraft-from-reference`）。engine 盒同時包住整流罩、
   * 罩殼與機腹進氣口的前段。
   *
   * ```
   *   機翼正後方投影  1段 7.1(2盒)  2段 6.1  3段 5.4  4段 5.1  5段 4.8 …
   *   → 取 3 段（再切一段的改善 < 10%）
   *   尾段切分 |x| = 0.35　正後方 10.4 → 2.4 m²
   *
   *   正投影 m²      正後方  後上方30°  側方90°  正上方
   *   真實外形         4.7     16.3     11.6     30.7
   *   拆之後           8.6     25.6     16.6     38.7
   *   放大倍數        1.82×    1.58×    1.44×    1.26×
   * ```
   *
   * 產生腳本：`npx vite-node test/tools/hitbox-emit.probe.ts`
   * 量測腳本：`ID=a6m5 npx vite-node test/tools/japan-hit.measure.ts`
   */
  hitBoxes: [
    makeHitBox('engine', [-0.63, -0.79, -2.45], [0.63, 0.71, -0.78]),   // 整流罩＋引擎罩殼
    makeHitBox('cockpit', [-0.34, 0.58, -0.17], [0.34, 1.06, 2.20]),    // 座艙罩與框條
    makeHitBox('fuselage', [-0.60, -0.79, -0.80], [0.60, 0.86, 4.40]),  // 機身＋進氣口＋中央翼
    makeHitBox('tail', [-2.43, 0.10, 4.47], [2.43, 0.27, 5.99]),        // 平尾
    makeHitBox('tail', [-0.37, -0.36, 4.36], [0.37, 1.76, 6.76]),       // 垂尾＋尾錐
    makeHitBox('wingRight', [0.52, -0.60, -0.85], [2.23, 0.12, 1.97]),  // 右翼第 1 段
    makeHitBox('wingRight', [2.18, -0.44, -0.58], [3.89, 0.07, 1.66]),  // 右翼第 2 段
    makeHitBox('wingRight', [3.84, -0.22, -0.45], [5.54, 0.14, 1.36]),  // 右翼第 3 段
    makeHitBox('wingLeft', [-2.23, -0.60, -0.85], [-0.52, 0.12, 1.97]), // 左翼第 1 段
    makeHitBox('wingLeft', [-3.89, -0.44, -0.58], [-2.18, 0.07, 1.66]), // 左翼第 2 段
    makeHitBox('wingLeft', [-5.54, -0.22, -0.45], [-3.84, 0.14, 1.36]), // 左翼第 3 段
  ],

  /**
   * 機首兩挺 九七式 7.7 mm（同調）＋ 翼內兩門 九九式二〇粍二号四型。
   * 彈道、槍位推導與亞型的澄清都寫在 `weapons/a6m5.ts`。
   */
  battery: A6M5_BATTERY,
  turrets: [],
}

/**
 * 史實驗收值。**主幹是日方（栄二一型、2,733 kg）**，三項跨源，逐條標明。
 */
export const A6M5_HISTORICAL: HistoricalReference = {
  // 日方，推力式単排気管型。剛好落在栄二一型的二速全開高度上
  vmaxAtCritical: { speed: 565 * KMH, altitude: 6000 },
  /**
   * **跨源**：TAIC 102D 的 295 mph（War Emergency）。
   *
   * 【為什麼可以搬】TAIC 認定的 Sakae 31A 離昇是 1,120 hp，栄二一型是
   * 1,130 PS ＝ 1,115 hp —— **海平面兩者相差 0.4%**。兩款發動機真正的差別
   * 在二速全開高度（6,584 對 6,000 m），不在海平面。所以海面極速與海面
   * 爬升是可以搬的，臨界高度那一項不行。
   */
  vmaxSeaLevel: 295 * MPH,
  /**
   * **由日方的爬升時間換算，不是 TAIC 那個 3,140 ft/min。**
   *
   * 【為什麼不用 TAIC 的】海面極速可以搬（兩款發動機的海平面出力只差 0.4%），
   * 但**爬升不能** —— 爬升吃的是 0 到 6,000 m 一整段的出力，而那正是兩款
   * 發動機差最多的地方（二速全開高度 6,000 對 6,584 m）。實測兩個來源差 7%。
   *
   * 【換算】日方推力式単排気管型：**6,000 m ／ 7 分 01 秒** → 平均 14.25 m/s。
   * 那是平均不是海平面值，乘上本模型裡三台已校準戰鬥機的「海平面 ÷ 到
   * 6,000 m 平均」：
   *
   * ```
   *   P-51D 1.142   Bf 109 K-4 1.047   F6F-5 1.090   → 平均 1.093
   *   14.25 × 1.093 = 15.58 m/s = 935 m/min
   * ```
   *
   * **比值取自另外三台，不是 A6M5 自己** —— 用自己的模型算比值再拿去斷言
   * 自己，那是循環論證。
   *
   * 【非循環的檢查是時間本身】模型爬到 6,000 m 要 414 秒，日方 421 秒，
   * 差 −1.6%。那一項不經過任何換算。
   *
   * 【記下被換掉的那個】TAIC 102D 給 3,140 ft/min ＝ 15.95 m/s，比這裡高 2.4%。
   */
  climbRateSeaLevel: 15.58,
  /**
   * **跨源**：TSFER-1973（1946-03-13）對繳獲機 EB-2 的實飛，74 mph clean
   * @ 5,544 lb ＝ 2,514.7 kg。按 √W 修正到 2,733 kg 得 77.15 mph。
   *
   * ⚠️ **三個要記住的落差**：異重量 8.0%（已修正）、**異發動機**（那架是
   * Sakae 31，不影響失速）、而且**報告只做了儀器誤差修正、沒有做位置誤差
   * 修正**（見 `lift` 的討論）。
   */
  stallSpeed: 77.15 * MPH,
  /**
   * **跨源，而且是真的分歧**：日方 11,740 m 對 TAIC 的 35,100 ft ＝
   * 10,698 m，差 **9.7%**。走日方那一組就取日方值，TAIC 的記在這裡。
   */
  serviceCeiling: 11740,
  // 由上面的失速速度與 2,733 kg 反解，見 `lift`
  clMax: 2 * (2733 * 9.80665) / (1.225 * 21.30 * (77.15 * MPH) ** 2),
}
