import { DEG } from '../core/math'
import { makeHitBox } from '../world/hit'
import { KI84_BATTERY } from '../weapons/ki84'
import type { AircraftSpec, HistoricalReference } from './types'

/** 公制馬力（PS）。日方的出力一律以此為單位。 */
const PS = 735.5
const KMH = 1 / 3.6
const MPH = 0.44704

/**
 * Ki-84-I 甲 疾風。
 *
 * ══ 史實表的出處：**日方三次試飛的平均，不是那個 687 km/h** ═══════
 *
 * 流傳最廣的「687 km/h / 427 mph」**從來沒有人量過**。1946 年 Wright Field
 * 那份繳獲機報告（`F-IM-1119C-ND` ／ `TSFTE-2001`，T-2 Serial No. 302）的
 * FACTUAL DATA 一節**逐字照抄了 1945 年 3 月的 TAIC 156A-2 估算表** ——
 * 重量、燃油、四個速度、四個爬升率、升限、起飛滑跑、四個航程，一個數字都
 * 沒有改。而同一份報告第 11 頁自己寫著：
 *
 * ```
 *   5. Performance — None obtained.
 * ```
 *
 * 那架飛機在 Wright Field 總共只飛了 11.5 小時，**唯一真正量到的飛行數據是
 * 失速速度、操縱特性與俯衝限制**。TAIC 手冊自述的方針是「缺乏權威證據時，
 * 盡量從寬給予日本飛機性能評價」。
 *
 * 這裡用的是**日方三次試飛的平均**：
 *
 * ```
 *    km/h   高度 m   重量 kg   出力
 *     631    6,120    3,794    ハ45 定格 3,000 rpm +350 mmHg（船橋中尉）
 *     634    6,650   ≈3,400    同上（輕載試飛）
 *     624    6,550    未載明    ハ45**特**（＝ハ45-11，另一款發動機）
 *   ─────────────────────────────────────────────────────────
 *     630    6,400    3,600    ← 採用
 * ```
 *
 * 重量取兩個有載明者的平均 3,597，**恰好等於 TAIC 假定的 3,601.5** ——
 * 換掉的其實只有速度與高度，質量那一格兩條路殊途同歸。
 *
 * ══ 兩點反解把 cd0 夾在 0.021–0.024，出貨值 0.023 落在中間 ═══════
 *
 * 反解「要達成這組極速需要多少 cd0」（用出貨的 etaMax 0.85、vRef 72）：
 *
 * ```
 *   臨界高度 6,400 m、630 km/h、公称二速 1,620 PS   →  0.0240
 *   海平面        、545 km/h、公称一速 1,800 PS   →  0.0212
 *   ──────────────────────────────────────────────────────
 *   出貨 0.023 落在兩者中間，與 He 111 的做法一樣
 *  （那台兩點是 0.0202 / 0.0222，出貨 0.0206）
 * ```
 *
 * 【一個要記下來的錯誤】「兩點只差 0.6%」這個說法**是錯的** ——
 * 海平面那一點誤用了離昇出力 2,000 PS 而不是公称的 1,800 PS。改回同一個
 * 出力狀態之後兩點差 12.6%，與 He 111 的 9.5% 同一個量級。**這正是本專案
 * 一再強調「同一個出力狀態」的原因**：混了出力狀態的一致性是假的。
 *
 * ══ 誠實揭露 ═══════════════════════════════════════════════
 *
 * 1. **量產機實測約 580 km/h**（燃料品質、工藝劣化、誉的可靠度）。630 是
 *    試飛值，已經是偏樂觀的一端。
 * 2. **升限與失速是跨源的**，見 `KI84_HISTORICAL`。
 * 3. `en.wikipedia` 寫「687 km/h at 7,000 m」是**錯的** —— 它把原表兩行併成
 *    一行。原表是 W.E.P. 427 mph @ 20,000 ft 與 Military 426 mph @ 23,000 ft。
 */
export const KI84: AircraftSpec = {
  id: 'ki84',
  name: 'Ki-84 疾風',
  faction: 'japan',
  role: 'fighter',

  /**
   * # 3,600 kg —— 日方兩次有載明重量的試飛平均
   *
   * 3,794 kg（船橋中尉、第 4 號試作機）與約 3,400 kg（輕載試飛）的平均是
   * 3,597。日本陸軍的正規全備是 3,890 kg，TAIC 假定的 Normal Fighter 是
   * 3,601.5 kg —— **我們落在 TAIC 那一端**，也就是一架油帶得不滿的疾風。
   *
   * 【誠實揭露】比正規全備輕 7.4%。要換成 3,890 kg 的話，整組史實值都得
   * 跟著換（速度會掉、爬升會掉），而日方沒有 3,890 kg 的試飛紀錄。
   */
  mass: 3600,

  /**
   * 慣量 —— 沒有 Ki-84 的實測值，由 P-51D 的迴轉半徑比例外推（做法與
   * `specs/f6f5.ts` 相同）：
   *
   * ```
   *   滾轉  k/(b/2)      P-51D 1.430/5.64  = 0.2536 → × 5.619 = 1.425
   *   俯仰  k/L          P-51D 1.599/9.83  = 0.1627 → × 9.92  = 1.614
   *   偏航  k/((b+L)/4)  P-51D 2.156/5.278 = 0.4085 → × 5.290 = 2.161
   * ```
   *
   * 乘上 3,600 kg 得 7,310 / 9,380 / 16,810。翼展與全長用**量到的**值
   *（`test/tools/ki84-glb.probe.ts`：11.240 / 9.906），不是史實表上的 ——
   * 慣量描述的是我們畫出來的那一台。
   *
   * 【滾轉比俯仰小很多】翼展 11.24 對機身 9.92，比例與 P-51D 同向
   *（那台是 11.28 對 9.83）。疾風滾得快是這個比例的結果，不是算錯。
   */
  inertia: { pitch: 9380, yaw: 16810, roll: 7310 },

  /**
   * 翼面積 21.00 m²（美日兩系完全一致：TAIC 226 sq ft ＝ 21.00、日方 21.00）、
   * 翼展 11.238 m（日方；美方 37.1 ft ＝ 11.308，我們量到 11.240）。
   *
   * 平均氣動弦 1.948 m 出自 **MATSC 手冊的「Weight and Balance Data」，
   * 那是唯一給出 MAC 絕對值的來源** —— 日美文件一律只用「% MAC」表示重心。
   *
   * oswald 0.80：與 F6F-5 同一檔（一般翼型 ＋ 大直徑星形發動機的粗機身），
   * 低於 P-51D 層流翼的 0.85。
   */
  wing: { area: 21.00, span: 11.238, chord: 1.948, oswald: 0.80 },

  /**
   * 【CL_max 由失速反推，不是抄來的】3,600 kg 在海平面失速 164.6 km/h：
   *
   *   CL_max = 2W / (ρ S V²) = 2 × 35,304 / (1.225 × 21.00 × 45.72²) = 1.313
   *
   * 推導值 clAlpha × (alphaCrit − alphaZero) = 4.5 × 16.8° = 1.319，
   * 對 1.313 是 +0.5%，遠在 `specs.test.ts` 的 ±8% 內。
   *
   * 【比 F6F-5 的 1.515 低是對的】那台是 NACA 230 系列的厚翼配分裂式襟翼；
   * 疾風的翼薄得多（翼根 15.5% 對 23015 的 15%，但翼型是層流化的中島系）。
   */
  lift: {
    clAlpha: 4.5,
    alphaZero: -1.5 * DEG,
    alphaCrit: 15.3 * DEG,
    stallBlend: 8 * DEG,
    postStallFactor: 0.6,
    slatAlphaBonus: 0,
    slatDeployAlpha: Infinity,   // 無縫翼
    slatRetractAlpha: Infinity,
  },

  /**
   * 【0.023 —— 由兩點反解夾出來的，見檔頭】落在 F6F-5（0.0195）與
   * Bf 109 K-4（0.0279）之間，對一台裝 1,180 mm 直徑誉發動機的粗機身
   * 戰鬥機是相稱的。
   *
   * 【只退回本項、其餘出貨值的實測】
   *
   * ```
   *   cd0      臨界極速   海面極速   爬升     升限     到 5,000 m
   *   0.021     +4.9%      +0.3%    +1.7%   −2.6%      −4.2%
   *   0.022     +3.1%      −1.4%    +0.3%   −3.1%      −2.8%
   *   0.023     +1.4%      −3.1%    −1.1%   −3.7%      −1.4%   ← 出貨
   *   0.025     −1.9%      −6.2%    −2.6%   −4.8%      +1.4%
   * ```
   *
   * 【調參掃描】`npx vite-node test/tools/japan-tune.probe.ts`
   */
  drag: { cd0: 0.023, cdBeta: 0.65, machCrit: 0.68, machDragFactor: 60 },

  side: { cyBeta: -0.75 },

  moments: {
    cm0: 0.02, cmAlpha: -0.85, cmQ: -13, cmDe: 1.15,
    clBeta: -0.09, clP: -0.48, clDa: 0.032,
    cnBeta: 0.11, cnR: -0.14, cnDr: 0.07,
  },

  /**
   * 【副翼衰減比 P-51D 略好】美方對繳獲機的評語：IAS 74–350 mph 全區間無
   * 控制面反效或平台區，300 mph IAS 以上舵面才明顯變硬
   *（`TSFTE-2001`，1946-07-16 實飛）。
   */
  controlStiffening: { qRef: 10884, aileronK: 0.30, elevatorK: 0.25, rudderK: 0.25 },

  engine: {
    /**
     * ハ45-21（誉二一型），18 缸雙排星形，**單級二速**增壓。
     *
     * 【型式是硬證據判定的，不是文獻轉述】MATSC 手冊第 14 頁記載增壓器葉輪
     * 增速比 **5.81 與 7.95**，與日方 ハ45-21 的規格完全吻合（ハ45-11 是
     * 5.47／7.49）。這也解釋了日方那三次試飛裡 624 km/h 那一次為什麼慢 ——
     * 它用的是 ハ45**特**（＝ハ45-11），二速全開高度只有 5,700 m。
     *
     * 【填公称出力，不是 W.E.P.】日方那三次試飛寫的是「定格 3,000 rpm、
     * +350 mmHg」，那就是公称。**史實表與出力狀態必須一致**，不然反解出來
     * 的每一個係數都在補一個不存在的差額（P-51D 那一輪的教訓）。
     *
     * ```
     *   離昇        2,000 PS   3,000 rpm  +500 mmHg   S.L.
     *   公称一速    1,860 PS   3,000 rpm  +350 mmHg   1,750 m   ← 填這個
     *   公称二速    1,620 PS   3,000 rpm  +350 mmHg   6,100 m   ← 與這個
     * ```
     *
     * 【兩個海平面出力沒有直接來源】一速的 1,800 PS 是由離昇 2,000
     *（+500 mmHg）退到公称進氣壓推的；二速的 1,450 PS 純粹是換檔曲線的
     * 形狀參數。**五項驗收對這兩個數字幾乎無感**（它們只影響換檔那一段），
     * 守的是「爬升曲線不要有假凹谷」，與 `specs/f6f5.ts` 的中立檔同一個處境。
     *
     * 【美日兩系在這一塊吻合得非常好，可以互相信任】
     *
     * ```
     *                        一速全開              二速全開
     *   美方 Military      1,875 hp @ 1,798 m    1,695 hp @ 6,096 m
     *   日方 ハ45-21 公称  1,860 PS @ 1,750 m    1,620 PS @ 6,100 m
     *   差                 出力 +0.8%、高度 +48 m  出力 +4.6%、高度 −4 m
     * ```
     */
    gears: [
      { powerSeaLevel: 1800 * PS, powerCritical: 1860 * PS, altCritical: 1750 },
      { powerSeaLevel: 1450 * PS, powerCritical: 1620 * PS, altCritical: 6100 },
    ],
    /**
     * 【0.35 —— 進氣衝壓幾乎沒有把峰值往上抬】飛機的極速峰值在 6,400 m，
     * 發動機的二速全開高度是 6,100 m，只抬了 **300 m**。對照 P-51D 的機腹
     * 進氣道抬了 1,700 m（吃 0.90）、F6F-5 的下巴進氣口抬了 490 m（吃 0.50），
     * 疾風的更小。
     */
    ramEfficiency: 0.35,
  },

  /**
   * ラチエ ペ32 電動定速槳，**3.05 m 四葉**（美方對繳獲機量到 3.097）。
   *
   * 【etaMax 0.85：全專案最低的一台戰鬥機，理由是槳盤載荷最高】
   *
   * ```
   *   Ki-84   1,598 hp / (π × 1.525²) = 219 hp/m²   ← 這裡
   *   P-51D   1,720 hp / (π × 1.700²) = 189 hp/m²   etaMax 0.90
   *   F6F-5   2,000 hp / (π × 1.995²) = 160 hp/m²   etaMax 0.88
   * ```
   *
   * 葉數不是重點，槳盤載荷才是（見 `specs/f6f5.ts`）。疾風用 3.05 m 的槳
   * 吃 1,600 hp，載荷比 P-51D 高 16%，峰值效率低一檔是物理上該有的。
   *
   * 【etaMax 是上界不是工作值】不要拿它反推 cd0 —— 那會系統性高估阻力
   * 10–18%（本專案已經被咬過兩次）。上面的 cd0 是由 `envelope` 反解極速
   * 得到的，用的是模型自己的 η(V)。
   *
   * ══ vRef 72 —— 全專案最高，而且它一個人扛下整個爬升修正 ═══════
   *
   * **這是這台最需要被質疑的一個值**，所以完整寫下來。
   *
   * 校準時的病灶：**兩個極速都對得上（±3% 內），爬升卻高 30%。**
   * 未調之前模型爬到 5,000 m 只要 273 秒，日方三次試飛的平均是 359 秒。
   *
   * η(V) = etaMax × (1 − e^(−V/vRef))。vRef 是**唯一能把高速與低速分開**的
   * 旋鈕：抬高它會讓效率更晚才建立起來，所以低速端掉得多、高速端掉得少。
   *
   * ```
   *   vRef       η @60 m/s（爬升）   η @175 m/s（極速）
   *    45            0.628               0.833
   *    72            0.480               0.775
   *   變化           −23.6%              −7.0%
   * ```
   *
   * 反解要多高：把爬升砍 30% 需要 η(60) 由 0.628 降到 0.48 →
   * 1 − e^(−60.4/vRef) = 0.565 → **vRef = 72.6**。掃描出來的最佳值 72
   * 與這個手算一致，不是湊出來的。
   *
   * 【物理上說得通嗎】部分說得通：槳盤載荷 219 hp/m² 是全專案最高，
   * 高載荷意味著粗槳距，粗槳距的效率本來就要更高的速度才建立起來。
   * 但 72 對 P-51D 的 45 是 60% 的差距，而載荷只差 16% —— **這個旋鈕承擔的
   * 比它應得的多。**
   *
   * 【所以它是第一個該被推翻的值】如果日後有人找到誉在兩個全開高度**之間**
   * 那一段的實測功率曲線（現在 `gears[1].powerSeaLevel` 是猜的，見上方），
   * 或找到 ラチエ ペ32 的實測效率曲線，**先動這裡**。
   */
  prop: { diameter: 3.05, etaMax: 0.85, vRef: 72, figureOfMerit: 0.8 },

  /**
   * 【vne 740 km/h 是實測的】`TSFTE-2001`：5,000 ft 以上不得超過
   * **460 mph IAS**。那是那架繳獲機唯一真正量到的三項之一。
   */
  limits: { gPositive: 7.5, gNegative: -3.5, vne: 460 * MPH },

  hp: 800,

  /**
   * 各部位的防護力。1.0 = 基準，扣血 = 傷害 × 部位倍率 ÷ 這裡的值。
   *
   * **Ki-84 是日本戰鬥機裡防護最好的一款之一** —— 防漏箱、防彈鋼板、
   * 防彈玻璃三樣都有，這在日本陸海軍機裡並不常見（對照 `specs/a6m5.ts`
   * 那台三樣都沒有）。
   *
   * ```
   *   cockpit  1.25   座椅後方 13 mm 鋼板（美方對繳獲機的實機檢查：
   *                   「covers the pilot's shoulders to the lower part of
   *                   the waist」，MATSC 手冊 p.12）＋前風防 65 mm 防彈玻璃
   *   engine   1.20   氣冷 18 缸誉，沒有冷卻液可以流失。對照液冷的 P-51D
   *                   與 Bf 109 K-4 都是 0.85
   *   fuselage 1.10   胴體主油箱是防漏箱（F-IM-1119C-ND p.1：
   *                   "The fuel tanks are self-sealing"）
   *   wing     1.05   翼內四個油箱同樣是防漏箱；TAIC 156A-3 的 VULNERABILITY
   *                   圖把**機內油箱全部**畫成 "Fuel tanks, protected"
   *   tail     1.00   沒有特別之處
   * ```
   *
   * 【厚度兩系對不上，但差 5–8%，不影響一個 0.05 級的格子】防彈玻璃美方
   * 65 mm、日方 70 mm；鋼板美方 13 mm、日方 12.4 mm。前者是情報與實機檢查，
   * 後者是 ja.wikipedia 的無腳註敘述，**分不出哪一份對**。
   *
   * 【它沒有滅火裝置】`F-IM-1119C-ND` p.1：「there are no fire extinguishers
   * nor emergency escape means」，MATSC p.3 獨立佐證。這一項是**沒有**不是
   * 查不到 —— 日方的防弾装備節逐條列了防漏箱、鋼板、玻璃，完全沒提消火。
   * 本模型沒有起火機制，所以它現在不影響任何數字，記在這裡是為了日後有人
   * 加起火模型時知道這台該偏哪一邊。
   */
  protection: {
    cockpit: 1.25, engine: 1.20, tail: 1.00,
    fuselage: 1.10, wingLeft: 1.05, wingRight: 1.05,
  },

  /**
   * 命中盒 —— 機翼與尾段由 `hitbox-emit.probe.ts` 產生，機首／座艙／機身
   * 三個由 `ki84-hit.measure.ts` 的逐 z 帶幅度手切（那支探針不動這三個）。
   *
   * ```
   *   機翼正後方投影  1段 6.9(2盒)  2段 5.9  3段 5.1  4段 4.7  5段 4.4 …
   *   → 取 3 段（再切一段的改善 < 10%）
   *   尾段切分 |x| = 0.30　正後方 8.9 → 1.9 m²
   *
   *   正投影 m²      正後方  後上方30°  側方90°  正上方
   *   真實外形         4.3     16.1     12.6     30.6
   *   拆之後           8.6     25.6     20.1     39.1
   *   放大倍數        1.99×    1.59×    1.60×    1.28×
   * ```
   *
   * 【機首兩挺同調槍靠 engine 盒接住】`hitbox-emit` 只把**展向落在機翼段
   * 範圍內**的掛架併進機翼盒，機首那兩挺（x ±0.18）不在裡面。
   *
   * 產生腳本：`npx vite-node test/tools/hitbox-emit.probe.ts`
   * 量測腳本：`npx vite-node test/tools/ki84-hit.measure.ts`
   */
  hitBoxes: [
    makeHitBox('engine', [-0.64, -0.73, -2.81], [0.64, 0.74, -1.35]),   // 整流罩＋槳轂＋引擎罩
    makeHitBox('cockpit', [-0.28, 0.71, 0.55], [0.28, 1.10, 2.73]),     // 座艙罩與框條
    makeHitBox('fuselage', [-0.64, -1.00, -1.35], [0.64, 0.95, 4.85]),  // 機身＋機腹整流體＋中央翼
    makeHitBox('tail', [-2.30, 0.33, 4.85], [2.30, 0.48, 6.17]),        // 平尾
    makeHitBox('tail', [-0.32, -0.28, 4.77], [0.32, 1.64, 7.12]),       // 垂尾＋尾錐
    makeHitBox('wingRight', [0.57, -0.63, -0.84], [2.30, 0.07, 1.81]),  // 右翼第 1 段
    makeHitBox('wingRight', [2.25, -0.48, -0.85], [3.99, -0.04, 1.47]), // 右翼第 2 段
    makeHitBox('wingRight', [3.93, -0.27, -0.65], [5.65, 0.04, 1.12]),  // 右翼第 3 段
    makeHitBox('wingLeft', [-2.30, -0.63, -0.84], [-0.57, 0.07, 1.81]), // 左翼第 1 段
    makeHitBox('wingLeft', [-3.99, -0.48, -0.85], [-2.25, -0.04, 1.47]),// 左翼第 2 段
    makeHitBox('wingLeft', [-5.65, -0.27, -0.65], [-3.93, 0.04, 1.12]), // 左翼第 3 段
  ],

  /**
   * 機首兩挺 ホ103 12.7 mm（同調）＋ 翼內兩門 ホ5 20 mm（自由射擊）。
   * 彈道、槍位推導與「哪一個數字最弱」都寫在 `weapons/ki84.ts`。
   */
  battery: KI84_BATTERY,
  turrets: [],
}

/**
 * 史實驗收值。**主幹是日方三次試飛的平均**（見 `KI84` 檔頭），
 * 兩項跨源、一項推導，逐條標明。
 */
export const KI84_HISTORICAL: HistoricalReference = {
  // 日方三次試飛平均 630 km/h；高度取三者（6,120／6,650／6,550）平均 6,440
  vmaxAtCritical: { speed: 630 * KMH, altitude: 6400 },
  // 日方：輕載試飛 545、岩橋 544
  vmaxSeaLevel: 545 * KMH,
  /**
   * **推導值，不是量測值。** 日方給的是到 5,000 m 的時間，不是海平面爬升率：
   *
   * ```
   *   5′54″（3,794 kg）  5′37″（≈3,400 kg）  6′26″（ハ45特）
   *   平均 359 秒 → 到 5,000 m 的平均爬升率 13.93 m/s
   * ```
   *
   * 本模型裡三台已校準的戰鬥機「海平面 ÷ 到 5,000 m 平均」是 1.126（P-51D）、
   * 1.038（K-4）、1.069（F6F-5），平均 **1.078**。13.93 × 1.078 ＝ 15.0。
   *
   * 【這個換算引入約 ±4% 的不確定度】三個比值的散佈就是那麼寬，而驗收容差
   * 是 ±5%，餘裕很薄。**這一項如果紅了，先看換算不要先動係數。**
   */
  climbRateSeaLevel: 15.0,
  /**
   * **跨源，但接縫非常窄。** 102 mph IAS 來自 `TSFTE-2001`（1946-07-16）對
   * **同一架飛機**（T-2 Serial No. 302）的實飛，報告重量 7,900 lb ＝
   * 3,583 kg，對我們的 3,600 kg 差 0.5%。而且**失速不吃發動機出力**，
   * 所以出力狀態不同在這一項不構成問題。
   *
   * 102 mph 是 IAS、在 8,000 ft 壓力高度量的。IAS ≈ EAS，所以它等價於
   * 海平面的 164.6 km/h，可以直接對 `stallSpeed(spec, 0, 1)` ——
   * **不要另外做高度修正。**
   */
  stallSpeed: 102.2 * MPH,
  /**
   * **跨源。** 日方官方諸元，對應正規全備 3,890 kg 而不是我們的 3,600 kg。
   * TAIC 156A-2 給 38,800 ft ＝ 11,826 m。兩個相差 4.6%，都記在這裡。
   */
  serviceCeiling: 12400,
  // 由上面的失速速度與 3,600 kg 反解，見 `lift`
  clMax: 2 * (3600 * 9.80665) / (1.225 * 21.00 * (102.2 * MPH) ** 2),
}
