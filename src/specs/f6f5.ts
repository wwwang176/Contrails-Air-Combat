import { DEG } from '../core/math'
import { makeHitBox } from '../world/hit'
import { F6F5_BATTERY } from '../weapons/f6f5'
import type { AircraftSpec, HistoricalReference } from './types'

const HP = 745.7
const KMH = 1 / 3.6
const MPH = 0.44704
const FT = 0.3048
const FPM = 0.00508
const LB = 0.45359237

/**
 * F6F-5 Hellcat。
 *
 * ══ 史實表的出處：**一份試飛報告，一個重量，一個出力狀態** ═══════
 *
 * 全部四項驗收值取自同一份文件 —— Patuxent River 海軍航空試驗中心
 * 1944-09-07，F6F-5 No. 58310，**"Overload Fighter" 總重 12,420 lb**
 * （報告的圖表抬頭就是這麼寫的）：
 *
 * ```
 *   軍用出力 臨界高度        23,100 ft   極速 391.0 mph
 *   軍用出力 海平面爬升率     2,660 fpm
 *   實用升限                37,100 ft
 *   失速（乾淨、無動力）       98.0 mph      （放襟翼無動力 84.5 mph）
 * ```
 *
 * 【為什麼堅持同一份】技能坑 21 與專案自己的教訓：史實性能表很常混著不同
 * 掛載狀態的數字，混進同一張表之後症狀看起來完全像是模型壞了。P-51D 那一輪
 * （2026-08-27）就是被這件事咬到的。這裡從一開始就只用一份報告，四項互相
 * 對得起來是**前提**而不是結果。
 *
 * 【誠實揭露：這是軍用出力，不是加水噴射的戰鬥出力】
 * 上面四項全部是 military power（2,700 rpm、54"Hg、不噴水）。`engine.gears`
 * 因此也填軍用出力，而不是欄位註解說的 WEP 值 —— **必須同一個出力狀態，
 * 不然反解出來的每一個係數都在補一個不存在的差額。**
 *
 * 為什麼不用戰鬥出力：F6F-5 加水噴射的完整表只有 F6F-3 那一份
 * （1945-04-24 Combat Power Evaluation），而且它顯示噴水會把臨界高度**壓低**
 * （高增壓檔 15,080–18,000 ft、381–382.5 mph），極速反而比軍用出力的
 * 391 mph @ 23,100 ft 低。拿 F6F-3 的戰鬥出力配 F6F-5 的軍用升限，就是在
 * 重蹈那張混表的覆轍。
 *
 * **代價**：遊戲裡 P-51D 的 throttle 1.1 是 WEP，這台是軍用出力，兩者的
 * 「全開」不對等。這是平衡取捨，要不要改由專案負責人裁決。
 *
 * 資料來源：wwiiaircraftperformance.org 的 F6F Performance Trials 檔案室。
 */
export const F6F5: AircraftSpec = {
  id: 'f6f5',
  name: 'F6F-5 Hellcat',
  faction: 'allied',
  role: 'fighter',

  /**
   * # 5,634 kg —— **試飛報告上的重量，一公斤都沒有校準過**
   *
   * 12,420 lb 就是 1944-09-07 那份報告的總重。這是本專案**唯一**一台不必
   * 動質量的飛機：
   *
   * ```
   *   只動質量，史實這三項各自要求多重（其餘全為出貨值）
   *                              要求         扣掉 clMax 偏差後
   *     海平面爬升 811 m/min  →  5,610 kg（99.6%）
   *     實用升限  11,308 m    →  5,568 kg（98.8%）
   *     失速   157.7 km/h     →  5,285 kg（93.8%）  →  5,634 kg（100.0%）
   *                              ─────────────────────────────────
   *                              三者聚在 1.2% 之內，位置就是試飛重量
   * ```
   *
   * 【失速那一項為什麼要扣偏差】它反解的是 m/CL_max，而模型的推導 CL_max
   * 是 1.421、史實那個數字要求 1.515（−6.2%，見 `lift`）。5,285 ×
   * (1.515/1.421) = 5,634 —— 也就是說失速那一項**完全**由 CL_max 的差額
   * 解釋，它本身沒有指向任何別的重量。P-51D 的 `mass` 註解做的是同一件事
   *（「已扣掉模型 clMax 與史實 1.45 的偏差」）。
   *
   * 【為什麼這件事值得寫下來】P-51D 曾經因為史實表混了載重狀態（極速／爬升／
   * 升限來自 9,760 lb 的試飛、失速卻屬於約 8,600 lb），三項反解「都指向
   * 3,900 kg」，質量因此被校準掉 —— 真正的成因是 `engine.gears` 填錯。
   * 這台不會踩到那個坑，因為四項驗收值來自同一份報告的同一次試飛。
   * 那正是堅持單一來源買到的東西。
   *
   * 【誠實揭露：載重狀態仍然與另外兩台不對等】12,420 lb 是報告寫的
   * "Overload Fighter"，不是 F6F-5 的正常戰鬥重量（約 12,600 lb）也不是
   * 滿掛（15,415 lb）。它與 P-51D 的 4,427 kg 試飛重量並不對等，但兩台
   * 現在**都是有文件的實際飛行重量**，而不是校準值。
   */
  mass: 5634,

  /**
   * 慣量 —— 沒有 F6F 的實測值，由 P-51D 的迴轉半徑比例外推：
   *   滾轉  k/(b/2)   P-51D 1.430/5.64 = 0.2536 → F6F 0.2536 × 6.53 = 1.656
   *   俯仰  k/L       P-51D 1.599/9.83 = 0.1627 → F6F 0.1627 × 10.24 = 1.666
   *   偏航  k/((b+L)/4) P-51D 2.156/5.278 = 0.4085 → F6F 0.4085 × 5.825 = 2.380
   * 乘上 5,634 kg 得 15,450 / 15,640 / 31,920。
   *
   * 【滾轉慣量和俯仰幾乎一樣大是對的】P-51D 是滾轉 < 俯仰，因為它翼展短。
   * F6F 翼展 13.06 對機身 10.24，比例反過來 —— 這正是 Hellcat 滾得慢的原因
   * 之一，不是算錯。
   */
  inertia: { pitch: 15600, yaw: 31900, roll: 15450 },

  /**
   * 翼展 42 ft 10 in = 13.06 m、翼面積 334 ft² = 31.03 m²（兩者都是真機值，
   * 而且與 Blender 模型逐站量到的平面形一致：13.06 × (3.235+1.513)/2 = 31.00）。
   * 平均氣動弦取真機的 8 ft = 2.44 m（量到的梯形給 2.48，+1.6%）。
   *
   * oswald 0.80：NACA 230 系列的一般翼型 + 粗機身，比 P-51D 的層流翼
   * （0.85，取教科書區間上緣）低一檔。
   */
  wing: { area: 31.03, span: 13.06, chord: 2.44, oswald: 0.80 },

  /**
   * 【CL_max 是從失速反推的，不是抄來的】12,420 lb 在海平面失速 98.0 mph：
   *
   *   CL_max = 2W / (ρ S V²) = 2 × 55,266 / (1.225 × 31.03 × 43.81²) = 1.515
   *
   * 1.515 對乾淨構型偏高，但它與同一份報告的放襟翼失速 84.5 mph 自洽
   * （襟翼帶來 +34% CL_max，分裂式襟翼的合理值）。
   *
   * 推導值 clAlpha × (alphaCrit − alphaZero) = 4.4 × 18.5° = 1.421，
   * 對 1.515 是 −6.2%，在 specs.test.ts 的 ±8% 內。alphaCrit 再往上就變成
   * 不合物理的失速迎角了。
   */
  lift: {
    clAlpha: 4.4,
    alphaZero: -1.5 * DEG,   // NACA 23015 翼根／23009 翼尖，彎度比層流翼小
    alphaCrit: 17.0 * DEG,
    stallBlend: 8 * DEG,
    postStallFactor: 0.6,
    slatAlphaBonus: 0,
    slatDeployAlpha: Infinity,   // 無縫翼
    slatRetractAlpha: Infinity,
  },

  /**
   * 【只退回本項、其餘出貨值的實測】五項驗收 vs 史實：
   *
   * ```
   *   cd0      臨界極速   海面極速   爬升      升限      失速
   *   0.0175    +1.3%     +3.8%    −2.9%    −0.5%    +3.3%
   *   0.0185    −0.7%     +1.8%    −4.2%    −1.2%    +3.3%
   *   0.0195    −2.6%     −0.1%    −5.5%    −1.8%    +3.3%   ← 出貨（配 etaMax 0.88）
   *   0.0210    −5.2%     −2.8%    −7.2%    −2.7%    +3.3%
   * ```
   *
   * 【誠實揭露：0.0195 比文獻的 F6F CD0 估值（約 0.0211）低 7.6%】接受的理由
   * 與 P-51D 的 `oswald` 同一條：本模型的 cd0 是常數、不含 CL 相依的黏性項，
   * 它承擔的是「整條極速曲線」而不是零升阻力本身。真要把 cd0 拉到 0.0211
   * 得把 etaMax 推到 0.90（見 `prop`），而那是螺旋槳的物理上限 —— 寧可讓
   * 阻力側吃 7.6%，也不要把螺旋槳頂死。
   */
  drag: { cd0: 0.0195, cdBeta: 0.65, machCrit: 0.68, machDragFactor: 60 },

  side: { cyBeta: -0.75 },

  moments: {
    cm0: 0.02, cmAlpha: -0.85, cmQ: -13, cmDe: 1.15,
    clBeta: -0.09, clP: -0.48, clDa: 0.030,
    cnBeta: 0.11, cnR: -0.14, cnDr: 0.07,
  },

  // F6F-5 中期之後裝彈簧補償片副翼，高速滾轉權限比 P-51D 略好
  controlStiffening: { qRef: 10884, aileronK: 0.32, elevatorK: 0.25, rudderK: 0.25 },

  engine: {
    /**
     * R-2800-10W Double Wasp，18 缸雙排星形，二段三速增壓（中立／低／高）。
     * **軍用出力**（2,700 rpm、54"Hg、不噴水），見檔頭的說明。
     *
     * 臨界高度取的是**引擎的**不是飛機的：飛機臨界高度 23,100 ft 含進氣衝壓，
     * 而衝壓在本模型是 `ramEfficiency` 另外算的，寫進來會重複計一次。
     */
    gears: [
      { powerSeaLevel: 1975 * HP, powerCritical: 2000 * HP, altCritical: 5500 * FT },
      { powerSeaLevel: 1800 * HP, powerCritical: 1800 * HP, altCritical: 16900 * FT },
      { powerSeaLevel: 1550 * HP, powerCritical: 1650 * HP, altCritical: 21500 * FT },
    ],
    /**
     * 【中立檔的臨界高度與低增壓檔的海平面出力是照「爬升曲線的形狀」定的】
     * 五項驗收對這兩個數字**完全無感**（極速、爬升、升限、失速一位數都沒動），
     * 因為它們只影響 0–5,000 m 這一段的換檔行為。定它們的是另一件事：
     *
     * ```
     *   高度 m                0   1000   2000   3000   4000   5000   6000
     *   中立檔 1,000 ft     806    762    669    680    689    696    620
     *   中立檔 5,500 ft     806    816    813    684    689    696    620
     *   ＋低檔 SL 1,800 hp  806    816    813    705    703    700    620   ← 出貨
     * ```
     *
     * 1,000 ft 那一版在 2,000 m 有一個 −17% 的**凹谷**，那是兩檔之間的空隙，
     * 不是物理。真機在低增壓檔到 11,200 ft 的爬升幾乎是平的
     *（Boscombe Down 的 Hellcat II JV.224，1945-05-11）。
     *
     * 【誠實揭露：這一段沒有直接量測背書】58310 報告有爬升曲線圖，但線上只有
     * 950 px 的掃描，讀不出數字。所以這裡守的是「形狀要平、不要有假凹谷」，
     * 不是「每一站對得上」。
     */
    /**
     * 【比 P-51D 低很多，而且是量出來的】P-51D 的機腹進氣道把極速峰值由
     * 引擎臨界高度 5,900 m 抬到 7,600 m，抬了 1,700 m，所以它吃 0.90。
     * F6F 的飛機臨界高度 23,100 ft 對引擎的 21,500 ft，只抬了 1,600 ft
     * ≈ 490 m —— 下巴進氣口 + 大直徑星形引擎，總壓恢復本來就差得多。
     */
    ramEfficiency: 0.5,
  },

  /**
   * Hamilton Standard 定速槳，13 ft 1 in = 3.99 m，三葉。
   *
   * 【etaMax 0.88：比 P-51D 低，但比「三葉所以比較差」的直覺高】0.90 是
   * 1940 年代定速槳的物理上限，P-51D 的四葉槳吃滿了它。葉數不是重點，
   * **槳盤載荷**才是：
   *
   *     F6F-5   2,000 hp / (π × 1.995²) = 160 hp/m²
   *     P-51D   1,720 hp / (π × 1.700²) = 189 hp/m²
   *
   * Hellcat 的槳盤大得多、載荷低 15%，峰值效率高一點是物理上該有的，不是
   * 為了湊爬升硬拉的。0.88 仍留 0.02 給上限。
   *
   * 【只退回本項、其餘出貨值的實測（cd0 = 0.0195）】
   *   etaMax 0.87：臨界 −1.6%、海面 +0.7%、爬升 −2.2%、升限 −1.0%
   *   etaMax 0.88：臨界 −1.2%、海面 +1.2%、爬升 −0.6%、升限 −0.6%   ← 出貨
   *
   * 【etaMax 是上界不是工作值】所以不要拿它去反推 cd0 —— 那會系統性高估
   * 阻力 10–18%（本專案已經被咬過兩次）。上面的 cd0 是由 `envelope` 反解
   * 極速得到的，用的是模型自己的 η(V)。
   */
  prop: { diameter: 3.99, etaMax: 0.88, vRef: 45, figureOfMerit: 0.8 },

  limits: { gPositive: 7.5, gNegative: -3.5, vne: 725 * KMH },

  hp: 1000,

  /**
   * 命中盒 —— **切分平面是人訂的，六個盒的六個面全部是量出來的。**
   *
   * 量測腳本 `test/tools/f6f-hit.measure.ts`：讀 `public/models/f6f5.glb`
   * 的八個節點（Fuselage / Wing / Tailplane / Fin / Canopy / CowlFace /
   * Spinner / Prop），依下面三個平面分堆，每一堆取緊包圍盒再往外讓 0.01。
   *
   * ```
   *   防火牆   z = −1.00     引擎與機身的分界
   *   平尾前緣 z =  5.16     機身與尾段的分界（翼根前緣量到 5.180）
   *   機身側面 x = ±0.76     機身與機翼的分界（機身最寬量到 0.756）
   * ```
   *
   * 三個平面之外的兩個數字：座艙盒的 z 0.28…1.45 是座艙罩本體
   *（量到 0.300…1.443），y 下界 0.05 是**地板高度的約定**，與 P-51D、
   * K-4 同一個訂法 —— 座艙倍率 2.5 蓋到駕駛員的腿，不是只蓋玻璃。
   *
   * ```
   *   盒        緊包圍盒（量測）                      採用
   *   engine    ±0.719  −0.899…0.916  −2.932…−1.341   ±0.72  −0.90…0.92  −2.94…−1.00
   *   cockpit   ±0.480   0.783…1.513   0.300… 1.443   ±0.48   0.05…1.52   0.28… 1.45
   *   fuselage  ±0.756  −0.958…1.410  −0.983… 5.085   ±0.76  −0.96…1.42  −1.00… 5.16
   *   tail      ±2.870  −0.591…2.167   5.180… 7.190   ±2.88  −0.60…2.17   5.16… 7.20
   *   wing       1.260…6.530  −0.561…0.283  −0.693…2.209
   *                                                    0.76…6.54  −0.57…0.29  −0.87…2.21
   * ```
   *
   * 【機翼盒的前緣為什麼多讓 0.18 m】−0.87 不是機翼幾何，是**內側那挺槍的
   * 槍口**（z = −0.85）。翼槍的槍管伸在前緣之外，盒子得包住它，否則子彈
   * 會從機體外面憑空冒出來。P-51D 的機翼盒也是這樣讓的（前緣 −0.565、
   * 盒到 −0.97）。
   *
   * 【尾段盒的下緣是上一版真正漏掉的東西】上一版寫 −0.56，而機身在
   * z 5.16…5.50 這一段的腹線量到 −0.591 —— 256 個頂點落在六個盒之外，
   * 尾段腹部打不到。那一版是照「整機包圍盒」目測切的，沒有跑覆蓋率。
   * 現在 `hitbox.test.ts` 把 F6F-5 也納入掃描，這一類漏洞會直接紅。
   *
   * 【已驗證】六個盒合計 92.2 m³，佔整機包圍盒 413.0 m³ 的 22%
   *（K-4 是 21%）；螺旋槳除外的每一個頂點都被覆蓋。
   */
  hitBoxes: [
    makeHitBox('engine', [-0.72, -0.90, -2.94], [0.72, 0.92, -1.00]),
    makeHitBox('cockpit', [-0.48, 0.05, 0.28], [0.48, 1.52, 1.45]),
    makeHitBox('fuselage', [-0.76, -0.96, -1.00], [0.76, 1.42, 5.16]),
    makeHitBox('tail', [-2.88, -0.60, 5.16], [2.88, 2.17, 7.20]),
    makeHitBox('wingRight', [0.76, -0.57, -0.87], [6.54, 0.29, 2.21]),
    makeHitBox('wingLeft', [-6.54, -0.57, -0.87], [-0.76, 0.29, 2.21]),
  ],

  /**
   * 六挺 AN/M2 白朗寧 .50。**槍是同一款、位置不是** —— 彈道與傷害指向
   * P-51D 那份 `M2_BROWNING` 物件（同一款槍不該有兩份數字），槍位另外
   * 依 F6F 的摺翼線與前緣線訂。推導、來源與「最弱的那個假設是哪一個」
   * 都寫在 `weapons/f6f5.ts`。
   */
  battery: F6F5_BATTERY,
  turrets: [],
}

/**
 * 史實驗收值。**四項全部出自 Patuxent River 1944-09-07 的 F6F-5 No. 58310**
 * （總重 12,420 lb，軍用出力），見 `F6F5` 檔頭。
 *
 * 【海平面極速是唯一的例外，而且要說清楚】那份報告的摘要沒有列海平面極速，
 * 只有臨界高度那一點。324 mph 是各處通行的 F6F-5 海平面值，與臨界高度極速
 * 的比值 324/391 = 0.829 跟 P-51D 的 595/703 = 0.846 同級。
 *
 * 它**不是**跟另外四項同一份文件，所以本來打算排除在 L2 斷言之外；但模型
 * 在其餘四項校準完之後打出 +1.2%，那反而是一個**獨立來源的交叉驗證** ——
 * 一個沒有參與校準的數字自己對上了。所以照樣進斷言，這一行是它的出處。
 */
export const F6F5_HISTORICAL: HistoricalReference = {
  vmaxAtCritical: { speed: 391.0 * MPH, altitude: 23100 * FT },
  vmaxSeaLevel: 324 * MPH,
  climbRateSeaLevel: 2660 * FPM,
  stallSpeed: 98.0 * MPH,
  serviceCeiling: 37100 * FT,
  // 由同一份報告的失速速度與試飛重量反解，見 `lift` 的說明
  clMax: 2 * (12420 * LB * 9.80665) / (1.225 * 31.03 * (98.0 * MPH) ** 2),
}
