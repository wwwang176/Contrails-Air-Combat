import { DEG } from '../core/math'
import { makeHitBox } from '../world/hit'
import { HE111_BATTERY, HE111_TURRETS } from '../weapons/he111'
import type { AircraftSpec, HistoricalReference } from './types'

const PS = 735.5
const KMH = 1 / 3.6

/**
 * # Heinkel He 111 H-6 —— 專案的第三種機體，也是第一種**不是戰鬥機**的
 *
 * 【外形全部量自參考模型】`ref/he_111-h6.glb`，量法見
 * `.claude/skills/aircraft-from-reference` 與 `render/geometry/he111.hull.ts`。
 * 對齊之後全長量到 16.209 m 對真機 16.40（−1.2%）、翼型厚弦比 18.2% → 10.9%
 * 對史實的 NACA 23018 → 23010 —— **這台量尺兩項獨立指標都對得上**（坑 21）。
 *
 * ── ⚠ 這一份的氣動參數全部是**起始值** ────────────────────
 *
 * P-51D 與 Bf 109 的每一個係數後面都有一段實測掃描（見那兩支 spec 的註解：
 * 「cd0 0.023 → 0.0279，理由是…」）。**這一份還沒有。** 下面的數字是由
 * 史實性能反推與同類機型類比得到的第一版，`test/performance/historical.test.ts`
 * 的 L2 層跑起來之前，沒有一個是定案的。
 *
 * 每一項的來源都標在旁邊，這樣調的時候才知道動的是「量出來的」還是「猜的」。
 *
 * ── 一個非物理但必要的取捨：**等效單槳盤** ──────────────
 *
 * `prop.diameter` 進的是動量理論（`physics/propulsion.ts`），而這台是**雙發**。
 * 真機每具 VDM 三葉槳直徑 3.50 m，兩具的槳盤面積合計 19.24 m²。若照填 3.50，
 * 動量理論會用**一具**的面積去算**兩具**的推力，靜推力的上界會被夾到一半。
 *
 * 所以填**等效直徑 4.95 m**（π/4 × 4.95² = 19.24 m²）。這不是史實值，是把
 * 「兩個槳盤」翻譯成「這個模型只認識一個槳盤」的正確換算。**視覺上仍然是
 * 兩具 3.50 m 的槳**（`render/geometry/he111.ts` 自己畫）。
 */
export const HE111: AircraftSpec = {
  id: 'he111',
  name: 'He 111 H-6',
  faction: 'axis',
  role: 'bomber',

  /**
   * 史實：空重 8,680 kg、最大起飛 14,000 kg。
   *
   * 【為什麼不是 12,500】那是自己取的「掛滿彈的作戰重量」，沒有對應到任何
   * 一份資料表。13,727 kg 是 IL-2 的 He 111 H-6 資料表的 **standard
   * weight** —— `HE111_HISTORICAL` 底下那一
   * 整組數字（極速、爬升、升限、失速）全部是在這個重量下量的，質量對齊了
   * 才是同一個實驗條件。
   */
  mass: 13727,
  /**
   * 【怎麼推的】沒有史實慣量可查，所以用 Bf 109 反解無因次係數再放大：
   *
   * ```
   *   Bf 109  m 3150、半翼展 4.935、半長 4.475
   *           roll  4200 = m·(b/2)²·0.0548
   *           pitch 6200 = m·(l/2)²·0.0983
   * ```
   *
   * 同係數代入 He 111（m 13727、半翼展 11.30、半長 8.20）：
   *   roll  13727 × 127.7 × 0.0548 ≈ 96,000
   *   pitch 13727 ×  67.2 × 0.0983 ≈ 90,700
   *   yaw   ≈ roll + pitch（平板近似）≈ 187,000
   *
   * **質量由 12,500 改成 13,727 時這三個數字同比例跟著放大**（×1.098）——
   * 上面那條推導式左右都有 m，只改一邊就不自洽了。
   *
   * **這三個數字沒有實測支撐**，它們決定的是「舵面下去之後多久才轉起來」。
   */
  inertia: { pitch: 91000, yaw: 187000, roll: 96000 },

  // 史實：翼面積 86.5 m²、翼展 22.60 m。chord 取平均弦 = 面積/翼展 = 3.827
  wing: { area: 86.5, span: 22.60, chord: 3.83, oswald: 0.75 },

  lift: {
    /**
     * 【推導】展弦比 AR = 22.60² / 86.5 = 5.905。二維升力線斜率取 6.0/rad，
     * 三維修正 a = a0 / (1 + a0/(π·AR·e))：
     *   6.0 / (1 + 6.0/(π × 5.905 × 0.75)) = 6.0 / 1.4317 = 4.19
     * 取 4.30，略高於純理論（真機的翼根整流與機身升力都貢獻一點）。
     */
    clAlpha: 4.30,
    // NACA 23018 的零升迎角約 −1.2°，翼尖 23010 約 −1.0°。取 −1.5° 含機身
    alphaZero: -1.5 * DEG,
    // 厚翼型 + 大展弦比，失速迎角略高於 109
    alphaCrit: 16.0 * DEG,
    // 大展弦比的直翼失速較銳利，過渡比 109（有縫翼）窄
    stallBlend: 8 * DEG,
    postStallFactor: 0.60,
    // 【沒有前緣縫翼】He 111 是純機械式後緣襟翼，沒有 109 那套自動縫翼。
    // bonus 為 0 時展開與否不影響結果，兩個門檻只是為了維持「展開 > 收回」
    // 的遲滯不變式
    slatAlphaBonus: 0,
    slatDeployAlpha: 90 * DEG,
    slatRetractAlpha: 89 * DEG,
  },

  /**
   * 【cd0 0.0227，由海平面極速反解】0.032 那個量級是**算錯的**。
   *
   * 「以總功率 2,680 PS、螺旋槳效率 0.80 估，f = C_D0 × S ≈ 2.8 m²」那個
   * 算法照做一次：推力功率 1971 kW × 0.80 = 1577 kW，海平面 360 km/h
   * （100 m/s）對應阻力 15,768 N，動壓 6,125 Pa，總阻力面積 2.574 m²，
   * 減掉誘導的 0.333 m² 之後 f = 2.241 —— **不是 2.8**。它自己的方法給的是
   * 0.0259，不是 0.032。
   *
   * 再修一件事：0.80 是 `prop.etaMax`，不是這台在極速下**真正拿得到**的效率。
   * `η(V) = etaMax·(1 − e^(−V/vRef))` 在 vRef 45、V 100 只到 **0.713**。
   * 代進去 → f = 1.966 → cd0 = 0.0227。
   *
   * 【最終值 0.0206】上面那個 0.0227 是對著舊的、混了掛載狀態的史實表解出來
   * 的（海平面 360 km/h @ 12,500 kg）。史實表換成 IL-2 那一組同重量的數據
   * 之後（海平面 369 km/h @ 13,727 kg），同一個解法給 **0.0206**，海平面
   * 極速實測 369.0 km/h（+0.0%）。
   *
   * 這仍然比兩台戰鬥機髒（P-51D 0.0163、109 0.0279 是含冷卻器的值），而那是
   * 對的：雙發動機艙、機首玻璃罩、腹艙、四個機槍座、外露天線。
   *
   * 量測見 `test/tools/power-needed.probe.ts`、`test/tools/he111-engine.probe.ts`。
   */
  drag: { cd0: 0.0206, cdBeta: 0.9, machCrit: 0.70, machDragFactor: 40 },

  // 側面積大（16.4 m 長的機身 + 大垂尾），側力係數高於戰鬥機
  side: { cyBeta: -0.90 },

  /**
   * 【全部由 109 依尺度與構型類比】沒有一項有實測。方向與量級的理由：
   *
   *   cmAlpha −1.10  尾力臂長（四分之一弦到平尾約 11.8 m，109 只有 4.5 m）
   *                  → 縱向靜穩定度高得多
   *   cmQ     −18    同上，俯仰阻尼跟著尾力臂平方走
   *   cmDe     0.90  升降舵權限相對機體慣量小 —— 轟炸機不該一拉就抬頭
   *   clDa     0.035 見下方獨立說明 —— 這一項已經不是類比值了
   *   cnBeta   0.12  大垂尾 + 長力臂
   *
   * 【clDa 的來源換掉了：從「照 109 類比」改成戰時設計規範】
   *
   * 穩態滾轉是「操縱力矩 = 滾轉阻尼力矩」，滿舵時
   *   `p = clDa·V / (−clP·b/2)`  ⟹  **`pb/2V = clDa / (−clP)`**
   * `pb/2V` 是螺旋角（翼尖走過的螺旋線與飛行路徑的夾角），與速度、翼展都
   * 無關 —— 它量的正是「副翼設計得好不好」。Perkins & Hage 列的最低要求是
   * **戰鬥機 0.09、轟炸／運輸 0.07**（NACA Report 715 同一個門檻）。
   *
   * 兩台轟炸機原本的類比值離規範很遠：
   *
   * ```
   *   機種      clDa    clP    pb/2V   規範   達成率
   *   P-51D    0.033  −0.45   0.0733   0.09    81%
   *   Bf 109   0.028  −0.45   0.0622   0.09    69%
   *   He 111   0.012  −0.50   0.0240   0.07    34%   ← 舊值
   *   B-17G    0.008  −0.55   0.0145   0.07    21%   ← 舊值
   * ```
   *
   * **拉到規範的 0.07**，所以 clDa = 0.07 × (−clP)。
   *
   * 【一個已知的怪處】拉上去之後轟炸機的螺旋角（0.070）會
   * **高於 Bf 109 的 0.062** —— 以「副翼設計品質」這個尺度看轟炸機贏了戰鬥機。
   * 但換算成實際滾轉率仍然是 28.7 對 96.3 °/s（400 km/h、出貨值），翼展差兩
   * 倍多，玩起來的關係是對的。量測見 `test/tools/rollrate.probe.ts`。
   *
   * 【還沒處理的第二層】`controlStiffening.aileronK` 在 400 km/h 又把舵權限
   * 乘上 0.61（He 111）／0.51（B-17G），兩台戰鬥機是 1.00 —— 同一件「轟炸機
   * 滾得慢」的事扣了兩次，症狀是滾轉率**隨速度變慢**。見 `docs/backlog.md`。
   */
  moments: {
    cm0: 0.02, cmAlpha: -1.10, cmQ: -18, cmDe: 0.90,
    clBeta: -0.09, clP: -0.50, clDa: 0.035,
    cnBeta: 0.12, cnR: -0.16, cnDr: 0.055,
  },

  // qRef 對應 300 km/h 海平面動壓（0.5 × 1.225 × 83.3² = 4,250 Pa）。
  // 轟炸機的舵面在高速下重得多，aileronK 高於 109
  controlStiffening: { qRef: 4250, aileronK: 1.8, elevatorK: 0.9, rudderK: 0.6 },

  engine: {
    /**
     * Jumo 211F-2 **×2**，單級增壓。這裡填的是**兩具的合計**。
     *
     * 【altCritical 5,300 不是 4,800】原本填 4,800，那是猜的。真機 Jumo 211F
     * 的 **Nennhöhe（額定高度）是 5,300 m**，IL-2 的引擎表也在 5,300 m 給
     * 1,060 PS。差這 500 m 的後果不小：4,800 之下 6,000 m 的極速被壓到
     * 271 km/h，改成 5,300 之後回到 380。
     *
     * 【真機有兩種額定，這個模型只有一個位置放】
     *
     * ```
     *              海平面    1,900 m   5,300 m
     *   起飛檔     1,340       —         —      （一分鐘限時，只在地面公布）
     *   爬升檔     1,120     1,210     1,060    （連續）
     * ```
     *
     * `powerSeaLevel` 填 1,340 ×2 = 2,680（`WEP_THROTTLE` 對應的就是起飛檔）。
     * 那麼 `powerCritical` 該填哪一個？
     *
     *   - 填爬升檔的 1,060 ×2 = **2,120** → 兩端不同額定，高空被系統性低估：
     *     5,000 m 極速 379.9 km/h 對史實 405（**−6.2%**）。
     *   - 依兩檔在海平面的比值 1,340/1,120 = 1.196 換算成「起飛檔等效」的
     *     1,268 ×2 = **2,536** → 5,000 m 極速 +3.6%，但**升限衝到 +16.8%**。
     *
     * 兩個都不對，而且原因是同一個：**兩個史實參考值本身用的不是同一種額定**
     * ——「5,000 m 極速 405」是全開的數字，「升限 6,300 m」是連續額定爬到爬
     * 不動為止的數字。模型只有一個 `powerCritical`，接不住兩種額定。
     *
     * 【`powerCritical` 與 `powerSeaLevel` 一起抬】要的是「5,000 m 極速與
     * 海平面爬升都提高，**可以超過史實**」。兩個旋鈕剛好各管一項而且互不
     * 干擾：
     *
     *   `powerSeaLevel` 2,680 → **2,854**  海平面爬升 3.92 → 4.50 m/s（命中
     *                                      史實 4.5）。**完全不動升限** ——
     *                                      升限在臨界高度之上，只看 powerCritical
     *   `powerCritical` 2,216 → **2,340**  5,000 m 極速 390.2 → 404.6 km/h
     *                                      （命中史實 405）
     *
     * **2,854 已經超出真機的起飛額定**（1,340 ×2 = 2,680），這是刻意的，不是
     * 量出來的。代價寫在下面。掃描見 `test/tools/he111-boost2.probe.ts`。
     *
     * ```
     *                 前      後      L2
     *   海平面極速    +0.0%   +3.0%   守死（還在 ±5% 內）
     *   5,000 m      −3.6%   −0.1%   守死
     *   海平面爬升   −13.0%   +0.0%   不斷言
     *   失速          +5.6%   +5.6%   PENDING（完全不動）
     *   實用升限      +3.5%   +8.9%   **由守死降級成 PENDING**
     * ```
     *
     * 【下面這段是 2,216 那一輪的推導，留著，因為它解釋了為什麼兩個史實值
     * 本來就接不進同一個 powerCritical】取兩者之間**讓兩項偏差等量**的點：
     *
     * ```
     *   powerCritical   5,000 m 極速   實用升限     兩項都在 ±5% 內？
     *      2,120           −6.2%        −0.9%      否（極速出界）
     *      2,150           −5.4%        +0.5%      否
     *      2,216           −3.6%        +3.5%      是   ← 出貨值
     *      2,250           −2.8%        +5.0%      臨界
     *      2,380           +0.3%       +10.6%      否（升限出界）
     * ```
     *
     * 這是**單一 gear 的妥協，不是量出來的引擎數據**。真要修根因得讓
     * `engine.gears` 分得出「起飛／爬升」兩種額定，那是模型結構的改動。
     * 掃描見 `test/tools/he111-ram.probe.ts`。
     *
     * 【為什麼不是動 cd0 或 etaMax】那兩個是全速域的，海平面極速會跟著跑
     * （現在是 +0.0%）。`powerCritical` 與 `ramEfficiency` 是僅有的兩個在
     * 海平面**完全不作用**的旋鈕 —— `σ_eff = min(σ·ramFactor, 1)` 在海平面
     * 一定夾在 1，於是 `t = 1`、功率恆等於 `powerSeaLevel`。`ramEfficiency`
     * 推到 1.00 也只補回 1 個百分點，力道不夠，所以動的是這一項。
     */
    gears: [{ powerSeaLevel: 2854 * PS, powerCritical: 2340 * PS, altCritical: 5300 }],
    // 進氣口在發動機艙前緣、位置乾淨，但巡航速度低（衝壓效應本來就小）
    ramEfficiency: 0.5,
  },

  /**
   * 【diameter 4.95 是等效單槳盤，不是史實】推導見檔頭。真機是兩具 3.50 m。
   *
   * etaMax 0.80：VDM 三葉定速槳，低於 P-51D 的 Hamilton Standard（0.90）
   *              與 109 的 0.88 —— 轟炸機的槳是為巡航效率調的，不是為極速。
   * vRef 45：特徵速度低於兩台戰鬥機，對應它低得多的工作速域。
   */
  prop: { diameter: 4.95, etaMax: 0.80, vRef: 45, figureOfMerit: 0.7 },

  // 史實：轟炸機的結構限制遠低於戰鬥機。俯衝限速約 600 km/h
  limits: { gPositive: 3.5, gNegative: -1.5, vne: 600 * KMH },

  /**
   * 【3000 是起始值，由試飛裁定】M7 spec §6.3 定「戰鬥機一律 1000」，而這是
   * 第一台不是戰鬥機的。三倍的理由：四具發動機的 B-17 綽號是空中堡壘，
   * 雙發的 He 111 沒那麼硬，但它仍然是一架 12.5 噸、有裝甲隔板與自封油箱的
   * 轟炸機，被一梭 .50 掃過不該就掉下來。
   *
   * **這個數字直接決定「打大機好不好玩」**，所以它一定要由試飛裁定而不是
   * 由實作者定（護欄重新定值是負責人的決定）。
   */
  hp: 3000,
  /**
   * 各部位的防護力。1.0 = 基準，扣血 = 傷害 × 部位倍率 ÷ 這裡的值。
   *
   * ```
   *   engine   0.85   兩具液冷 Jumo 211。與單發戰鬥機同一個係數 —— 停一具
   *                   不致命，但兩具都在冷卻液流失的風險下
   *   其餘     1.00   自封油箱與少量乘員裝甲是同期轟炸機的標配，不算優勢
   * ```
   *
   * 【整體效果 +1.1%】
   */
  protection: {
    cockpit: 1.00, engine: 0.85, tail: 1.00,
    fuselage: 1.00, wingLeft: 1.00, wingRight: 1.00,
  },

  /**
   * 命中盒 —— **全部由當下的網格量出來**，產生腳本
   * `test/tools/hitbox-emit.probe.ts`。
   *
   * 一個部位可以有多個盒，因為好幾個部位的
   * AABB 裡大半是空氣：
   *
   * ```
   *   尾段    平尾＋垂尾是一個十字，AABB 連十字外面那四塊空氣一起包
   *   機翼    上反角。整片機翼的 AABB 高 = 翼厚 ＋ 翼尖相對翼根的抬升
   *   發動機  多發機的四／兩具發動機共用一個盒，中間那一大片內翼一起包
   * ```
   *
   * 而 `hitAircraft` 的倍率取「線段碰到的**所有**盒之中最高的那一個」，
   * 那些空氣於是以 tail 1.2 或 engine 2.0 贏過機身盒的 1.0 與機翼盒的 0.7
   * —— 實測 20v20/12v12/8v8 曾有 **47% 的命中被判成 tail**，名目基準
   * `fuselage` 只有 4%，而座艙 2.5 從正後方是 **0%**。
   *
   * ── 這一台拆成幾段、買到什麼 ─────────────────────────────
   *
   * ```
   *   機翼正後方投影   1段 23.0(2盒)  2段 23.6(4盒)  3段 24.7(4盒)  4段 25.7(4盒)  5段 25.8(6盒)  6段 26.9(6盒)  7段 23.7(8盒)  8段 24.3(8盒)  9段 24.2(10盒)  10段 25.4(10盒)
   *   → 取 2 段
   *   尾段切分 |x| = 0.55　正後方 5.8 → 5.8 m²
   *   發動機 1 盒 → 3 盒　正後方 8.6 → 8.6 m²
   *
   *   正投影 m²      正後方  後上方30°   側方90°    正上方
   *   真實外形    18.2  62.2  33.5  120.0
   *   拆之後      31.3  101.3  46.4  152.4
   *   放大倍數    1.72×  1.63×  1.38×  1.27×
   * ```
   *
   * 【段數為什麼不是越多越好】切分面等分，落在兩個展向站位之間的那一段
   * 裡有翼面卻**一個頂點都沒有** —— `hitbox.test.ts` 的「沒有空盒」會紅，
   * 而那條測試沒有錯（它擋的是打不到的裝飾盒，只是它只看得到頂點）。做法是
   * 把空段**併進鄰居**，展向仍然連續、覆蓋率一格不少。**上限由機翼網格的
   * 展向站位數決定**，不是由「程式化還是 GLB」決定 —— 109 換成 GLB 之後
   * 曲線一模一樣，切超過兩段照樣被併回去。
   *
   * 【切分面怎麼保證不留縫】x 用切分面本身當盒的邊界（相鄰兩段共用、各自
   * 往外讓 0.02），y/z 取「把三角形夾進這一片、再只留落在原機翼盒內的點」
   * 的緊界。第一版是把**頂點**分堆再各取緊包圍盒 —— 頂點覆蓋率完美，但
   * P-51D 兩段之間有 2.7 m 的翼面沒有任何盒子蓋著，那一段打得到卻不扣血。
   * 抓到它的是「盒的投影比真實外形還小」這個幾何上不可能的數字。
   *
   * 【左翼是右翼的鏡射】網格本身有 0.01 級的左右不對稱，照兩邊各自算會得到
   * 對不齊的兩組盒。命中盒是**簡化的傷害體積**，不是外形的複製品。
   *
   * ── 補漏 ────────────────────────────────────────────
   *
   * 砲塔、發動機艙前端、翼尖這幾塊很容易落在所有盒之外 —— **打得到但不
   * 扣血**，而覆蓋率掃描若不含轟炸機就沒有人會發現。He 111 漏 412 個頂點、B-17G 漏 **6,012** 個（最大的兩塊是尾砲塔
   * 2,528 點與上部砲塔＋背脊 2,214 點）。
   *
   * 補法：漏掉的點按 0.8 m 鏈結距離分群，每群補一個緊盒，部位取離它最近的
   * 那個盒的。凡是碰到那一群的三角形，三個頂點一起算進盒裡 —— 只包漏掉的
   * 頂點的話，跨在邊界上的三角形會有一段面落在兩個盒之間。
   *
   * ```
   *   正投影 m²      正後方  後上方30°   側方90°    正上方
   *   真實外形    18.2  62.2  33.5  120.0
   *   補完之後    31.5  102.2  46.8  153.6
   *   放大倍數    1.73×  1.64×  1.40×  1.28×
   * ```
   *
   * 兩台的覆蓋率現在都是 0，`hitbox.test.ts` 的掃描也把它們納進來了。
   */
  hitBoxes: [
    makeHitBox('cockpit', [-0.90, -0.85, -3.23], [0.90, 1.60, 0.36]),              // 座艙
    makeHitBox('engine', [-3.27, -1.04, -3.11], [-1.93, 0.55, 1.59]),             // 發動機艙 1
    makeHitBox('engine', [-1.35, -0.90, -3.04], [1.35, 0.65, 1.84]),              // 發動機艙 2
    makeHitBox('engine', [1.93, -1.04, -3.11], [3.27, 0.55, 1.59]),              // 發動機艙 3
    makeHitBox('fuselage', [-0.90, -1.25, 0.36], [0.90, 1.50, 9.16]),              // 機身
    makeHitBox('tail', [-3.78, -0.17, 10.05], [3.78, 0.17, 12.92]),             // 平尾
    makeHitBox('tail', [-0.57, -0.74, 9.19], [0.57, 2.65, 12.94]),             // 垂尾＋尾錐
    makeHitBox('wingRight', [0.82, -0.61, -1.91], [6.12, 0.69, 3.90]),              // 右翼第 1 段
    makeHitBox('wingRight', [6.07, 0.01, -0.34], [11.36, 0.95, 3.69]),             // 右翼第 2 段
    makeHitBox('wingLeft', [-6.12, -0.61, -1.91], [-0.82, 0.69, 3.90]),             // 左翼第 1 段
    makeHitBox('wingLeft', [-11.36, 0.01, -0.34], [-6.07, 0.95, 3.69]),             // 左翼第 2 段
    makeHitBox('tail', [-3.98, -0.11, 10.79], [-3.73, 0.11, 12.18]),            // 補漏 1（tail）
    makeHitBox('tail', [-3.42, -0.14, 11.75], [3.42, 0.14, 13.00]),             // 補漏 2（tail）
    makeHitBox('tail', [3.73, -0.11, 10.79], [3.98, 0.11, 12.18]),             // 補漏 3（tail）
    makeHitBox('wingLeft', [-11.33, 0.76, 0.85], [-9.69, 1.01, 2.42]),             // 補漏 4（wingLeft）
    makeHitBox('wingRight', [9.69, 0.76, 0.85], [11.33, 1.01, 2.42]),             // 補漏 5（wingRight）
    makeHitBox('fuselage', [-0.31, -1.31, 3.59], [0.31, -1.13, 4.74]),             // 補漏 6（fuselage）
    makeHitBox('fuselage', [-0.53, 1.25, 0.59], [0.53, 1.68, 2.44]),              // 補漏 7（fuselage）
  ],

  battery: HE111_BATTERY,
  turrets: HE111_TURRETS,
}

/**
 * 史實性能。**L2 測試還沒有把這一台納進來** —— 納進來之前，上面的氣動係數
 * 沒有一個算定案。
 *
 * 資料取自 He 111 H-6（Jumo 211F-2、作戰重量）。
 */
/**
 * **整張表出自同一份資料、同一個掛載狀態。** 混兩種狀態的表會拿輕載的
 * 數字去要求一台滿載的飛機。查證與量測見
 * `test/tools/he111-engine.probe.ts`。
 *
 * ```
 *   欄位            舊值          新值        舊值錯在哪
 *   臨界高度極速   435 @ 6000   405 @ 5000   435 是輕載值。滿載的 H-6 在
 *                                            6,000 m 的爬升只剩 1.8 m/s，
 *                                            已經快貼到升限，飛不到 435
 *   海平面極速     360          369          360 沒有出處；369 是資料表值
 *   海平面爬升     4.2          4.5          4.2 是「到 2,000 m 約 8.5 分」
 *                                            換算的平均值，不是海平面值
 *   實用升限       8500         6300         8,500 是**空載**的天花板。同一
 *                                            段來源下一句就寫「掛上重磅彈
 *                                            之後降到 6,500」
 *   失速           150          150          不動（資料表的淨形 150…184）
 * ```
 *
 * 新值全部取自 IL-2 的 He 111 H-6 資料表，**同一個重量（13,727 kg）下的
 * 同一組實驗**——`mass` 也已經對齊到它。這是這張表第一次內部自洽。
 */
export const HE111_HISTORICAL: HistoricalReference = {
  vmaxAtCritical: { speed: 405 * KMH, altitude: 5000 },
  vmaxSeaLevel: 369 * KMH,
  climbRateSeaLevel: 4.5,
  stallSpeed: 150 * KMH,
  serviceCeiling: 6300,
  /**
   * 厚翼型（NACA 23018）的 CL_max 高於戰鬥機的層流翼。
   *
   * **這一項沒有任何測試在守**（`specs.test.ts` 的 `CASES` 只有兩台戰鬥機）。
   * 記錄實情：`derivedClMax` 由 `lift` 三參數算出來是 **1.313**，離這裡的
   * 1.55 差 15%。要把兩者對上得動 `alphaCrit`／`clAlpha`，而那會連帶動失速
   * 速度（現在 +5.6%）—— 還沒有人量過該往哪邊調。
   */
  clMax: 1.55,
}
