import { DEG } from '../core/math'
import { makeHitBox } from '../world/hit'
import { BF109K4_BATTERY } from '../weapons/bf109k4'
import type { AircraftSpec, HistoricalReference } from './types'

const PS = 735.5
const KMH = 1 / 3.6

/**
 * # 這一台是「E-4 的外型 + K-4 的性能」——刻意的，不是資料錯誤
 *
 * 三個看起來互相矛盾的東西全都是對的：
 *
 * ```
 *   id     bf109k4      飛行模型是 K-4
 *   name   Bf 109 K-4   玩家看到的機種名也是 K-4
 *   造型    Bf 109 E-4   render/geometry/bf109e.ts，**沒有改**
 * ```
 *
 * 【裁決的出處】2026-08-25，專案負責人：「BF109 的性能參數改成 BF109K，
 * 3D 模型不要動」「型號名稱也要改」「E-4 外型 + K-4 性能 沒錯」。
 * 也就是說**顯示名稱跟著性能走，外型明知不符而保留**。
 *
 * 【這比上一輪更不像】前一版是「E-4 外型 + G-6 性能」，差 4 年、400 匹。
 * 現在差 **6 年、900 匹**，而且外觀上 K-4 還多了加高的木製垂尾、可收放
 * 尾輪、Erla Haube 座艙罩、加大的 MG 131 鼓包與增壓器進氣口——一樣都
 * 沒有。這是負責人在知情下的取捨（換模型的成本 > 外型不符的代價）。
 *
 * 【所以下次要改什麼之前先確認】如果你想「修正」`name` 回到 E-4、或把
 * 發動機換回 DB 601A／DB 605A，那不是修 bug，那是**推翻負責人的裁決**，
 * 要先問過。命中盒仍然取自 E-4 的造型，那是對的——命中判定必須跟畫面
 * 一致，不能跟性能一致。
 *
 * 【武裝也是 K-4 的】MK 108 30 mm + 2× MG 131，見 weapons/bf109k4.ts。
 * 這一項與外型的落差同樣是刻意的。
 */
export const BF109K4: AircraftSpec = {
  id: 'bf109k4',
  name: 'Bf 109 K-4',
  faction: 'axis',
  role: 'fighter',

  // 3150 → 3375：K-4 的正常起飛重量。加高垂尾、MW-50 增壓水甲醇箱
  //（85 L）與加大的 MG 131 使空重上升。
  mass: 3375,

  // 隨質量等比放大（+7.14%）：外型完全沒動，質量分布形狀不變，
  // I = Σmr² 於是與質量成正比。6200/10500/4200 × 1.0714。
  inertia: { pitch: 6640, yaw: 11250, roll: 4500 },

  // 翼面積、翼展與翼型 E→K 全系列未變。
  wing: { area: 16.05, span: 9.87, chord: 1.68, oswald: 0.78 },

  lift: {
    clAlpha: 4.45,
    alphaZero: -2.0 * DEG,
    alphaCrit: 15.5 * DEG,
    // 縫翼展開後失速較溫和，過渡寬度大於 P-51
    stallBlend: 10 * DEG,
    postStallFactor: 0.65,
    slatAlphaBonus: 2.5 * DEG,
    slatDeployAlpha: 8 * DEG,
    slatRetractAlpha: 6 * DEG,
  },

  // cd0 **維持 0.0279 不動**，儘管 K-4 在外觀上比 G-6 乾淨（可收放尾輪、
  // Erla Haube、整流較好的機首）。
  //
  // 【誠實揭露：這一項是 load-bearing 的，而且動機是數字不是氣動學】
  // 【僅退回本項、其餘全為出貨值的實測】
  //   cd0 0.0279：海平面 588.72 km/h（−1.06%）、7,500 m 736.53 km/h（+3.74%）
  //   cd0 0.026 ：海平面 603.56 km/h（+1.44%）、7,500 m 755.32 km/h（**+6.38%，FAIL**）
  //   cd0 0.024 ：海平面 620.80 km/h（+4.34%）、7,500 m 774.24 km/h（**+9.05%，FAIL**）
  // 也就是說 K-4 那些「變乾淨」的細節在本模型裡**沒有預算可以花**——
  // 2,000 PS 已經把兩項極速推到 ±5% 的上緣。
  //
  // 氣動上還是說得過去：K-4 同時也**變髒了**——MG 131 的鼓包比 G-6 更大、
  // 增壓器進氣口加大、垂尾加高加大，這些抵銷了尾輪與座艙罩的收益。
  // 0.0279 仍明顯高於 P-51D 的 0.0163（specs 測試要求 < 0.8 倍）。
  drag: { cd0: 0.0279, cdBeta: 0.7, machCrit: 0.68, machDragFactor: 60 },

  side: { cyBeta: -0.68 },

  moments: {
    cm0: 0.02, cmAlpha: -0.75, cmQ: -10, cmDe: 1.15,
    clBeta: -0.075, clP: -0.45, clDa: 0.028,
    cnBeta: 0.09, cnR: -0.1, cnDr: 0.065,
  },

  // qRef 對應 400 km/h 海平面動壓。aileronK 遠大於 P-51，
  // 使 650 km/h 時滾轉率衰減至約 30°/s——109 的關鍵弱點，K-4 沒有改善
  //（副翼與操縱系統與 G 系列相同，Flettner 補償片只在部分機上）。
  controlStiffening: { qRef: 7560, aileronK: 1.5, elevatorK: 0.6, rudderK: 0.4 },

  engine: {
    // DB 605DC，1.98 ata、C3 燃料 + MW-50 加力。2,000 PS 起飛／緊急功率。
    // altCritical 6,000 m 是加力檔的全增壓高度；配合 ram 0.6，模型極速峰值
    // 落在 7,650 m，史實 K-4 的峰值在 7,500 m。
    gears: [{ powerSeaLevel: 2000 * PS, powerCritical: 1800 * PS, altCritical: 6000 }],
    // 0.6 維持不動。109 的增壓器進氣口仍在機首側面的邊界層內，K-4 只是把
    // 它加大，沒有改成 P-51D 那種專用進氣道。
    // 【僅退回本項的實測】ram 0.8：7,500 m 極速 +4.03%（仍 PASS，但餘裕由
    // 8.9 km/h 縮到 2.1 km/h）、峰值高度由 7,650 m 飄到 8,250 m（離史實的
    // 7,500 m 更遠）、升限 +3.52%。保留 0.6。
    ramEfficiency: 0.6,
  },

  // 【爬升率守史實 ±5%，兩機的相對關係也對得上】
  //
  // ```
  //                        模型      史實      偏差
  //   K-4 海平面爬升      1,452.9   1,470    −1.16%
  //   P-51D 海平面爬升    1,047.5   1,097    −4.54%
  //   K-4 ÷ P-51D          1.3870   1.3397    +3.54%
  // ```
  //
  // 【這裡曾經有一段互相牽制，現在沒有了】P-51D 的爬升一度差史實 14.45%，
  // 使比值飄到 +15.5%；當時的解法是把 109 也同步壓低去換正確的相對關係，
  // 後來改為讓 K-4 守自己的絕對值、接受比值跑掉。兩者現在都不需要了 ——
  // 成因是 P-51D 的 `engine.gears` 混了兩個出力狀態（低增壓檔的海平面填了
  // 61″Hg 的軍用值），見 `specs/p51d.ts`。**P-51D 自己修好之後，比值自動
  // 歸位，K-4 一個字都沒動**。
  //
  // 所以下面 vRef 50 的取值理由是「K-4 自己對上 1,470 m/min」，與 P-51D
  // 無關。要改它請只看 K-4 自己的五項驗收。
  //
  // 【vRef 50 怎麼定的】僅退回本項的實測：
  //   vRef 46  ：爬升 1,512.5（+2.89%）、海平面極速 590.80（−0.71%）
  //   vRef 50  ：爬升 1,452.9（−1.16%）、海平面極速 588.72（−1.06%）  ← 出貨
  //   vRef 55.7：爬升 1,371.4（−6.70%，**FAIL**）
  // 46 與 50 都在 ±5% 內，取 50 是因為它讓靜推力留在動量理論之下（見下）。
  //
  // 【figureOfMerit 0.7 → 0.8：本項不影響任何驗收數字】
  // 僅退回本項的實測顯示五項全部**一字不差**——夾制只作用在 V→0 的靜推力，
  // 而爬升與極速都發生在 80 m/s 以上。改它的理由是不要讓模型「靠夾制活著」：
  //   vRef 50 時 η_max·P/vRef = 25.890 kN
  //   3.0 m 槳盤吃 2,000 PS 的動量理論理想靜推力 = 33.464 kN
  //   fom 0.7 的夾制 23.425 kN → **低於 25.890，夾制生效**
  //   fom 0.8 的夾制 26.771 kN → 高於 25.890，餘裕 3.29%，不生效
  // 0.8 也有史實依據：K-4 的 VDM 9-12159A 是寬弦「槳板」式螺旋槳，靜止與
  // 低速推力明顯優於 G-6 的窄弦槳。0.8 是 specs.test.ts 的上界，與 P-51D
  // 的 Hamilton Standard 同級——不准再往上。
  prop: { diameter: 3.0, etaMax: 0.88, vRef: 50, figureOfMerit: 0.8 },

  // vne **維持 750 km/h**：109 全系列的最高容許表速（Höchstzulässige
  // Fahrt）在 K-4 的操作手冊裡仍是 750 km/h，機體強化沒有放寬這一項。
  limits: { gPositive: 7.5, gNegative: -3.5, vne: 750 * KMH },

  hp: 1000,

  /**
   * 命中盒 —— **全部由當下的網格量出來**，產生腳本
   * `test/tools/hitbox-emit.probe.ts`。
   *
   * 一個部位可以有多個盒（2026-08-31 放寬那條護欄），因為好幾個部位的
   * AABB 裡大半是空氣：
   *
   * ```
   *   尾段    平尾＋垂尾是一個十字，AABB 連十字外面那四塊空氣一起包
   *   機翼    上反角。整片機翼的 AABB 高 = 翼厚 ＋ 翼尖相對翼根的抬升
   *   發動機  多發機的四／兩具發動機共用一個盒，中間那一大片內翼一起包
   * ```
   *
   * 而 `hitAircraft` 的倍率取「線段碰到的**所有**盒之中最高的那一個」，
   * 那些空氣於是以 tail 1.2 或 engine 2.0 贏過機身盒的 1.0 與機翼盒的 0.7
   * —— 實測 20v20/12v12/8v8 曾有 **47% 的命中被判成 tail**，名目基準
   * `fuselage` 只有 4%，而座艙 2.5 從正後方是 **0%**。
   *
   * ── 這一台拆成幾段、買到什麼 ─────────────────────────────
   *
   * ```
   *   機翼正後方投影   1段 7.3(2盒)  2段 5.7(4盒)  3段 5.9(4盒)  4段 5.6(6盒)  5段 5.6(6盒)  6段 6.2(6盒)  7段 5.6(8盒)  8段 5.9(8盒)  9段 6.2(8盒)  10段 5.8(10盒)
   *   → 取 2 段
   *   尾段切分 |x| = 0.35　正後方 5.4 → 2.4 m²
   *
   *   正投影 m²      正後方  後上方30°   側方90°    正上方
   *   真實外形    3.4  11.3  9.6  21.7
   *   拆之前      11.9  26.0  12.7  32.4
   *   拆之後      8.4  20.5  12.8  27.8
   *   放大倍數    2.48×  1.81×  1.34×  1.28×
   * ```
   *
   * 【段數為什麼不是越多越好】切分面等分，落在兩個展向站位之間的那一段
   * 裡有翼面卻**一個頂點都沒有** —— `hitbox.test.ts` 的「沒有空盒」會紅，
   * 而那條測試沒有錯（它擋的是打不到的裝飾盒，只是它只看得到頂點）。做法是
   * 把空段**併進鄰居**，展向仍然連續、覆蓋率一格不少。程式化建的那幾台展向
   * 站位很稀疏，切超過兩、三段就會被併回去，上表的曲線因此在那裡就平了。
   *
   * 【切分面怎麼保證不留縫】x 用切分面本身當盒的邊界（相鄰兩段共用、各自
   * 往外讓 0.02），y/z 取「把三角形夾進這一片、再只留落在原機翼盒內的點」
   * 的緊界。第一版是把**頂點**分堆再各取緊包圍盒 —— 頂點覆蓋率完美，但
   * P-51D 兩段之間有 2.7 m 的翼面沒有任何盒子蓋著，那一段打得到卻不扣血。
   * 抓到它的是「盒的投影比真實外形還小」這個幾何上不可能的數字。
   *
   * 【左翼是右翼的鏡射】網格本身有 0.01 級的左右不對稱（K-4 只有左舷有
   * 增壓器進氣口），照兩邊各自算會得到對不齊的兩組盒。命中盒是**簡化的
   * 傷害體積**，不是外形的複製品。
   */
  hitBoxes: [
    makeHitBox('cockpit', [-0.37, 0.05, 0.24], [0.37, 0.98, 1.70]),              // 座艙
    makeHitBox('engine', [-0.60, -0.51, -2.60], [0.60, 0.85, -0.10]),             // 發動機
    makeHitBox('fuselage', [-0.45, -0.51, -0.10], [0.45, 0.90, 4.50]),              // 機身
    makeHitBox('tail', [-1.67, 0.18, 4.94], [1.67, 0.63, 5.95]),              // 平尾
    makeHitBox('tail', [-0.37, -0.19, 4.48], [0.37, 1.47, 6.09]),              // 垂尾＋尾錐
    makeHitBox('wingRight', [0.39, -0.50, -0.55], [2.70, 0.23, 1.61]),              // 右翼第 1 段
    makeHitBox('wingRight', [2.66, -0.16, -0.42], [4.96, 0.30, 1.19]),              // 右翼第 2 段
    makeHitBox('wingLeft', [-2.70, -0.50, -0.55], [-0.39, 0.23, 1.61]),             // 左翼第 1 段
    makeHitBox('wingLeft', [-4.96, -0.16, -0.42], [-2.66, 0.30, 1.19]),             // 左翼第 2 段
  ],

  battery: BF109K4_BATTERY,
  // 戰鬥機沒有自衛砲塔
  turrets: [],
}

/**
 * K-4 的史實性能。來源為 DB 605DC、1.98 ata、C3 + MW-50 的加力狀態
 *（1944 年底的量產標準）。
 *
 * 【失速速度是推算的，不是查到的】G-6 在 3,150 kg 是 170 km/h；翼面積與
 * 翼型未變，失速速度隨 √質量 放大：170 × √(3375/3150) = 175.9 → 取 175。
 * 其餘四項都是文獻值。
 */
export const BF109K4_HISTORICAL: HistoricalReference = {
  vmaxAtCritical: { speed: 710 * KMH, altitude: 7500 },
  vmaxSeaLevel: 595 * KMH,
  climbRateSeaLevel: 1470 / 60,
  stallSpeed: 175 * KMH,
  serviceCeiling: 12500,
  clMax: 1.4,
}
