import { DEG } from '../core/math'
import { makeHitBox } from '../world/hit'
import { B17G_BATTERY, B17G_TURRETS } from '../weapons/b17g'
import type { AircraftSpec, HistoricalReference } from './types'

const HP = 745.7
const KMH = 1 / 3.6

/**
 * # Boeing B-17G Flying Fortress —— 專案的第四種機體，第二種轟炸機
 *
 * 【外形全部量自參考模型】`ref/1943_boeing_b-17g-60-ve_flying_fortress.glb`，
 * 量法見 `.claude/skills/aircraft-from-reference` 與
 * `render/geometry/b17g.hull.ts`。
 *
 * **這台量尺的尺寸與位置都可信**（坑 21）：依翼展縮放之後，**沒有參與縮放**
 * 的水平尾翼展量到 13.05 對真機 13.11（−0.5%）；主翼梯形面積 133.3 m² 對
 * 真機 131.92（+1.0%）。對照 Bf 109 那台（主翼偏大 2.7%、尾翼偏小 11%，
 * 只有位置可信），這台兩項獨立指標都對得上。
 *
 * ── ⚠ 這一份的氣動參數全部是**起始值** ────────────────────
 *
 * 與 He 111 同樣的處境：`test/performance/historical.test.ts` 的 L2 層跑起來
 * 之前，下面沒有一個係數是定案的。每一項的來源都標在旁邊，這樣調的時候才
 * 知道動的是「量出來的」「反推的」還是「猜的」。
 *
 * ── 一個非物理但必要的取捨：**等效單槳盤** ──────────────
 *
 * `prop.diameter` 進的是動量理論（`physics/propulsion.ts`），而這台是**四發**。
 * 真機每具 Hamilton Standard 三葉槳直徑 11 ft 7 in = 3.53 m，四具的槳盤面積
 * 合計 4 × 9.787 = 39.15 m²。若照填 3.53，動量理論會用**一具**的面積去算
 * **四具**的推力，靜推力的上界會被夾到四分之一。
 *
 * 所以填**等效直徑 7.06 m**（π/4 × 7.06² = 39.15 m²）。這不是史實值，是把
 * 「四個槳盤」翻譯成「這個模型只認識一個槳盤」的正確換算。**視覺上仍然是
 * 四具 3.53 m 的槳**（`render/geometry/b17g.ts` 自己畫）。
 *
 * 【與 He 111 的差別不只是變大】B-17 有**渦輪增壓器**：四具 R-1820-97 的
 * 出力一路撐到 7,620 m。He 111 的 Jumo 211F-2 是機械增壓，4,800 m 之後
 * 就掉。這一條是「B-17 在高空打」這個史實戰術的物理來源。
 */
export const B17G: AircraftSpec = {
  id: 'b17g',
  name: 'B-17G Flying Fortress',
  faction: 'allied',
  role: 'bomber',

  /**
   * 史實：空重 16,391 kg（36,135 lb）、最大起飛 29,710 kg（65,500 lb）。
   *
   * 【2026-08-20 由 24,500 改成 22,000，專案負責人裁定】原本取「掛滿彈的
   * 作戰重量」24,500 kg（54,000 lb）。量出來的問題是：`B17G_HISTORICAL`
   * 那張表**不是在 24,500 kg 量的**。同時解升限與失速兩式，兩者都指向
   * 約 19,000 kg：
   *
   * ```
   *   質量      海平面極速  臨界高度極速   海爬     失速     升限
   *   24500       +1.9%      −2.1%     −9.1%  +13.2%  −16.7%
   *   22000       +3.1%      +1.3%    +12.4%   +7.3%   −9.5%
   *   19017       +4.4%      +4.4%    +43.9%   −0.2%   −0.1%    ← 表對得上的重量
   * ```
   *
   * 四個互相獨立的量同時落在 19,017 上不是巧合。但**減到那裡會讓它不像
   * 一台空中堡壘** —— 套上 `GAME_FEEL` 的 mass ×0.8 之後有效重量只剩
   * 15,214 kg，爬升 +43.9%。負責人取中間的 **22,000**：手感上還是重的，
   * 五項沒有一項大錯（最差的升限 −9.5%）。
   *
   * 量測見 `test/tools/mass.probe.ts`。
   */
  mass: 22000,

  /**
   * 【怎麼推的】沒有史實慣量可查，用與 He 111 同一套：拿 Bf 109 反解無因次
   * 係數再放大。
   *
   * ```
   *   Bf 109  m 3150、半翼展 4.935、半長 4.475
   *           roll  4200 = m·(b/2)²·0.0548
   *           pitch 6200 = m·(l/2)²·0.0983
   * ```
   *
   * 同係數代入 B-17G（m 22000、半翼展 15.81、半長 11.33）：
   *   roll  22000 × 249.96 × 0.0548 ≈ 301,400
   *   pitch 22000 × 128.37 × 0.0983 ≈ 277,600
   *   yaw   ≈ roll + pitch（平板近似）≈ 579,000
   *
   * **質量由 24,500 改成 22,000 時這三個數字同比例跟著縮**（×0.898）——
   * 上面那條推導式左右都有 m，只改一邊就不自洽了。
   *
   * **這三個數字沒有實測支撐**，它們決定的是「舵面下去之後多久才轉起來」。
   * 對這台尤其重要 —— 慣量是 He 111 的 3.8 倍，手感差別應該非常明顯。
   */
  inertia: { pitch: 278000, yaw: 579000, roll: 301000 },

  // 史實：翼面積 131.92 m²（1,420 sq ft）、翼展 31.62 m。
  // chord 取平均弦 = 面積/翼展 = 4.172
  wing: { area: 131.92, span: 31.62, chord: 4.17, oswald: 0.80 },

  lift: {
    /**
     * 【推導】展弦比 AR = 31.62² / 131.92 = 7.579 —— 明顯高於 He 111 的
     * 5.905，那是 B-17 航程與高空性能的來源。二維升力線斜率取 6.0/rad，
     * 三維修正 a = a0 / (1 + a0/(π·AR·e))：
     *   6.0 / (1 + 6.0/(π × 7.579 × 0.80)) = 6.0 / 1.315 = 4.56
     */
    clAlpha: 4.55,
    /**
     * 【−3.0° 是安裝角不是翼型】B-17 的翼型是 **NACA 0018 → 0010**，
     * 對稱的 —— 翼型自己的零升迎角是 0。而主翼相對機身有 3.5° 安裝角，
     * 本模型的 α 是機體軸迎角，所以零升落在機體 −3.5°。取 −3.0° 含機身。
     *
     * 這一條有量測佐證：量參考模型的機翼中厚線（＝對稱翼型的弦線）時，
     * 翼根量到 −4.16°、外段 −3.05°，扣掉姿態 −1.03° 之後是 3.13° 與 2.02°
     * ——安裝角 3.1°、扭轉 1.1°。
     */
    alphaZero: -3.0 * DEG,
    // 厚對稱翼型 + 高展弦比。低於 He 111 的 16°（那台是彎度翼型）
    alphaCrit: 15.0 * DEG,
    // 高展弦比的直翼失速銳利，但 1.1° 的扭轉讓翼根先失速 → 過渡比 109 寬一點
    stallBlend: 7 * DEG,
    postStallFactor: 0.58,
    // 【沒有前緣縫翼】純機械式後緣襟翼。與 He 111 同樣的處理
    slatAlphaBonus: 0,
    slatDeployAlpha: 90 * DEG,
    slatRetractAlpha: 89 * DEG,
  },

  /**
   * 【cd0 0.0245 是反推的，不是量的】史實 7,620 m（25,000 ft）極速
   * 462 km/h。以四具合計 4,000 hp（渦輪增壓在該高度仍有 1,000 hp/具）、
   * 螺旋槳效率 0.82 估：
   *
   * ```
   *   ρ(7620 m) = 0.549      V = 128.3 m/s     q = 4,518 Pa
   *   可用功率 = 4000 × 745.7 × 0.82 = 2.446 MW  → D = 19,065 N
   *   C_D = D/(q·S) = 0.0320
   *   C_L = W/(q·S) = 0.403   →  C_Di = C_L²/(π·AR·e) = 0.0085
   *   C_D0 = 0.0320 − 0.0085 = 0.0235
   * ```
   *
   * 【2026-08-20 改成 0.0216】上面那段推導的**算術是對的**，錯在兩件事：
   *
   * 1. `0.82` 是 `prop.etaMax`，不是這台在 128.3 m/s **真正拿得到**的效率。
   *    `η(V) = etaMax·(1 − e^(−V/vRef))` 在 vRef 50、V 128.3 只到 **0.740**。
   *    代進去：可用功率 2,207 kW → D = 17,203 N → C_D = 0.0289 →
   *    C_D0 = 0.0204。
   * 2. 「取 0.0245，比推導值高一點，因為砲塔」是憑感覺加的 4%，沒有量。
   *
   * 兩個史實極速點各自反解（`test/tools/reconcile.probe.ts`）：
   *
   * ```
   *   海平面 380 km/h  要求 cd0 = 0.0226
   *   7620 m 462 km/h  要求 cd0 = 0.0206      兩者只差 1.10 倍
   * ```
   *
   * **這台的兩個點對得起來**（He 111 差 1.53 倍，對不起來）。取中間的
   * **0.0216**，實測海平面 387.2（+1.9%）、7,620 m 452.4（−2.1%），兩項
   * 都在 L2 的 ±5% 之內。砲塔的凸起就在這 1.9% 的餘裕裡，不另外加。
   *
   * 對照：P-51D 0.0163、Bf 109 0.0279、He 111 0.0227。**B-17 比 He 111
   * 乾淨得多**，這一點違反直覺但是對的：它大得多（同樣的凸起攤在 131.9 m²
   * 上），而且是全金屬應力蒙皮的後期設計。
   *
   * 兩個極速點已經對上；**失速與升限還沒有**，見 `docs/backlog.md` §2.18。
   */
  drag: { cd0: 0.0216, cdBeta: 1.0, machCrit: 0.68, machDragFactor: 40 },

  // 22.66 m 的機身 + 巨大的垂尾。側力係數高於 He 111
  side: { cyBeta: -1.05 },

  /**
   * 【全部由 He 111 依尺度與構型類比】沒有一項有實測。方向與量級的理由：
   *
   *   cmAlpha −1.30  尾力臂 12.7 m（He 111 是 11.8）、平尾面積比也大
   *   cmQ     −24    同上，俯仰阻尼跟著尾力臂平方走
   *   cmDe     0.80  升降舵權限相對機體慣量更小 —— 22 噸不該一拉就抬頭
   *   clDa     0.0385 見下方獨立說明 —— 這一項已經不是類比值了
   *   cnBeta   0.14  B-17 的垂尾在同代機裡是出了名的大
   *
   * 【clDa 的來源換掉了：從「照 109 類比」改成戰時設計規範】
   *
   * 穩態滾轉是「操縱力矩 = 滾轉阻尼力矩」，滿舵時
   *   `p = clDa·V / (−clP·b/2)`  ⟹  **`pb/2V = clDa / (−clP)`**
   * `pb/2V` 是螺旋角（翼尖走過的螺旋線與飛行路徑的夾角），與速度、翼展都
   * 無關 —— 它量的正是「副翼設計得好不好」。Perkins & Hage 列的最低要求是
   * **戰鬥機 0.09、轟炸／運輸 0.07**（NACA Report 715 同一個門檻）。
   *
   * 兩台轟炸機原本的類比值離規範很遠：
   *
   * ```
   *   機種      clDa    clP    pb/2V   規範   達成率
   *   P-51D    0.033  −0.45   0.0733   0.09    81%
   *   Bf 109   0.028  −0.45   0.0622   0.09    69%
   *   He 111   0.012  −0.50   0.0240   0.07    34%   ← 舊值
   *   B-17G    0.008  −0.55   0.0145   0.07    21%   ← 舊值
   * ```
   *
   * **2026-08-20 專案負責人裁定拉到規範的 0.07**，所以 clDa = 0.07 × (−clP)。
   *
   * 【一個已知的怪處，裁定時已知悉】拉上去之後轟炸機的螺旋角（0.070）會
   * **高於 Bf 109 的 0.062** —— 以「副翼設計品質」這個尺度看轟炸機贏了戰鬥機。
   * 但換算成實際滾轉率仍然是 28.7 對 96.3 °/s（400 km/h、出貨值），翼展差兩
   * 倍多，玩起來的關係是對的。量測見 `test/tools/rollrate.probe.ts`。
   *
   * 【還沒處理的第二層】`controlStiffening.aileronK` 在 400 km/h 又把舵權限
   * 乘上 0.61（He 111）／0.51（B-17G），兩台戰鬥機是 1.00 —— 同一件「轟炸機
   * 滾得慢」的事扣了兩次，症狀是滾轉率**隨速度變慢**。見 `docs/backlog.md`。
   */
  moments: {
    cm0: 0.02, cmAlpha: -1.30, cmQ: -24, cmDe: 0.80,
    clBeta: -0.10, clP: -0.55, clDa: 0.0385,
    cnBeta: 0.14, cnR: -0.18, cnDr: 0.050,
  },

  // qRef 對應 300 km/h 海平面動壓。四發重轟的舵面在高速下最重
  controlStiffening: { qRef: 4250, aileronK: 2.4, elevatorK: 1.1, rudderK: 0.8 },

  engine: {
    /**
     * Wright R-1820-97 Cyclone **×4**，配 General Electric B-22 渦輪增壓器。
     * 這裡填的是**四具的合計**：
     *   起飛 1,200 hp ×4 = 4,800 hp
     *   臨界高度 7,620 m（25,000 ft）時 1,000 hp ×4 = 4,000 hp
     *
     * 【渦輪增壓是這台的招牌】機械增壓的 He 111 在 4,800 m 之後就掉出力，
     * 這台撐到 7,620 m —— 那正是 B-17 編隊在 25,000 ft 轟炸的物理來源。
     */
    gears: [{ powerSeaLevel: 4800 * HP, powerCritical: 4000 * HP, altCritical: 7620 }],
    // 進氣在發動機艙前緣、位置乾淨，但巡航速度低（衝壓效應本來就小）
    ramEfficiency: 0.5,
  },

  /**
   * 【diameter 7.06 是等效單槳盤，不是史實】推導見檔頭。真機是四具 3.53 m。
   *
   * etaMax 0.82：Hamilton Standard 三葉定速槳。略高於 He 111 的 0.80、
   *              低於 P-51D 的 0.90 —— 轟炸機的槳是為巡航調的。
   * vRef 55：特徵速度高於 He 111（巡航 293 km/h 對 He 111 的 235）。
   */
  prop: { diameter: 7.06, etaMax: 0.82, vRef: 55, figureOfMerit: 0.7 },

  // 史實：placard 限速約 305 mph = 490 km/h。結構限制低於 He 111（更大更重）
  limits: { gPositive: 3.0, gNegative: -1.2, vne: 490 * KMH },

  /**
   * 【5000 是起始值，由試飛裁定】He 111 是 3000（M7 spec §6.3 定「戰鬥機
   * 一律 1000」）。B-17 取它的 1.67 倍，理由是**綽號就叫空中堡壘**：四具
   * 發動機、分艙的自封油箱、大量的結構冗餘 —— 史實上帶著半邊機翼、沒有
   * 垂尾、三具發動機停車飛回英國的紀錄不只一次。
   *
   * 但這個倍數是猜的。**這個數字直接決定「打大機好不好玩」**，所以它一定要
   * 由試飛裁定而不是由我定（護欄重新定值是專案負責人的決定）。
   */
  hp: 5000,

  /**
   * 命中盒 —— **全部由當下的網格量出來**，產生腳本
   * `test/tools/hitbox-emit.probe.ts`。
   *
   * 一個部位可以有多個盒（2026-08-31 放寬那條護欄），因為好幾個部位的
   * AABB 裡大半是空氣：
   *
   * ```
   *   尾段    平尾＋垂尾是一個十字，AABB 連十字外面那四塊空氣一起包
   *   機翼    上反角。整片機翼的 AABB 高 = 翼厚 ＋ 翼尖相對翼根的抬升
   *   發動機  多發機的四／兩具發動機共用一個盒，中間那一大片內翼一起包
   * ```
   *
   * 而 `hitAircraft` 的倍率取「線段碰到的**所有**盒之中最高的那一個」，
   * 那些空氣於是以 tail 1.2 或 engine 2.0 贏過機身盒的 1.0 與機翼盒的 0.7
   * —— 實測 20v20/12v12/8v8 曾有 **47% 的命中被判成 tail**，名目基準
   * `fuselage` 只有 4%，而座艙 2.5 從正後方是 **0%**。
   *
   * ── 這一台拆成幾段、買到什麼 ─────────────────────────────
   *
   * ```
   *   機翼正後方投影   1段 38.6(2盒)  2段 39.3(4盒)  3段 38.3(6盒)  4段 39.0(6盒)  5段 37.6(8盒)  6段 40.5(8盒)  7段 37.1(10盒)  8段 37.7(12盒)  9段 38.3(12盒)  10段 36.3(12盒)
   *   → 取 2 段
   *   尾段切分 |x| = 1.05　正後方 17.8 → 17.8 m²
   *   發動機 1 盒 → 5 盒　正後方 14.0 → 14.2 m²
   *
   *   正投影 m²      正後方  後上方30°   側方90°    正上方
   *   真實外形    31.8  103.0  65.1  201.5
   *   拆之後      60.4  161.9  85.0  240.2
   *   放大倍數    1.90×  1.57×  1.31×  1.19×
   * ```
   *
   * 【段數為什麼不是越多越好】切分面等分，落在兩個展向站位之間的那一段
   * 裡有翼面卻**一個頂點都沒有** —— `hitbox.test.ts` 的「沒有空盒」會紅，
   * 而那條測試沒有錯（它擋的是打不到的裝飾盒，只是它只看得到頂點）。做法是
   * 把空段**併進鄰居**，展向仍然連續、覆蓋率一格不少。**上限由機翼網格的
   * 展向站位數決定**，不是由「程式化還是 GLB」決定 —— 109 換成 GLB 之後
   * 曲線一模一樣，切超過兩段照樣被併回去。
   *
   * 【切分面怎麼保證不留縫】x 用切分面本身當盒的邊界（相鄰兩段共用、各自
   * 往外讓 0.02），y/z 取「把三角形夾進這一片、再只留落在原機翼盒內的點」
   * 的緊界。第一版是把**頂點**分堆再各取緊包圍盒 —— 頂點覆蓋率完美，但
   * P-51D 兩段之間有 2.7 m 的翼面沒有任何盒子蓋著，那一段打得到卻不扣血。
   * 抓到它的是「盒的投影比真實外形還小」這個幾何上不可能的數字。
   *
   * 【左翼是右翼的鏡射】網格本身有 0.01 級的左右不對稱，照兩邊各自算會得到
   * 對不齊的兩組盒。命中盒是**簡化的傷害體積**，不是外形的複製品。
   */
  hitBoxes: [
    makeHitBox('cockpit', [-1.25, -1.10, -6.03], [1.25, 2.40, -1.60]),             // 座艙
    makeHitBox('engine', [-7.04, -0.57, -2.81], [-5.76, 1.07, 2.64]),             // 發動機艙 1
    makeHitBox('engine', [-3.86, -0.83, -3.08], [-2.24, 0.82, 2.64]),             // 發動機艙 2
    makeHitBox('engine', [-1.20, -0.82, -3.18], [1.20, 1.19, 2.91]),              // 發動機艙 3
    makeHitBox('engine', [2.24, -0.83, -3.08], [3.86, 0.82, 2.64]),              // 發動機艙 4
    makeHitBox('engine', [5.76, -0.57, -2.81], [7.04, 1.07, 2.64]),              // 發動機艙 5
    makeHitBox('fuselage', [-1.25, -1.30, -1.60], [1.25, 1.80, 9.50]),              // 機身
    makeHitBox('tail', [-6.52, 1.06, 10.89], [6.52, 1.46, 14.09]),             // 平尾
    makeHitBox('tail', [-1.08, -0.34, 9.50], [1.08, 5.70, 15.24]),             // 垂尾＋尾錐
    makeHitBox('wingRight', [1.21, -0.64, -1.40], [8.56, 1.07, 4.42]),              // 右翼第 1 段
    makeHitBox('wingRight', [8.51, 0.13, -0.40], [15.86, 1.08, 3.72]),             // 右翼第 2 段
    makeHitBox('wingLeft', [-8.56, -0.64, -1.40], [-1.21, 1.07, 4.42]),             // 左翼第 1 段
    makeHitBox('wingLeft', [-15.86, 0.13, -0.40], [-8.51, 1.08, 3.72]),             // 左翼第 2 段
  ],

  battery: B17G_BATTERY,
  turrets: B17G_TURRETS,
}

/**
 * 史實性能。**L2 測試還沒有把這一台納進來** —— 納進來之前，上面的氣動係數
 * 沒有一個算定案。
 *
 * 資料取自 B-17G（R-1820-97、作戰重量 54,000 lb）。
 */
export const B17G_HISTORICAL: HistoricalReference = {
  // 287 mph @ 25,000 ft
  vmaxAtCritical: { speed: 462 * KMH, altitude: 7620 },
  // 海平面沒有可靠的公開值；由渦輪增壓的功率曲線與 cd0 反推約 380
  vmaxSeaLevel: 380 * KMH,
  // 史實爬升：到 20,000 ft 約 37 分 → 平均 2.7 m/s；海平面較高，取 4.5
  climbRateSeaLevel: 4.5,
  // 約 90 mph
  stallSpeed: 145 * KMH,
  serviceCeiling: 10850,
  // 厚對稱翼型（NACA 0018）配大展弦比。低於 He 111 的 1.55（那台有彎度）
  clMax: 1.42,
}
