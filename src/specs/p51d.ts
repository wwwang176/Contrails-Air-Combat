import { DEG } from '../core/math'
import { makeHitBox } from '../world/hit'
import { P51D_BATTERY } from '../weapons/p51d'
import type { AircraftSpec, HistoricalReference } from './types'

const HP = 745.7
const KMH = 1 / 3.6

export const P51D: AircraftSpec = {
  id: 'p51d',
  name: 'P-51D Mustang',
  faction: 'allied',
  role: 'fighter',

  /**
   * # 3,900 kg —— 這是「史實表自己指向的重量」，不是 P-51D 的戰鬥重量
   *
   * 【怎麼定出來的】問「其餘全為出貨值，只動質量，史實這三項各自要求多重」：
   *
   * ```
   *                     P-51D                 Bf 109 K-4（對照組）
   *   海平面爬升   →   3,804 kg（88.5%）      3,343 kg（ 99.0%）
   *   實用升限     →   3,841 kg（89.3%）      3,577 kg（106.0%）
   *   失速速度     →   3,906 kg（90.8%）*     3,680 kg（109.0%）
   *                  ────────────────       ────────────────
   *                  三者聚在 2.7% 之內       散開 10%，且跨在出貨值兩側
   * ```
   * \* 已扣掉模型 clMax 與史實 1.45 的偏差
   *
   * **失速那一項是關鍵**：它完全不吃出力，只有升力除以重量。它跟爬升、升限
   * 指到同一個地方，就排除了「模型出力不足、用減重去補」這個解釋。K-4 做
   * 同一件事三項散開、還跨在出貨值兩側 —— 所以這不是模型的通病，是 P-51D
   * 這一台的史實表在講一個比 4,300 kg 輕的載重狀態。取三者中位的 3,900。
   *
   * 【誠實揭露：兩台的載重狀態並不對等】3,900 kg ≈ 8,600 lb ≈ 空重 + 飛行員
   * + 彈藥 + 約 11% 內油。而 K-4 的 3,375 kg **是它的滿油起飛重量**。也就是
   * 說遊戲裡是「滿油的 109 對上幾乎空油箱的 P-51」。
   *
   * 【裁決的出處】2026-08-27，專案負責人：「因為其他數據玩家能感受到，但是
   * 重量玩家感受不到」。爬升、轉彎、滾轉玩家摸得到，載重狀態摸不到，所以
   * 用後者去換前者。要推翻這個取捨，動的是 `mass`，不是下面任何一項。
   *
   * 【慣量刻意**不**跟著縮】質量是校準值——它是「模型重現真機實測性能所需
   * 的重量」；慣量是實測值，不參與這次校準，所以留在原地。兩者因此不一致，
   * 這是知情的。
   *
   * 【如果連動會怎樣】等比縮到 9,977／18,140／7,982 之後，P-51D 的起始滾轉
   * 角加速度會**超過** K-4（實測 0.02598 對 0.02516 rad/s），把「109 起滾
   * 較快」這個性格翻掉。滾轉**率**本來就不受慣量影響
   *（p = (clDa/−clP)·(2V/b)），翻掉的只有拉桿後那一瞬間的響應——但那是
   * 玩家摸得到的東西，而質量不是。同一個裁決（見上）在這裡指向不要動它。
   */
  mass: 3900,
  inertia: { pitch: 11000, yaw: 20000, roll: 8800 },

  // 【以下數字一律以「僅退回本項、其餘全為出貨值」實測，非規劃期估算】
  //   oswald 0.80：升限 12,424.6 m（−2.70%）、爬升 1,014.7 m/min（−4.27%）、
  //                L/D_max 14.99、9,000 m 對 K-4 的速度優勢 23.2 km/h
  //   oswald 0.85：升限 12,571.9 m（−1.55%）、爬升 1,023.1 m/min（−3.48%）、
  //                L/D_max 15.45、9,000 m 優勢 25.8 km/h              ← 出貨
  //   oswald 0.88：升限 12,655.6 m（−0.90%）、爬升 1,027.7 m/min（−3.05%）、
  //                L/D_max 15.72、9,000 m 優勢 27.2 km/h
  // 三者的六項史實驗收全部通過，所以這一項不再是「為了讓升限進 ±5% 的必要
  // 條件」——那個角色現在由 `mass` 擔任。0.85 取的是層流翼 Oswald 因子教科書
  // 區間 0.70~0.85 的上緣。
  // 【誠實揭露】L/D_max 15.45 仍比史實約 14.6 高 5.8%。接受的理由是本模型的
  // cd0 為常數、不含 CL 相依的黏性項，此處的 oswald 比教科書的 Oswald 因子
  // 更接近純展向效率。上界已寫成 specs.test.ts 的斷言。
  wing: { area: 21.83, span: 11.28, chord: 1.98, oswald: 0.85 },

  lift: {
    clAlpha: 4.4,
    alphaZero: -2.5 * DEG,
    // CL_max 為推導值 clAlpha·(alphaCrit − alphaZero)。
    // 【僅退回本項、其餘全為出貨值的實測】
    //   15.5°：CL_max 1.3823（史實 1.45，−4.67%）、失速 163.76 km/h（+2.35%）  ← 出貨
    //   17.0°：CL_max 1.4975（+3.28%）、失速 157.34 km/h（−1.66%）
    // 兩者的失速都在 ±5% 內、CL_max 都在 specs 測試的 ±8% 內。取 15.5° 是因為
    // 它同時讓 CL_max 與失速都落在史實的**同一側附近**，不必靠「CL_max 偏高
    // 去補質量偏高」互相抵銷 —— 那個抵銷在質量修正之後已經不需要了。
    alphaCrit: 15.5 * DEG,
    stallBlend: 8 * DEG,
    postStallFactor: 0.6,
    slatAlphaBonus: 0,
    slatDeployAlpha: Infinity, // 無縫翼，永不展開
    slatRetractAlpha: Infinity,
  },

  drag: { cd0: 0.0163, cdBeta: 0.6, machCrit: 0.72, machDragFactor: 60 },

  side: { cyBeta: -0.7 },

  moments: {
    cm0: 0.02, cmAlpha: -0.8, cmQ: -12, cmDe: 1.2,
    clBeta: -0.08, clP: -0.45, clDa: 0.033,
    cnBeta: 0.1, cnR: -0.12, cnDr: 0.07,
  },

  // qRef 對應 480 km/h 海平面動壓，該速度下副翼權限為滿（滾轉率約 100°/s）
  controlStiffening: { qRef: 10884, aileronK: 0.35, elevatorK: 0.25, rudderK: 0.25 },

  engine: {
    // V-1650-7 二級二速增壓，67"Hg WEP
    gears: [
      { powerSeaLevel: 1490 * HP, powerCritical: 1720 * HP, altCritical: 1900 },
      { powerSeaLevel: 1290 * HP, powerCritical: 1370 * HP, altCritical: 5900 },
    ],
    // **這一項現在買的是「峰值位置」與「高空優勢」，不再是實用升限。**
    // 物理動機：P-51D 的機腹進氣道總壓恢復極佳；史實極速峰值在 7,600 m 而
    // 高檔臨界高度僅 5,900 m，這 1,700 m 的抬升正是 ram 的效果。
    //
    // 【僅退回本項、其餘全為出貨值的實測】升限對這一項已經很鈍（質量修正之後
    // 它有 3.5 點餘裕），會動的是另外兩件事，而且方向相反：
    //
    //   ram   升限        9,000 m 對 K-4 速度優勢   峰值餘裕（4,600 m / 10,600 m）
    //   0.80  −2.14%        13.5 km/h              39.8 / 26.1
    //   0.85  −1.85%        19.6 km/h              35.6 / 18.9
    //   0.90  −1.55%        25.8 km/h              31.3 / 11.7      ← 出貨
    //   0.95  −1.25%        32.1 km/h              27.0 /  4.3
    //
    // ram 愈高，高空愈快 ——「P-51 是高空戰鬥機」這個性格愈明顯（relative.test.ts
    // 要求 9,000 m 拉開 20 km/h 以上），但模型的極速峰值也愈往上飄，逼近
    // 10,600 m 的取樣點。0.90 是兩者都有餘裕的位置：高空優勢 25.8 km/h，
    // 峰值餘裕 11.7 km/h。
    //
    // 【誠實揭露】模型並未把峰值精確放在 7,600 m，只是在該取樣點上把速度值
    // 對準（734.3 km/h，+4.45%）。「極速在臨界高度附近達到峰值」測試守的是
    // 7,600 m 高於 4,600 m 與 10,600 m 兩側，不是峰值的確切位置。
    ramEfficiency: 0.9,
  },

  // 真實定速槳在最佳爬升速度處的效率約 0.75~0.80。
  // 【僅退回本項、其餘全為出貨值的實測】
  //   etaMax 0.85 / vRef 42：η = 0.727 @ 81.2 m/s、爬升 948.4 m/min
  //   etaMax 0.90 / vRef 55：η = 0.710 @ 85.6 m/s、爬升 894.6 m/min
  //   etaMax 0.90 / vRef 42：η = 0.773 @ 82.1 m/s、爬升 1,023.1 m/min  ← 出貨
  // etaMax 0.90 是 1940 年代定速螺旋槳的物理上限，不准再往上。
  //
  // vRef 不能再低。T = η(V)·P/V 在 V→0 的極限為 etaMax·P/vRef，vRef = 42 時
  // 為 23.809 kN，而 figureOfMerit 0.80 下的動量理論靜推力上限是 24.136 kN
  // ——**只剩 1.37% 餘裕**。0.80 是螺旋槳靜推力效率的合理上緣，也是
  // specs.test.ts 的斷言上界。夾制若被觸發，propulsion.test.ts 的 V→0 解析
  // 極限測試會由 0.12% 誤差跳到約 1.4% 而轉紅——那是調參的後果，不是
  // propThrust 的迴歸；該情況已由 propulsion.test.ts 的專屬測試守住。
  //
  // 【這一組不必再為爬升率背責任】海平面爬升率一度差史實 14.45%，當時的
  // 診斷是「螺旋槳模型的物理上限擋住了」，於是 etaMax／vRef／figureOfMerit
  // 三項全被推到各自的邊緣。真正的成因是質量（見 `mass` 的說明），修正之後
  // 爬升率是 −3.48%。**不要再為了爬升率去動這三個數字** —— 沿那條路走要把
  // figureOfMerit 推到 0.98 以上，那已經不是螺旋槳做得到的事。
  prop: { diameter: 3.4, etaMax: 0.9, vRef: 42, figureOfMerit: 0.8 },

  limits: { gPositive: 8, gNegative: -4, vne: 810 * KMH },

  hp: 1000,

  /**
   * 命中盒 —— 數字**取自造型的實測包圍盒**，不是手打的。
   *
   * 分界怎麼定的（機體座標，可對照 render/geometry/p51d.ts）：
   *   引擎  整流罩尖端 −3.402 到防火牆。防火牆取 −0.90，那是機翼前緣與
   *         座艙前壁之間、機身還沒被機翼佔住的最後一站。
   *   座艙  座艙開口 z 0.085…2.485、玻璃罩頂 1.066、內裝底 0.080。
   *   機身  防火牆到平尾前緣（4.407）。y 下界 −1.04 是散熱器導管的最低點
   *         （實測 −1.026），上界 0.80 是背鰭在該段的可見高度。
   *   尾翼  平尾前緣 4.407 起。x 到 ±1.98（平尾半翼展 1.97）、y 到 1.93
   *         （垂尾頂端 1.920）、z 到 6.58（方向舵後緣 6.571）。
   *   機翼  由機身側面 0.44 到翼尖 5.64。z 下界 −0.97 涵蓋翼根整流罩在
   *         x = 0.44 的前緣（−0.954）。
   *
   * 【已驗證】每一個頂點（螺旋槳除外）都落在這六個盒之內，而六個盒的
   * 體積總和 58.2 m³ 只有整機包圍盒 331.4 m³ 的 18%。
   */
  hitBoxes: [
    makeHitBox('engine', [-0.45, -0.82, -3.42], [0.45, 0.68, -0.90]),
    makeHitBox('cockpit', [-0.40, 0.05, 0.07], [0.40, 1.08, 2.50]),
    makeHitBox('fuselage', [-0.45, -1.04, -0.90], [0.45, 0.80, 4.42]),
    makeHitBox('tail', [-1.98, -0.31, 4.40], [1.98, 1.93, 6.58]),
    makeHitBox('wingRight', [0.44, -0.86, -0.97], [5.65, -0.08, 2.06]),
    makeHitBox('wingLeft', [-5.65, -0.86, -0.97], [-0.44, -0.08, 2.06]),
  ],

  battery: P51D_BATTERY,
  // 戰鬥機沒有自衛砲塔
  turrets: [],
}

export const P51D_HISTORICAL: HistoricalReference = {
  vmaxAtCritical: { speed: 703 * KMH, altitude: 7600 },
  vmaxSeaLevel: 595 * KMH,
  climbRateSeaLevel: 1060 / 60,
  stallSpeed: 160 * KMH,
  serviceCeiling: 12770,
  clMax: 1.45,
}
