import { DEG } from '../core/math'
import { makeHitBox } from '../world/hit'
import { P51D_BATTERY } from '../weapons/p51d'
import type { AircraftSpec, HistoricalReference } from './types'

const HP = 745.7
const KMH = 1 / 3.6
const MPH = 0.44704
const FT = 0.3048
const FPM = 0.00508

export const P51D: AircraftSpec = {
  id: 'p51d',
  name: 'P-51D Mustang',
  faction: 'allied',
  role: 'fighter',

  /**
   * # 4,427 kg —— **試飛報告上的重量，沒有校準過**
   *
   * 9,760 lb 就是 AAF 44-15342（P-51D-15NA，67"Hg WEP）的起飛總重，而
   * `P51D_HISTORICAL` 的極速／爬升／升限就是那一份試飛的數字。兩者同重，
   * 這是**前提**不是巧合 —— 見 `P51D_HISTORICAL` 的說明。
   *
   * ── 診斷質量的標準手法，以及它在這台的讀數 ──────────────────
   *
   * 診斷質量的標準手法是「只動質量，史實三項各自要求多重」。出貨值實測：
   *
   * ```
   *                    要求          扣掉 clMax 偏差後
   *     海平面爬升   4,370 kg（98.7%）
   *     實用升限     4,089 kg（92.4%）
   *     失速         4,220 kg（95.3%）  →  4,428 kg（100.0%）
   * ```
   *
   * 失速那一項反解的是 m/CL_max，而模型的推導 CL_max 是 1.382、史實那個
   * 數字要求 1.450（−4.7%）。4,220 × (1.450/1.382) = 4,428 —— 它自己指的
   * 就是試飛重量。升限那一項偏輕 7.6%（升限本身是 −3.2%，仍在 ±5% 內），
   * 那是模型高空出力略短，不是重量的證據。
   *
   * ── 這裡有一個會反覆咬人的陷阱 ──────────────────────────────
   *
   * 如果把 `stallSpeed` 填成通行的 160 km/h（而不是換算到試飛重量的
   * 170.4），失速那一項會指向 3,900 kg。它完全不吃出力，所以很容易被當成
   * 「排除了模型出力不足」的鐵證。**不要那樣讀。**
   *
   * 它只證明一件事：「160 km/h 這個失速數字屬於一架 3,900 kg 的 P-51」。
   * 那個數字本來就不是 44-15342 那份報告的（那份沒有量失速），它是另一個
   * 載重狀態的。速度／爬升／升限自己指向的是 9,760 lb，失速不是它們的證人。
   *
   * 【所以 `P51D_HISTORICAL.stallSpeed` 是換算出來的】同一架飛機、同一個
   * CL_max 1.45，換到 4,427 kg 就是 170.4 km/h。四項因此回到同一個重量。
   *
   * 【慣量與質量現在自洽】11,000 / 20,000 / 8,800 是實測值，本來就屬於
   * 4,300 kg 這一級。
   */
  mass: 4427,
  inertia: { pitch: 11000, yaw: 20000, roll: 8800 },

  /**
   * 【只退回本項、其餘出貨值的實測】
   *
   * ```
   *   oswald   升限      海平面爬升        臨界極速   9,000 m 對 K-4 優勢
   *   0.80    −4.66%   1,038.3 −5.37%    +4.24%    20.9 km/h   ← 爬升出界
   *   0.85    −3.48%   1,047.5 −4.54%    +4.49%    23.9        ← 出貨
   *   0.88    −2.81%   1,052.5 −4.08%    +4.62%    25.6
   * ```
   *
   * 【它現在是承重的，不再只是偏好】出力修正之後爬升的餘裕只剩 0.5 點
   *（見 `prop`），0.80 就會讓爬升出界。0.85 是層流翼 Oswald 因子教科書區間
   * 0.70~0.85 的上緣；0.88 還能再買 0.46 點但已經離開那個區間。
   *
   * 【誠實揭露】L/D_max 比史實約 14.6 高約 6%。接受的理由是本模型的 cd0 為
   * 常數、不含 CL 相依的黏性項，此處的 oswald 比教科書的 Oswald 因子更接近
   * 純展向效率。上界已寫成 specs.test.ts 的斷言。
   */
  wing: { area: 21.83, span: 11.28, chord: 1.98, oswald: 0.85 },

  lift: {
    clAlpha: 4.4,
    alphaZero: -2.5 * DEG,
    // CL_max 為推導值 clAlpha·(alphaCrit − alphaZero)。史實失速 170.4 km/h
    // （由 CL_max 1.45 換算到試飛重量，見 P51D_HISTORICAL）。
    // 【只退回本項、其餘出貨值的實測】
    //   15.0°：CL_max 1.3439（−7.32%）、失速 176.95 km/h（+3.87%）
    //   15.5°：CL_max 1.3823（−4.67%）、失速 174.48 km/h（+2.42%）  ← 出貨
    //   16.0°：CL_max 1.4207（−2.02%）、失速 172.10 km/h（+1.03%）
    //   16.5°：CL_max 1.4591（+0.63%）、失速 169.82 km/h（−0.31%）
    //
    // 【16.0° 兩項都比 15.5° 好，為什麼還是留 15.5°】因為 CL_max 決定瞬間
    // 轉彎的氣動極限，而 `relative.test.ts` 有兩條斷言釘在 P-51 對 K-4 的
    // 瞬間轉彎交叉點（478 km/h）上。15.5° 已經全綠，動它只為了換餘裕不划算。
    // 哪天需要餘裕（例如失速那一項被別的改動推近門檻），16.0° 是第一順位。
    alphaCrit: 15.5 * DEG,
    stallBlend: 8 * DEG,
    postStallFactor: 0.6,
    slatAlphaBonus: 0,
    slatDeployAlpha: Infinity, // 無縫翼，永不展開
    slatRetractAlpha: Infinity,
  },

  /**
   * 【只退回本項、其餘出貨值的實測】
   *
   * ```
   *   cd0      臨界極速   海面極速   爬升     升限     9,000 m 對 K-4 優勢
   *   0.0178    +5.4%     −0.1%   −1.2%   −2.9%    33.1 km/h
   *   0.0180    +5.0%     −0.5%   −1.4%   −3.1%    29.0
   *   0.0182    +4.6%     −0.9%   −1.6%   −3.2%    25.1        ← 出貨
   *   0.0184    +4.2%     −1.2%   −1.8%   −3.3%    21.2
   *   0.0186    +3.8%     −1.6%   −2.0%   −3.4%    17.4
   * ```
   *
   * 這一項同時被兩條方向相反的斷言夾住：臨界高度極速要 ≤ +5%
   * （`historical.test.ts`），9,000 m 對 K-4 的優勢要 > 20 km/h
   * （`relative.test.ts`）。0.0182 兩邊各留約 0.4 點與 5 km/h。
   *
   * 【不要再往下調】0.0163 那個值是配著短少的出力反解出來的，比文獻對
   * P-51D 的 CD0 估值（約 0.0176）還低 7%。出力修正之後它必須回到物理值，
   * 不然臨界高度極速會衝到 +8.6%。
   *
   * 【machCrit 不准往下調】試過用它壓高空極速（0.62 時五項也全綠），但
   * Bf 109 K-4 的 machCrit 是 **0.68** —— 把 P-51 壓到它以下等於宣稱野馬的
   * 壓縮性比 109 差，方向是反的。這條路封掉。
   */
  drag: { cd0: 0.0182, cdBeta: 0.6, machCrit: 0.72, machDragFactor: 60 },

  side: { cyBeta: -0.7 },

  moments: {
    cm0: 0.02, cmAlpha: -0.8, cmQ: -12, cmDe: 1.2,
    clBeta: -0.08, clP: -0.45, clDa: 0.033,
    cnBeta: 0.1, cnR: -0.12, cnDr: 0.07,
  },

  // qRef 對應 480 km/h 海平面動壓，該速度下副翼權限為滿（滾轉率約 100°/s）
  controlStiffening: { qRef: 10884, aileronK: 0.35, elevatorK: 0.25, rudderK: 0.25 },

  engine: {
    /**
     * V-1650-7 二級二速增壓，**67"Hg WEP、3,000 rpm**。
     *
     * 【欄位註解說「功率為 WEP 值」，所以每一格都必須是 WEP 值】這句話值得
     * 寫下來，因為出過錯：低增壓檔的海平面一度填 1,490 hp，那是 **61"Hg 的
     * 軍用／起飛**額定；高增壓檔的臨界一度填 1,370 hp，也遠低於 WEP 的
     * 1,555。同一個檔位裡混了兩個出力狀態，海平面短少 9.4%、高增壓臨界
     * 短少 13.5% —— 剛好就是「爬升差史實 14%」的量。
     *
     * 【海平面填 1,780 而不是額定的 1,630】1,630 是靜態標準日的額定值；
     * 44-15342 的 WEP 爬升**實測** 0 ft 為 1,780 BHP（4,800 ft 1,730、
     * 5,000 ft 1,720）。史實表用的是那次試飛，出力也要用那次的實測值，
     * 不然又是在混狀態。低／高增壓檔的臨界則照額定：
     * 1,700 BHP @ 5,750 ft、1,555 BHP @ 19,300 ft。
     *
     * 【為什麼海平面比臨界高】67"Hg 在低增壓檔一路到 5,750 ft 都靠節流維持，
     * 排氣背壓隨高度下降反而讓 BHP 略降 —— 試飛的 1,780 → 1,720 就是這一段。
     */
    gears: [
      { powerSeaLevel: 1780 * HP, powerCritical: 1700 * HP, altCritical: 5750 * FT },
      { powerSeaLevel: 1400 * HP, powerCritical: 1555 * HP, altCritical: 19300 * FT },
    ],
    // **這一項現在買的是「峰值位置」與「高空優勢」，不再是實用升限。**
    // 物理動機：P-51D 的機腹進氣道總壓恢復極佳；試飛的極速峰值在 26,000 ft
    // （7,925 m）而引擎高檔臨界高度是 19,300 ft（5,883 m），這 2,042 m 的
    // 抬升正是 ram 的效果。
    //
    // 【僅退回本項、其餘全為出貨值的實測】升限對這一項已經很鈍（質量修正之後
    // 它有 3.5 點餘裕），會動的是另外兩件事，而且方向相反：
    //
    //   ram   升限      9,000 m 對 K-4 速度優勢   峰值餘裕（±3,000 m）
    //   0.80  −4.09%        11.2 km/h              86.1 / 54.0
    //   0.85  −3.79%        17.5 km/h              86.0 / 46.0
    //   0.90  −3.48%        23.9 km/h              86.0 / 37.9      ← 出貨
    //   0.95  −3.17%        30.5 km/h              85.7 / 29.7
    //
    // ram 愈高，高空愈快 ——「P-51 是高空戰鬥機」這個性格愈明顯（relative.test.ts
    // 要求 9,000 m 拉開 20 km/h 以上），但模型的極速峰值也愈往上飄，逼近
    // 10,600 m 的取樣點。0.90 是兩者都有餘裕的位置：高空優勢 25.8 km/h，
    // 峰值餘裕 37.9 km/h。**0.90 是下界**：`relative.test.ts` 要求 9,000 m
    // 對 K-4 拉開 20 km/h，0.85 只有 17.5。
    //
    // 【誠實揭露】模型並未把峰值精確放在 7,925 m，只是在該取樣點上把速度值
    // 對準（743.3 km/h，+4.49%）。「極速在臨界高度附近達到峰值」測試守的是
    // 7,925 m 高於 ±3,000 m 兩側，不是峰值的確切位置。
    ramEfficiency: 0.9,
  },

  /**
   * 真實定速槳在最佳爬升速度處的效率約 0.75~0.80。
   *
   * 【etaMax 0.90 是 1940 年代定速槳的物理上限，不准再往上。】
   *
   * ── 【vRef 由動量理論決定，不是由爬升決定】────────────────────
   *
   * T = η(V)·P/V 在 V→0 的極限是 etaMax·P/vRef。這個值必須小於致動盤的
   * 動量理論靜推力上限 figureOfMerit × ∛(2ρA P²)，否則螺旋槳在起飛推力上
   * 憑空生出動量。`propulsion.test.ts` 有一條專門的護衛在守它，失敗訊息
   * 直接寫著「這是調參越界」。
   *
   * 出力修正成真實的 67″Hg WEP（海平面 1,780 hp）之後，這條線被頂到了 ——
   * **功率上升時解析極限 ∝ P，動量上限只 ∝ P^⅔**，餘裕會被吃掉：
   *
   * ```
   *   vRef   夾制餘裕   海平面爬升      升限     臨界極速
   *    42     −4.5%   1,079.2 −1.65%  −3.18%   +4.60%   ← 越界
   *    44     +0.1%   1,058.0 −3.58%  −3.38%   +4.53%   ← 餘裕太薄
   *    45     +2.4%   1,047.5 −4.54%  −3.48%   +4.49%   ← 出貨
   *    46     +4.6%   1,037.1 −5.49%  −3.82%   +4.45%   ← 爬升出界
   * ```
   *
   * 45 是**唯一**同時滿足兩邊的值：夾制留 2.4% 餘裕、爬升 −4.54% 還在
   * ±5% 內（只剩 0.5 點）。降 etaMax 也能放鬆夾制，但代價更大
   *（0.85 / vRef 42 → 爬升 −8.86%）。
   *
   * 【爬升的餘裕只剩 0.5 點，動這三個數字之前請先讀這一段】任何往下調
   * etaMax、往上調 vRef、或往上調 cd0 的動作都會讓爬升出界；往反方向動
   * 則會撞上動量理論。要更多餘裕只能從別處來（`oswald` 已在教科書上緣、
   * `cd0` 已被臨界極速夾住）。
   */
  prop: { diameter: 3.4, etaMax: 0.9, vRef: 45, figureOfMerit: 0.8 },

  limits: { gPositive: 8, gNegative: -4, vne: 810 * KMH },

  hp: 1000,
  /** 【接線步驟：全部 1.0】真值在下一個 commit 填。 */
  protection: {
    cockpit: 1.0, engine: 1.0, tail: 1.0, fuselage: 1.0, wingLeft: 1.0, wingRight: 1.0,
  },

  /**
   * 命中盒 —— **全部由當下的網格量出來**，產生腳本
   * `test/tools/hitbox-emit.probe.ts`。
   *
   * 一個部位可以有多個盒（2026-08-31 放寬那條護欄），因為好幾個部位的
   * AABB 裡大半是空氣：
   *
   * ```
   *   尾段    平尾＋垂尾是一個十字，AABB 連十字外面那四塊空氣一起包
   *   機翼    上反角。整片機翼的 AABB 高 = 翼厚 ＋ 翼尖相對翼根的抬升
   *   發動機  多發機的四／兩具發動機共用一個盒，中間那一大片內翼一起包
   * ```
   *
   * 而 `hitAircraft` 的倍率取「線段碰到的**所有**盒之中最高的那一個」，
   * 那些空氣於是以 tail 1.2 或 engine 2.0 贏過機身盒的 1.0 與機翼盒的 0.7
   * —— 實測 20v20/12v12/8v8 曾有 **47% 的命中被判成 tail**，名目基準
   * `fuselage` 只有 4%，而座艙 2.5 從正後方是 **0%**。
   *
   * ── 這一台拆成幾段、買到什麼 ─────────────────────────────
   *
   * ```
   *   機翼正後方投影   1段 5.7(2盒)  2段 5.5(4盒)  3段 5.8(4盒)  4段 6.2(4盒)  5段 5.4(6盒)  6段 5.8(6盒)  7段 5.4(8盒)  8段 5.7(8盒)  9段 5.8(8盒)  10段 5.3(8盒)
   *   → 取 2 段
   *   尾段切分 |x| = 0.25　正後方 1.8 → 1.8 m²
   *
   *   正投影 m²      正後方  後上方30°   側方90°    正上方
   *   真實外形    4.4  14.9  13.6  29.4
   *   拆之後      8.3  24.9  18.9  37.9
   *   放大倍數    1.89×  1.67×  1.39×  1.29×
   * ```
   *
   * 【段數為什麼不是越多越好】切分面等分，落在兩個展向站位之間的那一段
   * 裡有翼面卻**一個頂點都沒有** —— `hitbox.test.ts` 的「沒有空盒」會紅，
   * 而那條測試沒有錯（它擋的是打不到的裝飾盒，只是它只看得到頂點）。做法是
   * 把空段**併進鄰居**，展向仍然連續、覆蓋率一格不少。**上限由機翼網格的
   * 展向站位數決定**，不是由「程式化還是 GLB」決定 —— 109 換成 GLB 之後
   * 曲線一模一樣，切超過兩段照樣被併回去。
   *
   * 【切分面怎麼保證不留縫】x 用切分面本身當盒的邊界（相鄰兩段共用、各自
   * 往外讓 0.02），y/z 取「把三角形夾進這一片、再只留落在原機翼盒內的點」
   * 的緊界。第一版是把**頂點**分堆再各取緊包圍盒 —— 頂點覆蓋率完美，但
   * P-51D 兩段之間有 2.7 m 的翼面沒有任何盒子蓋著，那一段打得到卻不扣血。
   * 抓到它的是「盒的投影比真實外形還小」這個幾何上不可能的數字。
   *
   * 【左翼是右翼的鏡射】網格本身有 0.01 級的左右不對稱，照兩邊各自算會得到
   * 對不齊的兩組盒。命中盒是**簡化的傷害體積**，不是外形的複製品。
   */
  hitBoxes: [
    makeHitBox('cockpit', [-0.40, 0.05, 0.07], [0.40, 1.08, 2.50]),              // 座艙
    makeHitBox('engine', [-0.45, -0.82, -3.42], [0.45, 0.68, -0.90]),             // 發動機
    makeHitBox('fuselage', [-0.45, -1.04, -0.90], [0.45, 0.80, 4.42]),              // 機身
    makeHitBox('tail', [-2.00, 0.40, 4.43], [2.00, 0.56, 5.71]),              // 平尾
    makeHitBox('tail', [-0.27, -0.27, 4.38], [0.27, 1.94, 6.49]),              // 垂尾＋尾錐
    makeHitBox('wingRight', [0.39, -0.82, -0.99], [3.06, -0.20, 1.97]),             // 右翼第 1 段
    makeHitBox('wingRight', [3.02, -0.52, -0.48], [5.69, -0.07, 1.41]),             // 右翼第 2 段
    makeHitBox('wingLeft', [-3.06, -0.82, -0.99], [-0.39, -0.20, 1.97]),            // 左翼第 1 段
    makeHitBox('wingLeft', [-5.69, -0.52, -0.48], [-3.02, -0.07, 1.41]),            // 左翼第 2 段
  ],

  battery: P51D_BATTERY,
  // 戰鬥機沒有自衛砲塔
  turrets: [],
}

/**
 * 史實驗收值。**四項全部出自 AAF 44-15342 的試飛** —— P-51D-15NA、
 * 起飛總重 **9,760 lb = 4,427 kg**、67"Hg WEP、3,000 rpm：
 *
 * ```
 *   海平面極速                    375 mph
 *   高增壓臨界高度 26,000 ft      442 mph
 *   海平面爬升率                3,600 fpm（實測 0 ft 的 BHP 為 1,780）
 *   實用升限                   41,600 ft
 * ```
 *
 * 【失速是換算的，而且非換算不可】那份報告沒有量失速。通行的 160 km/h 配
 * CL_max 1.45 反解出來是 3,900 kg —— **另一個載重狀態**。直接放進這張表，
 * 就是拿一架 8,600 lb 的失速去配一架 9,760 lb 的極速，四項不再屬於同一架
 * 飛機。同一個 CL_max 換到 4,427 kg 是：
 *
 *     √(2 × 4,427 × 9.80665 / (1.225 × 21.83 × 1.45)) = 47.32 m/s = 170.4 km/h
 *
 * 【為什麼這件事值得寫這麼多】混了載重狀態的史實表，症狀看起來完全像是模型
 * 壞了，而調係數只會把一項調對、另一項調更歪。這台被咬過一次：三項反解
 * 「都指向 3,900 kg」，於是質量被校準掉 —— 真正的成因是 `engine.gears` 混了
 * 出力狀態（見那裡的說明），而失速那一項從頭到尾就不是它們的證人。
 *
 * 資料來源：wwiiaircraftperformance.org 的 P-51 Mustang Performance 檔案室。
 */
export const P51D_HISTORICAL: HistoricalReference = {
  vmaxAtCritical: { speed: 442 * MPH, altitude: 26000 * FT },
  vmaxSeaLevel: 375 * MPH,
  climbRateSeaLevel: 3600 * FPM,
  stallSpeed: Math.sqrt(2 * 4427 * 9.80665 / (1.225 * 21.83 * 1.45)),
  serviceCeiling: 41600 * FT,
  clMax: 1.45,
}
