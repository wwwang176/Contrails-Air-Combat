import type { AircraftSpec } from './types'

/**
 * 手感係數：把「史實的飛機」與「玩起來的飛機」分成兩層。
 *
 * 【為什麼需要這一層】`src/specs/*.ts` 的每一個數字都對應真飛機，而
 * `test/performance/historical.test.ts` 的整層 L2 斷言（極速／失速／升限
 * ±5%、海平面爬升率 [−15%, −12%]）就是靠這個前提才有意義。直接去改 spec
 * 換手感，等於讓那層測試從此變成「符合我們亂調的數字」——它會照樣是綠的，
 * 卻不再守著任何東西。
 *
 * 所以手感調整走這裡：**史實值原封不動，遊戲執行時套一層倍率。**
 * `HISTORICAL`（全部 1.0）是那層測試永遠使用的輪廓。
 *
 * 【只放真的會動的旋鈕】不預先開一堆倍率欄位。每一項都要先評估代價
 * （見下方各欄位的註解）。
 *
 * ---
 *
 * # 遊戲裡的飛機**刻意不是史實飛機**（2026-08-07 專案負責人裁定）
 *
 * `GAME_FEEL` 出貨的 P-51D 爬升率是史實的 2.2 倍、持續迴旋率 1.9 倍。
 * 這是**明確的取捨，不是失誤**。原始的判斷是：
 *
 * > 「這個遊戲對於能量太過於寫實了，導致遊戲性很差（例如爬升很慢、
 * > 閃躲掉能量）。」
 *
 * 史實值原封不動留在 `specs/*.ts`，`historical.test.ts` 跑的是**沒有包裝
 * 的 spec**，所以那一層照樣守著真飛機。這個檔案是兩者之間唯一的閘門。
 *
 * ## 怎麼重新調（未來要改的人看這裡）
 *
 * 五個倍率不是獨立的，它們透過三條物理關係耦合。照這個順序調：
 *
 * 1. **先定極速**：高速平飛時誘導阻力可忽略，`V_max³ ∝ power / cd0`。
 *    所以 `power` 與 `cd0` **同倍率**一起動時極速不變。要改極速就打破
 *    這個比值，不要單獨動其中一個然後回頭修另一個。
 * 2. **再定失速／角落速度／瞬間迴旋**：這三個只由 `lift` 決定，與上面
 *    完全解耦。角落速度 = 「能拉滿 G 的最低速度」，它決定玩家在常態
 *    速度下轉不轉得動 —— 手感上比瞬間迴旋率本身更重要。
 * 3. **最後定持續迴旋**：由 `power` 與 `oswald` 一起決定。固定極速之後，
 *    `oswald` 與 `power` 之間還有一個自由度，而它換到的是
 *    **爬升 ↔ 俯衝保速**：`oswald` 高、`power` 低 → 俯衝保速好、爬升差；
 *    反之亦然。出貨值取 `oswald` 2.0（見 `cd0` 註解裡的整張表）。
 * 4. `roll` 獨立於以上全部，只影響操作反應速度。
 *
 * ## 調完之後必須一起回頭看的東西
 *
 * - **AI 的能量判準**：`steer.ts` 的 `brakeCornerRatio`、`unloadMargin`、
 *   `speedRecoverMargin`、`extendPitchAngle` 的兩個增益，全是照**舊包絡**
 *   調出來的。**2026-08-07 已全部重掃並結案**，結論是「來歷過期，值沒壞」
 *   —— 那些判準寫的都是**比值**（TAS ÷ 失速速度、TAS ÷ 角落速度），分母
 *   是飛機自己的氣動性能，倍率一動分母同步跟著走。`ai-manoeuvre` 五條指標
 *   沒有一條接近門檻，三條還變好。五個旋鈕裡只有 `brakeCornerRatio` 真的
 *   該動（1.6 → 1.8，高能量開局由「200 秒打不完、自己挨 428」變成
 *   「156 秒擊落、毫髮無傷」）。掃描表在 `steer.ts` 各欄位的註解裡，完整
 *   推論見 `docs/superpowers/specs/2026-08-07-ai-energy-recalibration-design.md`。
 *   **下次再調倍率時仍然要回頭看這一項** —— 結論是「這一次沒壞」，不是
 *   「這一層不受影響」。
 * - **目標選擇的 `turnTimeScale`**：`turnTime = 角度 ÷ 瞬間迴旋率`，飛機
 *   轉得快之後「掉頭去追後方」變便宜。實測 `rearShare` 因此由 25.6% 升到
 *   31.6%，`ai-targeting.test.ts` 的門檻已隨之重新定值（見該檔註解）。
 * - **指揮儀的方向舵瞄準輔助**：`betaFadeStart`/`betaMax`（6°/10°）是照
 *   側滑中位 1.44° 訂的；現在中位 6.80°，p10 的輔助權限由 0.94 掉到 0.51。
 *   小角度準星微調會變鈍。實測**不是**舵權不足（`cnDr` ×2 無效），是那個
 *   淡出區間過期了。2026-08-07 試飛後判定手感可接受，暫不處理。
 */
export interface FeelProfile {
  /**
   * 滾轉權限倍率。1 = 史實。
   *
   * 實作方式是縮放 `moments.clDa`（副翼滾轉力矩導數）。滿舵穩態滾轉率
   * `p = (clDa / −clP)·(2V/b)` 對 `clDa` 是線性的，所以倍率就是滾轉率倍率；
   * `FlightDirector.steadyRollRate` 讀的是同一個導數，指揮儀的內部估計會
   * 自動跟上，不會出現「飛機滾得比指揮儀以為的快」的錯配。
   *
   * 【為什麼滾轉是第一個被允許動的】滾轉率不進任何史實斷言的判準
   * （`relative.test.ts` 量的是**兩機的比值**，一起縮放不變），而且滾轉與
   * 能量無關 —— 副翼不吃升力配平。所以它是唯一「動了也還是那台 P-51」的
   * 旋鈕。當時同時記錄了推力不能動的理由：它會同時推動極速、爬升率、升限
   * 與極速峰值高度四項，實測 +20% 讓 P-51 的臨界高度極速由 +3.83% 變成
   * 約 +10%、海平面爬升率由 −14.45% 變成約 0%，四項全部出界。
   *
   * 【那條理由在 2026-08-07 被專案負責人推翻了】見檔案頂端的「非史實」
   * 宣告：現在 `power` 與 `cd0` 都在動，遊戲飛機刻意不再是史實 P-51。
   * 上面那段實測沒有失效 —— 它描述的後果**正是現在接受的代價**。
   */
  roll: number
  /**
   * 誘導阻力的**效率**倍率。1 = 史實。實作方式是縮放 `wing.oswald`，
   * 而 `inducedDragFactor = 1/(π·oswald·AR)`，所以倍率 1.5 = 誘導阻力 ÷1.5。
   *
   * 【為什麼是這一項而不是 cd0 或推力】阻力模型是 `CD = cd0 + k·CL²`，
   * 而兩項各自主宰完全不同的區域。實算 P-51D 的誘導阻力佔總阻力：
   *
   * ```
   *   巡航平飛  CL 0.12    5%
   *   最佳爬升  CL 0.62   59%
   *   硬破防    CL 1.45   89%
   * ```
   *
   * 「拉桿的代價」幾乎**全部**是誘導阻力。三個候選旋鈕的實算對照：
   *
   * ```
   *                        巡航總阻力  爬升總阻力  硬破防總阻力
   *   cd0 砍半（寄生阻力）     −48%       −20%        −6%
   *   oswald ×1.5（本項）      −2%       −19%       −29%
   *   oswald ×2.3             −3%       −33%       −50%
   * ```
   *
   * 砍 `cd0` 對閃躲幾乎無效（−6%）卻會讓極速暴增。提高推力則把 Ps 全域
   * 抬高，極速／爬升率／升限／臨界高度四項一起動，而且同樣沒有特別針對
   * 拉桿的代價。**`oswald` 是唯一同時打中「爬升慢」與「閃躲掉能量」而
   * 極速不動的旋鈕**，所以它是三個之中第一個上線的。
   *
   * 【後來 `power` 與 `cd0` 也上線了，不衝突】上面那段是「單獨動一項」的
   * 比較。`cd0` 單獨**放大**（不是砍半）配上同倍率的 `power` 可以鎖住極速
   * ——那是三項一起用時才出現的自由度，見 `cd0` 的註解。
   *
   * 【它會動到什麼】爬升率與升限（`p51d.ts` 的註解記錄過 oswald 0.75→0.88
   * 讓升限 +400 m、爬升率 +2.8%），以及角落速度與持續轉彎率 —— 後兩者正是
   * 目的。極速由 `cd0` 與推力決定，不受影響。
   *
   * 【為什麼超過 1.0 也合法】`oswald` 在物理上是效率係數，恆 ≤ 1。倍率把它
   * 推過 1 是**明確的遊戲化**，這正是它該待在 `feel.ts` 而不是 `p51d.ts` 的
   * 理由 —— 史實值原封不動，史實測試跑的是沒包裝的 spec。
   */
  oswald: number
  /**
   * 發動機功率倍率。1 = 史實。縮放每一段增壓級的 `powerSeaLevel` 與
   * `powerCritical`，比值不變，所以臨界高度與換檔點的形狀維持原樣。
   *
   * 【它買到什麼】爬升率與**持續**迴旋率 —— 兩者都由比余功率
   * `Ps = V(T−D)/W` 決定，而推力是那條式子裡唯一能整體抬高的項。
   * 下表是**第一輪掃描的原始紀錄**（當時 `oswald` 1.5、`cd0` 尚未存在），
   * 保留是為了顯示「`power` 單獨動時極速跟著跑」這個關係；出貨值不在表內，
   * 它是後來與 `cd0`、`oswald` 一起解出來的（見 `cd0` 註解）：
   *
   * ```
   *          海平面爬升 m/min   4000m 極速 km/h   升限 m   持續迴旋 °/s
   *   ×1.0        955               704          13168        16.1
   *   ×1.2       1199               745          13898        17.6
   *   ×1.4       1447               782          14507        18.8
   *   ×1.6       1696               814          15029        19.9
   * ```
   *
   * 【代價】極速、升限、極速峰值高度一起跑掉 —— 這是推力的本質，
   * 不是實作缺陷。`roll` 的註解記錄過同一件事（+20% 讓四項全部出界）。
   * **瞬間**迴旋率完全不動：那由 CLmax 與結構過載決定，與推力無關。
   *
   * ---
   *
   * # 2026-08-11：出貨值由 1.98 降到 1.572（×0.794），這是「爬升中性」的鑰匙
   *
   * 追加要求是「迴轉半徑 −20%，而且爬升不准動」。這兩件事看似衝突 ——
   * 減重是唯一能動半徑的旋鈕（見 `mass`），而它必然推高爬升。
   *
   * 解法是把兩者拆開：**縮圈靠翼負荷，爬升靠推力，兩者可以各自結清。**
   * 減重 20% 把爬升推到 2533 m/min，再把推力收到 ×0.794 正好還原成 1999。
   *
   * 【為什麼這筆交換幾乎免費】推力對迴轉半徑**近乎無效** —— 這台飛機不是
   * 推力受限的（`mass` 的註解記錄了 `oswald` ×3 只讓半徑 252 → 249 m）。
   * 實測 `power` ×0.7 只讓海平面最佳持續半徑由 168 m 變成 170 m。所以收掉
   * 的推力沒有從半徑那邊扣回去。
   *
   * 【`cd0` 必須同倍率跟著收】否則極速會掉（V_max³ ∝ power/cd0）。
   * 出貨值 `cd0` 1.636 = 2.06 × 0.794，與本項同一個 0.794。
   *
   * 實測對照（P-51D、20v20 三種開局的航跡半徑；包絡值 @4000 m）：
   *
   * ```
   *                          戰鬥中位半徑  爬升 m/min  SL極速  CLmax
   *   改動前                     362 m       1999      584    1.95
   *   mass 0.9（爬升放任）         309 m       2237      584    1.95
   *   本組（mass 0.8 + ×0.794）   291 m       1999      584    1.95   ← 出貨
   *   CLmax ×1.30（另一條爬升中性解）283 m       1999      584    2.53
   * ```
   *
   * 【順帶回收的東西】本檔頂端記錄過一個已知風險：「轉彎變便宜之後
   * `extend` 會失去意義，空戰有可能塌成平面的持續纏鬥」。推力收 20% 把
   * 那件事往回拉了一段 —— 實測 4000 m、20°/s 轉彎的掉速率由「還在加速
   * +2 km/h/s」回到接近打平。**尚未驗證這是否真的改善了 AI 的能量行為**，
   * 因為 `ai-defence` 等測試走的是史實 spec、不經過本層，看不到這一改。
   */
  power: number
  /**
   * 升力曲線斜率倍率。1 = 史實。實作方式是縮放 `lift.clAlpha`。
   *
   * 【為什麼動 `clAlpha` 而不是 `clMax`】`AircraftSpec.clMax` 這個欄位
   * **沒有任何程式碼在讀**（`grep '\.clMax' src/` 除了 `derivedClMax` 之外
   * 為空）。真正生效的是推導值 `derivedClMax = clAlpha·(alphaCrit − alphaZero)`，
   * 所以要動 CLmax 只能動 `clAlpha` 或 `alphaCrit`。取前者是因為 `alphaCrit`
   * 同時是失速混合區（`stallBlend`）的參考點，動它會連帶改變失速的手感曲線。
   *
   * 【它買到什麼】瞬間迴旋率，而且**把角落速度拉下來** —— 後者才是重點。
   * 角落速度是「能拉滿 G 的最低速度」，史實值 159 m/s（572 km/h）高於空戰
   * 的常態速度，於是大部分時間都拉不到極限。實算 @4000 m：
   *
   * ```
   *          失速 m/s   角落速度 m/s      瞬間迴旋 °/s   持續迴旋 °/s
   *   ×1.0      56       159 (572 km/h)      28.1          16.1
   *   ×1.2      51       145 (522 km/h)      30.8          17.0
   *   ×1.3      49       139 (500 km/h)      32.0          17.6
   *   ×1.4      47       134 (482 km/h)      33.2          17.6
   * ```
   *
   * 爬升、極速、升限**完全不動**。
   *
   * 【代價】失速速度跟著下降，飛機變得難以失速 —— 操作的技術門檻降低。
   *
   * 【為什麼不用結構過載上限】`limits.gPositive` 也能買瞬間迴旋，但它讓
   * 角落速度**上升**（×1.25 → 177 m/s、×1.5 → 194 m/s），與「在常態速度
   * 下轉得動」的目的相反。實測後排除。
   */
  lift: number
  /**
   * 戰鬥重量倍率。1 = 史實。縮放 `mass`。
   *
   * 【它買的是迴轉半徑，而且是唯一買得到的】2026-08-11 的實測把三個候選
   * 攤開比過（20v20、三種開局、量的是**航跡半徑**不是包絡值）：
   *
   * ```
   *                          戰鬥中位半徑   爬升 m/min   CLmax
   *   現行                       362 m        1999       1.95
   *   CLmax ×1.33（單獨）         308 m        1999       2.59
   *   軟夾到結構極限（單獨）          322 m        1999       1.95
   *   軟夾 + 本項 0.9             309 m        2237       1.95
   *   軟夾 + 本項 0.8             295 m        2533       1.95
   * ```
   *
   * `power`／`oswald`／`cd0` 三項對半徑**完全無效** —— 這台飛機早就不是
   * 推力受限了（4000 m 的最佳持續半徑 252 m ≈ 最小瞬時半徑 250 m，
   * `oswald` ×3 只讓它變成 249 m）。能動半徑的只有翼負荷與 CLmax 兩者，
   * 而本項動的是前者。
   *
   * 【為什麼選它而不是 `lift`】兩條路縮一樣多的半徑，但只有本項讓**拉桿
   * 變便宜**。同一個轉彎率下需要的過載 n 與重量無關（n 只由速度與轉彎率
   * 決定），而升力 = n·W，誘導阻力正比於升力平方 —— 所以輕的飛機轉同樣
   * 的圈付的阻力少。實測 4000 m、20°/s：
   *
   * ```
   *              350 km/h   450 km/h   550 km/h
   *   現行        +5 km/h/s  +2 km/h/s  −1 km/h/s
   *   本項 0.8   +10 km/h/s  +5 km/h/s  +1 km/h/s
   *   lift 1.5    +5 km/h/s  +2 km/h/s  −1 km/h/s   ← 與現行逐格相同
   * ```
   *
   * `lift` 那一欄每一格都與現行**一模一樣**：加 CLmax 只是讓你能拉得更兇，
   * 拉桿的價錢一分沒少。另外 `lift` 要推到 ×1.5 才有同樣的半徑，那會讓
   * CLmax 到 2.92 —— 比放滿襟翼的客機還高，而 AI 只用得掉其中 65%。
   *
   * 【它會動到什麼】爬升率與升限（0.9 → 爬升 +12%、升限 +434 m）、失速
   * 速度（145 → 137 km/h）。極速**不動** —— 高速平飛的阻力幾乎全是寄生
   * 阻力，與重量無關。兩機平衡也不動（109÷P51 的最小瞬時半徑比
   * 0.961 → 0.962）。
   *
   * 【只縮 `mass`，不縮 `inertia`】兩者在物理上會一起變，但這裡刻意分開：
   * 操縱力矩是氣動的（不隨重量變），慣量若跟著縮，角加速度會平白變快，
   * 等於偷渡一個滾轉／俯仰響應的加成到一個宣稱只動翼負荷的旋鈕裡。
   * 維持慣量原值讓本項的效果**只有**翼負荷，是可以單獨歸因的。
   *
   * 【出貨值 0.8 是「爬升中性」解的一部分，不能單獨讀】0.9 曾短暫出貨過
   * （值 −15%，代價爬升 +12%）。專案負責人的追加要求是「−20%，而且爬升
   * 不准動」，於是 `mass` 降到 0.8、`power` 與 `cd0` 同乘 0.794 把減重送的
   * 餘功率原路還回去。**這三個數字是一組解，動其中任何一個都要重解。**
   * 完整推導見 `power` 的註解。
   *
   * 實測（20v20、三種開局、航跡半徑）：362 → 291 m，−19.6%；
   * 海平面爬升率 1999 m/min 與改動前**逐格相同**。
   */
  mass: number
  /**
   * 寄生阻力倍率。1 = 史實。縮放 `drag.cd0`。
   *
   * 【它單獨存在沒有意義，它的用途是綁住極速】高速平飛時 CL 很小、誘導阻力
   * 可以忽略，於是推力 = 寄生阻力給出
   *
   * ```
   *   P·η/V = ½ρV²S·cd0   →   V_max³ ∝ P / cd0
   * ```
   *
   * **極速只由這個比值決定**。所以 `power` 與 `cd0` 同倍率一起放大時極速
   * 完全不動，而爬升與迴旋照樣受益 —— 因為那兩者發生在低速／高 CL，那裡
   * 誘導阻力才是主角，`cd0` 的權重小得多。實算 P-51D（`lift` 1.3 固定，
   * 每一列的 `cd0` 都是解出來讓 4000 m 極速 = 史實 699 km/h 的值）：
   *
   * ```
   *   oswald  power   cd0    爬升 m/min   持續迴旋 °/s   俯衝極速 km/h
   *     1.5    2.63   2.75      2658          25.0           974
   *     2.0    1.98   2.06      1993          25.0          1001   ← 出貨
   *     2.5    1.58   1.65      1595          25.0          1023
   *     3.0    1.32   1.38      1329          25.0          1042
   *   （史實）  1      1          907          13.4          1079
   * ```
   *
   * 【族內唯一的自由度是「爬升 ↔ 俯衝極速」】四列的極速、失速、角落速度、
   * 瞬間迴旋、持續迴旋**完全相同**。`cd0` 拉得越高，換到的爬升越多，
   * 但俯衝終端速度掉得越多 —— 因為俯衝時沒有 G、CL 極小，阻力幾乎全是寄生。
   *
   * 【要注意的手感副作用】`cd0` ×2 表示收油門或拉 G 之後**掉速快一倍**。
   * 速度會變得非常「油門相依」，能量交換的節奏跟史實不一樣。
   *
   * 【2026-08-11：2.06 → 1.636】不是獨立的決定 —— 它是 `power` 收到
   * ×0.794 之後為了鎖住極速而跟著收的同一個倍率（2.06 × 0.794 = 1.636）。
   * 見 `power` 的註解。這兩個數字必須一起動。
   */
  cd0: number
}

/** 史實輪廓。L2 史實測試恆用這一組 —— 它必須是無操作。 */
export const HISTORICAL: FeelProfile = {
  roll: 1, oswald: 1, power: 1, lift: 1, cd0: 1, mass: 1,
}

/**
 * 遊戲實際使用的輪廓。
 *
 * 【`roll: 1.2` 的由來】人工驗收的手感決定，不是實測回填的數字。二戰活塞
 * 戰機的滾轉率本來就不快（P-51D 約 100°/s @480 km/h），在滑鼠／鍵盤上
 * 操作時反應偏鈍。1.2 讓它到約 120°/s —— 仍在同世代機種的實際區間內
 * （FW-190 約 160°/s），不會變成噴射機。
 *
 * 【`oswald: 1.5` 的由來】專案負責人的判斷：「這個遊戲對能量太過於寫實，
 * 導致遊戲性很差（爬升很慢、閃躲掉能量）」。1.5 是起手值，**待人工試飛
 * 定案**，不是實測回填的數字。
 *
 * 【已知代價，試飛時要一起看】整個 AI 是建在「能量稀缺」上的 —— 五個意圖、
 * `cornerRatio`、`energyAdvantage`、`extendPitchAngle`、卸載層、速度回復閘門，
 * 存在的理由都是「轉彎要付錢」。轉彎變便宜之後 `extend` 會失去意義，空戰
 * 有可能塌成平面的持續纏鬥、垂直機動消失。這是旋鈕不是開關。
 *
 * 【`lift: 1.3` 的由來】`oswald` 1.5 試飛之後的回饋：「爬升速度還是不夠，
 * 迴旋速度也偏慢」。「迴旋慢」的實際原因是角落速度 572 km/h 高於空戰的
 * 常態速度，於是大部分時間都拉不到極限 —— `lift` 把它降到 500 km/h。
 *
 * 【`power` / `cd0` / `oswald` 三個一組解出來的】第二輪回饋：「極速改回跟
 * 史實一樣，其餘維持，持續迴旋看能不能到 25」。三個約束、三個未知數：
 *
 *   1. `power` 與 `cd0` 同倍率 → 極速鎖回史實（見 `cd0` 的註解）
 *   2. `oswald` 與 `power` 一起決定持續迴旋 → 解到 25.0
 *   3. 剩下的自由度是「爬升 ↔ 俯衝極速」→ 取 `oswald` 2.0
 *
 * 取 2.0 的理由是專案負責人兩輪都說爬升不夠：1993 m/min 比上一版再 +38%，
 * 而俯衝終端速度只掉 7%。同樣**待人工試飛定案**。P-51D @4000 m：
 *
 * ```
 *                  現在      上一版    史實
 *   爬升 m/min     1993      1447      907
 *   極速 km/h       699       782      699   ← 鎖回史實
 *   角落速度 m/s    139       139      159
 *   瞬間迴旋 °/s   32.0      32.0     28.1
 *   持續迴旋 °/s   25.0      20.3     13.4
 *   失速 m/s         49        49       56
 *   俯衝極速 km/h  1001      1082     1079
 *   實用升限 m    15369     14507    12198
 * ```
 *
 * 【這台已經不是 P-51 了】爬升率是史實的 2.2 倍、持續迴旋 1.9 倍、
 * 升限 15.4 km。這是明確的取捨，不是失誤 —— 史實值原封不動留在
 * `p51d.ts`，史實測試照樣守著它。
 *
 * 【還沒做的事】AI 的能量層門檻（`brakeCornerRatio`、`unloadMargin`、
 * `speedRecoverMargin`、`extendPitchAngle` 的兩個增益）全是照**舊包絡**
 * 調出來的。持續迴旋率動了 +51% 之後那些值需要重新檢查，否則 AI 會拿著
 * 一組對不上這台飛機的判準飛。
 */
export const GAME_FEEL: FeelProfile = {
  roll: 1.2, oswald: 2, power: 1.572, lift: 1.3, cd0: 1.636, mass: 0.8,
}

/**
 * 把手感輪廓套在一份 spec 上，回傳新的 spec。**不修改傳入的物件。**
 *
 * 倍率為 1 時原封不動回傳**同一個物件** —— 這讓「史實測試跑在未經包裝的
 * spec 上」可以用同一性斷言守住，而不是靠讀者自律。
 */
export function applyFeel(spec: AircraftSpec, feel: FeelProfile): AircraftSpec {
  if (
    feel.roll === 1 && feel.oswald === 1 && feel.power === 1
    && feel.lift === 1 && feel.cd0 === 1 && feel.mass === 1
  ) return spec
  // 【逐項判斷而不是一律複製】沒有被這一項動到的子結構要維持**同一個物件**，
  // 那是「這個倍率只碰它該碰的東西」唯一測得到的形式。
  const moments = feel.roll === 1
    ? spec.moments
    : { ...spec.moments, clDa: spec.moments.clDa * feel.roll }
  const wing = feel.oswald === 1
    ? spec.wing
    : { ...spec.wing, oswald: spec.wing.oswald * feel.oswald }
  const engine = feel.power === 1
    ? spec.engine
    : {
      ...spec.engine,
      gears: spec.engine.gears.map((g) => ({
        ...g,
        powerSeaLevel: g.powerSeaLevel * feel.power,
        powerCritical: g.powerCritical * feel.power,
      })),
    }
  const lift = feel.lift === 1
    ? spec.lift
    : { ...spec.lift, clAlpha: spec.lift.clAlpha * feel.lift }
  const drag = feel.cd0 === 1
    ? spec.drag
    : { ...spec.drag, cd0: spec.drag.cd0 * feel.cd0 }
  // 【只動 mass，inertia 原封不動】見 `FeelProfile.mass` 的註解：
  // 慣量若跟著縮，會在一個宣稱只動翼負荷的旋鈕裡偷渡角加速度加成。
  const mass = spec.mass * feel.mass
  return { ...spec, moments, wing, engine, lift, drag, mass }
}
