import { Vector3 } from 'three'
import { DEG } from '../../core/math'
import { createHull, type AircraftModel, type Blister, type FinParams, type LoftPart } from './assembly'
import type { Break } from './wing'
import type { FrameSpec } from './canopy'
import type { GlassPatch } from './hull'
import { B17G_HULL } from './b17g.hull'
import type { WingParams } from './wing'

/**
 * Boeing B-17G-60-VE Flying Fortress 的程序化外型 —— 真機翼展 31.62 m、
 * 全長 22.66 m、翼面積 131.92 m²、四具 Wright R-1820-97 各 1,200 hp、
 * 三葉槳各 3.53 m。
 *
 * 照片上最能認出它的四件事：
 *   1. **四具發動機**，內外艙各一對，內艙後面收主輪
 *   2. 極大的圓垂尾，配上一路從機腰延伸過來的**背鰭整流罩**
 *   3. 機腹的**球形砲塔**與機首下方的**下巴砲塔**（G 型才有）
 *   4. 又長又細的機身，橫剖面接近圓形（半寬 1.18、半高 1.15）
 *
 * 【全部座標量自參考模型】機身、機翼、四具發動機艙、尾翼、透明件都是同一次
 * 切片、同一個座標系量出來的（坑 16），最後整體平移 +1.1212 換成機體座標
 * （見 `b17g.hull.ts`）。量法見 `.claude/skills/aircraft-from-reference`。
 *
 * ── 這台在量測上比前三台難的地方 ──────────────────────────
 *
 * 機背長滿東西（上部砲塔、無線電艙的開放槍位與頂窗、背鰭整流罩），而且
 * **正上方那條射線與垂尾共面** —— Z 8.85 之後整段回 0。逐片試過也沒有任何
 * 一個 mesh 是完整的機身蒙皮（這個模型按材質分片不是按零件分片）。
 *
 * 解法是把射線原點挪開：後段用 (x 0.35, y 0.90) 那一組，量到的背線從
 * Z 7.0 到 13.0 完全連續而且單調。細節寫在 `b17g.hull.ts`。
 */

/**
 * 主翼。**單一梯形**（不像 He 111 有轉折）——量到的前後緣斜率沿翼展只在
 * 0.12～0.14 之間變，配一條直線就夠。
 *
 * 【量測值，機體座標】沿翼展 79 刀，`uWindow` 限住 Z 排掉螺旋槳與平尾。
 * 外推到 X = 0 用的是**乾淨段 X 2.2…4.0** —— 避開翼根整流罩（X < 2.2 前緣
 * 斜率 0.225，是整流罩不是翼）與內側發動機艙（X 2.40…3.80）：
 *
 * ```
 *   前緣  X 2.2 −1.237、X 4.0 −1.021  斜率 0.1217/m  → X0 −1.5045
 *   後緣  X 2.2  4.310、X 4.0  4.146  斜率 0.0922/m  → X0  4.5131
 *   翼根弦 6.018    翼尖（X 14.4）弦 2.735
 * ```
 *
 * 【交叉驗證：翼面積（坑 16）】這條梯形 (6.018 + 2.413)/2 × 15.81 × 2 =
 * **133.3 m²** 對真機 131.92，**+1.0%**。
 *
 * 注意量測腳本直接梯形積分得到的是 140.2（+6.4%）—— 那含 X < 1.2 的站位，
 * 而那些站位量到的「弦長 7.9、厚弦比 33%」是**機身**不是翼。
 *
 * 【上反角 3.87° 是量到的，不是史實值】中厚線 (vMin+vMax)/2 沿翼展：
 * X 4.4 → 0.199、X 9.0 → 0.509、X 15.0 → 0.915，兩段斜率 0.0674 / 0.0677
 * 完全一致。史實常引 4.5°，差 0.6° —— 依坑 21，這一項量測值自己內部一致
 * 而且有 6 m 的基線，採量測值。
 *
 * 【厚度】翼根 1.155（t/c 19.2%，史實翼型 NACA 0018）、翼尖 0.22
 * （史實 0010 × 弦長 2.413 = 0.241）。量到的逐站厚度由 X 1.4 的 1.072 到
 * X 14.4 的 0.297，斜率 −0.0596/m，兩端外推就是這兩個數。
 */
const WING: WingParams = {
  rootChord: 6.018, tipChord: 2.413, halfSpan: 15.81, rootZ: -1.5045,
  sweep: 7.53 * DEG, dihedral: 3.87 * DEG, rootY: -0.10,
  thickness: 1.155, tipThickness: 0.220,
  /**
   * 【0.45 與 He 111 同一個取捨】量到的翼尖是「撐住弦長到 95% 半翼展才急收」
   * ——X 15.00 弦長還有 2.440，15.40 剩 1.865，15.80 只有 0.246。`tipRound`
   * 只控制收多少、控制不了從哪裡開始收（坑 18），0.45 讓兩端都在 0.3 m 內。
   */
  tipRound: 0.45,
}

/**
 * 水平尾翼。量測值（機體座標）：
 *
 * ```
 *   X 1.0 → 5.8   前緣 10.901 → 12.070（後掠 13.7°）
 *                 後緣 14.065 → 13.743
 *                 弦長  3.163 →  1.674   厚度 0.345 → 0.168
 *   半展 6.500（全展 13.00，真機 13.11，−0.8%）
 * ```
 *
 * 【X < 1.0 要丟】那幾站量到的厚度是 1.87～1.04，那是**機身**不是尾翼。
 *
 * 【rootY 1.26 是量到的】沿 Z 切、`uWindow` 只留單邊平尾，中厚線在內段
 * 1.264、外段 1.253 —— 幾乎不變，所以上反角是 0。
 *
 * 這一項順帶驗了姿態：套上 pitch −1.03° 之後，外段（殘差 RMS 只有 0.006 的
 * 那一把尺）的斜率變成 **+0.04°**，等於歸零。
 */
const TAILPLANE: WingParams = {
  rootChord: 3.475, tipChord: 1.460, halfSpan: 6.50,
  sweep: 13.68 * DEG, dihedral: 0, thickness: 0.382, tipThickness: 0.140,
  rootZ: 10.658, rootY: 1.26, tipRound: 0.35,
}

/**
 * 垂尾 —— **一片**，用七個展向轉折配那條凹前緣。
 *
 * ══ 第一版：整片是照一條天線鋼索造的 ═══════════════════
 *
 * 參考模型有一個 mesh `Object_36`：**32 個三角形、透明材質、Y −1.13…5.06、
 * Z −5.27…11.42**，那是從垂尾頂拉到機身前段的兩條天線鋼索。`extent` 切片
 * 取的是**極值**，鋼索比垂尾前緣更靠前，所以沿 Y 切的每一層量到的「前緣」
 * 都是鋼索。症狀是那張表**完美的線性**：
 *
 * ```
 *   Y      2.40   2.52   2.65   2.77   2.90   3.02
 *   前緣   3.827  4.172  4.517  4.862  5.207  5.552   每一步剛好 0.345
 * ```
 *
 * **上一輪驗收報的「垂尾前緣中位差 0.004」是假的**：參考那邊量到鋼索、自家
 * 那邊量到照鋼索造的垂尾。對切只能證明兩邊一致，證明不了兩邊都對。
 *
 * ══ 第二版：七片獨立的面板 → 一疊有橫向接縫的階梯 ═══════
 *
 * 前緣配對了，但**每一片 `upright` 都是一個封閉的實體**：相鄰兩片的翼尖
 * 端面與翼根端面完全重合，而且各自的厚度分佈獨立。人工回報「垂直尾翼好像
 * 沒有很平整」就是那七道接縫。
 *
 * 第三版改成**單一面板 + 七個轉折**（`WingParams.breaks`）：一個 mesh、
 * 沒有內部端面、厚度沿展向連續。
 *
 * ══ 量測來源：兩組互相印證 ═══════════════════════════
 *
 * **前緣**（Y ≥ 2.50）沿 Y 切、排掉 `Object_36`／`Object_6`。
 * **背鰭整流罩**（Y < 2.50）改讀側視上緣線 —— 沿 Z 切、只留中線那條 X 帶
 * 的 `vMax`。整流罩是機背上唯一的東西，所以那條線就是它。
 *
 * 兩組在交界處互相印證：上緣線說機體 z 10.40 的最高點是 2.576，平面形說
 * Y 2.50 的前緣在 10.104、Y 2.60 在 10.475 —— 內插得 z 10.40 對應 Y 2.58。
 * 兩個獨立的量法差 0.004。
 *
 * ```
 *   Y      1.693  1.764  1.915  2.066  2.216  2.377  ← 上緣線（整流罩）
 *   前緣    4.90   5.40   6.40   7.40   8.40   9.40     dz/dy ≈ 6.5，很線性
 *
 *   Y      2.50    2.60    3.00    4.00    5.00    5.40    5.60  ← 平面形
 *   前緣  10.104  10.474  11.172  12.073  12.552  12.865  13.216
 *   後緣  15.322  15.301  15.216  15.021  14.716  14.363  14.001
 *   厚度   0.468   0.458   0.419   0.322   0.247   0.151   0.084
 * ```
 *
 * 頂點在機體 z 13.60、Y 5.670。前緣是**凹的**：Y 2.50→2.60 就走了 0.372，
 * 而 4.50→5.00 只走 0.24。轉折取在曲率變化處，每一段的中段誤差 < 0.05。
 *
 * ── `rootY` 1.10 必須低於**整段**機尾背線（坑 17）──────────
 *
 * 這一片的下緣是一條 z 1.045…15.34 的直線，必須整條埋在機身裡。重烘之後的
 * 機身背線在 z 15.30 只剩 1.180，所以 rootY 只能取 1.10。它自然從 z 4.90
 * 冒出來 —— 那一站的背線正好 1.693 ＝ 前緣在那個高度的位置。
 *
 * 【厚度沿展向線性，配的是可見段】量到的厚度不是線性的（Y 5.00 是 0.247，
 * 而 1.10 → 5.67 的直線在那裡只有 0.195）。取 (1.10, 0.621) → (5.67, 0.122)
 * 這條直線去配 Y 2.5…5.4 那一段，最大差 0.05。翼根那 0.621 是埋著的。
 */
const FIN_TOP = 5.670
const FIN_ROOT_Y = 1.10
const FIN_H = FIN_TOP - FIN_ROOT_Y
/** 量到的 (Y, 前緣, 後緣) → 展向比例、弦長、前緣 */
const finAt = (y: number, lead: number, trail: number): Break => ({
  at: (y - FIN_ROOT_Y) / FIN_H, chord: trail - lead, lead,
})
const FIN: FinParams = {
  chordRoot: 14.295, chordTip: 0.250, height: FIN_H,
  // 最後一段（Y 5.60 → 頂點）的前緣：13.216 → 13.340
  sweep: 62.4 * DEG, z: 1.045, rootY: FIN_ROOT_Y,
  breaks: [
    finAt(2.50, 10.104, 15.322),
    finAt(2.60, 10.474, 15.301),
    finAt(3.00, 11.172, 15.216),
    finAt(4.00, 12.073, 15.021),
    finAt(5.00, 12.552, 14.716),
    finAt(5.40, 12.865, 14.363),
    finAt(5.60, 13.216, 14.001),
  ],
}

/**
 * 四具發動機艙。**沿 Z 每 0.1 m 切、`uWindow` 只留該艙那一條翼展帶**量出來
 * 的。這是第二版，第一版取樣 0.4 m 而且半寬那一欄是假的。
 *
 * 【第一版錯在哪】兩條，都是坑 6／坑 30 的同一件事：
 *
 * **（甲）0.4 m 取樣配不上 0.1 m 級的特徵。** 整流罩唇口、輪艙鼓包的前後
 * 端都在 0.1～0.2 這一級，粗取樣量出來是波浪，配出來自然是波浪（坑 30）。
 * 0.1 m 重取樣之後頂線是 0.740 → 0.744 → 0.747 → 0.750 → 0.751 → 0.753，
 * 乾淨到不像話。
 *
 * **（乙）`uWindow` 開太窄，半寬變成窗口自己的寬度。** 第一版的窗是
 * [2.6, 3.6]，而艙比那還寬 —— 量到的「X 幅度」整段是 0.95～1.00，那是
 * 窗的寬度不是艙的寬度。窗放到 [2.0, 4.3] 之後真值出來了：
 *
 * ```
 *   機體Z   X最左   X最右   寬度    艙心    半寬
 *   -2.88   2.286   3.814   1.528   3.050   0.764
 *   -2.08   2.265   3.835   1.569   3.050   0.785
 *   -1.28   2.309   3.792   1.483   3.050   0.742
 * ```
 *
 * 所以**艙心在 x 3.050 不是 3.10、最大半寬 0.785 不是 0.70**。外艙同理：
 * 乾淨段量到 X 5.805…7.337，艙心 **6.571**、半寬 0.766。
 *
 * 交叉驗證：Wright R-1820 的整流罩直徑約 1.4 m，量到的 1.53～1.57 含艙身
 * 側面的整流，對得上。
 *
 * ── 量不到的那一段（坑 15）──────────────────────────────
 *
 * 半寬只量得到機翼前緣進來之前（內艙機體 z ≤ −1.28）加上射線還乾淨的一小段
 * （−1.18…−0.38，原點 x 3.10 的 −X 那條，減 0.05 回到艙心；它在 −1.28 與
 * 窗量到的 0.741 對得上，所以可信）。再往後**射線一出艙就沿著機翼跑**，
 * 量不到 —— 那一段的半寬照半高的收縮率外推到艙尾的 0.300。
 *
 * 【兩種量法的接縫要抹平】兩段各自可信，接起來卻是一階：−1.38 的 0.750 到
 * −1.18 的 0.653 是 0.097，0.1 m 之內掉 13%。真的艙不會這樣，那是量法換了。
 * −1.18／−0.98 改成由 −1.38（0.750）到 −0.78（0.642）內插。專案負責人：
 * 「引擎好像還是有一點鼓包，是不是可以更平順?」
 *
 * 這一段本來就大半被機翼蓋住，而上下輪廓（`halfHeight`／`centerY`）全程是
 * 量到的。
 *
 * ── 內外艙不是同一個形狀 ────────────────────────────────
 *
 * 外艙整個高 0.25（機翼上反角）。內艙的差別只有一個：主輪收在它裡面，所以
 * 尾段略粗一點 —— **但不是原本以為的那塊鼓包**，見下。
 *
 * ── 那塊「輪艙鼓包」是放下來的主輪（坑 15）───────────────
 *
 * 第一版量到內艙腹線在機體 z −1.8…−0.6 掉到 −1.15、其餘各站 −0.78 上下，
 * 把那 0.37 當成輪艙的鼓包烘了進去。專案負責人：「引擎好像也不對，下方有
 * 奇怪的突出」。
 *
 * **那台參考模型的起落架是放下來的。** 逐條 X 帶（相對艙心 ±0.06 一格）重量
 * 一次就分得出來 —— 鼓包會是對稱而且連續的，這一塊兩者都不是：
 *
 * ```
 *   機體Z   −0.24   −0.12    0.00   +0.12   +0.24   +0.36   +0.48
 *   −2.00  −0.782  −0.799  −0.804  −0.799  −0.768  −0.741  −0.672   ← 乾淨
 *   −1.60  −0.223  −0.899  −1.023  −1.060  −1.055  −1.015  −0.866
 *   −1.40  −0.072  −1.001  −1.099  −1.137  −1.133  −1.101  −0.950
 *   −0.60  −0.745  −0.723  −0.691  −0.751  −0.746  −0.668  −0.580   ← 乾淨
 * ```
 *
 * 低點偏在**外側**（+0.12 最低，不是 0.00），而 −0.24 那一條同時往上跑到
 * −0.07 —— 射線從艙門的開口射進了空的輪艙。對稱的鼓包不會這樣。
 *
 * 所以 z −1.78…−0.78 的腹線改用趨勢值：由 −1.98 的 −0.804 直線接到 −0.58
 * 的 −0.747。頂線不受污染（量到的 0.767…0.795 幾乎是定值），照量測值留著，
 * `centerY`／`halfHeight` 由「頂線、腹線」兩條反推。
 *
 * 外艙前端 −2.78 之前被螺旋槳蓋住（量到 z −2.78/−2.68 的高度 2.4、寬度
 * 2.2，那是槳葉），唇口那一環按內艙量到的比例 0.84 補。
 */
const NAC_X_INNER = 3.050
const NAC_X_OUTER = 6.571

const NACELLE_INNER: LoftPart = {
  roundness: 2.3,
  segments: 16,
  caps: { front: false },
  sections: [
    { z: -3.18, halfWidth: 0.648, halfHeight: 0.643, centerY: +0.002 },
    { z: -2.98, halfWidth: 0.760, halfHeight: 0.760, centerY: -0.021 },
    { z: -2.78, halfWidth: 0.765, halfHeight: 0.765, centerY: -0.018 },
    { z: -2.58, halfWidth: 0.767, halfHeight: 0.766, centerY: -0.016 },
    { z: -2.38, halfWidth: 0.769, halfHeight: 0.770, centerY: -0.008 },
    { z: -2.18, halfWidth: 0.784, halfHeight: 0.792, centerY: -0.007 },
    { z: -1.98, halfWidth: 0.782, halfHeight: 0.798, centerY: -0.006 },
    { z: -1.78, halfWidth: 0.775, halfHeight: 0.795, centerY: -0.000 },
    { z: -1.58, halfWidth: 0.766, halfHeight: 0.791, centerY: +0.003 },
    { z: -1.38, halfWidth: 0.750, halfHeight: 0.786, centerY: +0.007 },
    // 半寬在這裡是兩種量法的接縫，原值 0.653／0.647 對前一站的 0.750 是一階
    // 0.097 的落差 —— 那是量法換了不是形狀變了，改成兩端內插（見檔頭）
    { z: -1.18, halfWidth: 0.714, halfHeight: 0.780, centerY: +0.009 },
    { z: -0.98, halfWidth: 0.678, halfHeight: 0.774, centerY: +0.011 },
    { z: -0.78, halfWidth: 0.642, halfHeight: 0.766, centerY: +0.011 },
    { z: -0.58, halfWidth: 0.636, halfHeight: 0.757, centerY: +0.010 },
    { z: -0.38, halfWidth: 0.630, halfHeight: 0.747, centerY: +0.008 },
    { z: -0.18, halfWidth: 0.617, halfHeight: 0.731, centerY: +0.006 },
    { z: 0.02, halfWidth: 0.603, halfHeight: 0.714, centerY: -0.001 },
    { z: 0.22, halfWidth: 0.593, halfHeight: 0.702, centerY: -0.017 },
    { z: 0.42, halfWidth: 0.563, halfHeight: 0.664, centerY: -0.013 },
    { z: 0.62, halfWidth: 0.537, halfHeight: 0.632, centerY: -0.010 },
    { z: 0.82, halfWidth: 0.528, halfHeight: 0.620, centerY: -0.017 },
    { z: 1.02, halfWidth: 0.509, halfHeight: 0.598, centerY: -0.012 },
    { z: 1.22, halfWidth: 0.483, halfHeight: 0.565, centerY: -0.005 },
    { z: 1.42, halfWidth: 0.457, halfHeight: 0.533, centerY: -0.003 },
    { z: 1.62, halfWidth: 0.418, halfHeight: 0.484, centerY: +0.016 },
    { z: 1.82, halfWidth: 0.382, halfHeight: 0.441, centerY: +0.029 },
    { z: 2.02, halfWidth: 0.360, halfHeight: 0.412, centerY: +0.023 },
    { z: 2.22, halfWidth: 0.338, halfHeight: 0.386, centerY: +0.009 },
    { z: 2.42, halfWidth: 0.321, halfHeight: 0.364, centerY: -0.013 },
    { z: 2.62, halfWidth: 0.300, halfHeight: 0.339, centerY: -0.032 },
  ],
}

const NACELLE_OUTER: LoftPart = {
  roundness: 2.3,
  segments: 16,
  caps: { front: false },
  sections: [
    { z: -2.78, halfWidth: 0.643, halfHeight: 0.549, centerY: +0.257 },
    { z: -2.58, halfWidth: 0.678, halfHeight: 0.654, centerY: +0.253 },
    { z: -2.38, halfWidth: 0.735, halfHeight: 0.735, centerY: +0.246 },
    { z: -2.18, halfWidth: 0.764, halfHeight: 0.764, centerY: +0.244 },
    { z: -1.98, halfWidth: 0.766, halfHeight: 0.768, centerY: +0.247 },
    { z: -1.78, halfWidth: 0.771, halfHeight: 0.777, centerY: +0.248 },
    { z: -1.58, halfWidth: 0.781, halfHeight: 0.790, centerY: +0.248 },
    { z: -1.38, halfWidth: 0.784, halfHeight: 0.790, centerY: +0.253 },
    { z: -1.18, halfWidth: 0.777, halfHeight: 0.781, centerY: +0.259 },
    { z: -0.98, halfWidth: 0.769, halfHeight: 0.773, centerY: +0.261 },
    { z: -0.78, halfWidth: 0.754, halfHeight: 0.758, centerY: +0.266 },
    { z: -0.58, halfWidth: 0.739, halfHeight: 0.743, centerY: +0.268 },
    { z: -0.38, halfWidth: 0.737, halfHeight: 0.740, centerY: +0.252 },
    { z: -0.18, halfWidth: 0.732, halfHeight: 0.735, centerY: +0.231 },
    { z: 0.02, halfWidth: 0.699, halfHeight: 0.701, centerY: +0.231 },
    { z: 0.22, halfWidth: 0.635, halfHeight: 0.634, centerY: +0.256 },
    { z: 0.42, halfWidth: 0.574, halfHeight: 0.571, centerY: +0.272 },
    { z: 0.62, halfWidth: 0.527, halfHeight: 0.522, centerY: +0.272 },
    { z: 0.82, halfWidth: 0.482, halfHeight: 0.475, centerY: +0.278 },
    { z: 1.02, halfWidth: 0.441, halfHeight: 0.433, centerY: +0.285 },
    { z: 1.22, halfWidth: 0.414, halfHeight: 0.405, centerY: +0.288 },
    { z: 1.42, halfWidth: 0.401, halfHeight: 0.391, centerY: +0.292 },
    { z: 1.62, halfWidth: 0.383, halfHeight: 0.373, centerY: +0.289 },
    { z: 1.82, halfWidth: 0.360, halfHeight: 0.348, centerY: +0.279 },
    { z: 2.02, halfWidth: 0.340, halfHeight: 0.327, centerY: +0.267 },
    { z: 2.22, halfWidth: 0.324, halfHeight: 0.311, centerY: +0.252 },
    { z: 2.42, halfWidth: 0.307, halfHeight: 0.293, centerY: +0.230 },
    { z: 2.62, halfWidth: 0.280, halfHeight: 0.265, centerY: +0.202 },
  ],
}

/**
 * ── 尾艙罩：**往前延伸到機背**，前段金屬、後段玻璃 ────────
 *
 * 專案負責人：「機尾上方應該是一個高的圓角矩形凸起 …… 他其實是向前延伸往
 * 機背吧?」
 *
 * 【罩子的範圍：看頂線；玻璃的範圍：看透明 mesh】兩者不一樣，要分開量。
 *
 * **頂線**（x 0.32 那一條帶 —— 垂尾在 y 1.7 的半厚是 0.28，這一條剛好在它
 * 外面）：
 *
 * ```
 *   機體Z  13.0  13.7  14.3  14.9  15.0  15.3  15.6  15.8  15.9
 *   頂線   1.564 1.526 1.540 1.657 1.699 1.687 1.664 1.483 1.398
 *   尾樑頂 1.545 1.526 1.512 1.497 1.493 1.481 1.470 1.430 1.373
 * ```
 *
 * 13.7 之前頂線就是尾樑自己，之後**一路抬到 1.70**（尾樑只有 1.48）——
 * 罩子由機體 13.9 起，不是 14.7。
 *
 * **玻璃**（只打 `Object_50`）：
 *
 * ```
 *   機體Z   14.80  14.90  15.30  15.80  15.90  16.00  16.10
 *   x0.24     —    1.72…1.87  1.77…1.91  1.78…1.91  1.70  1.39   —
 * ```
 *
 * 玻璃只有 **14.90…16.00**。所以罩子的前段（13.9…14.9，鑽進垂尾的前緣
 * 整流罩裡）是金屬，後段才是射手的玻璃艙。
 *
 * 【前段是一道**長而低的斜坡**，不是一個立起來的方塊】逐帶量「參考頂線 −
 * 自家頂線」（x0.32 那一條，剛好在垂尾的半厚之外）：
 *
 * ```
 *   機體Z  10.0…13.7  13.9   14.2   14.4   14.8   15.0…15.7  15.8
 *   落差    +0.02…0.05 +0.08  +0.10  +0.21  +0.24  +0.31      收掉
 * ```
 *
 * 13.7 之前那 0.02～0.05 是背線本來就有的偏差，不是凸起；13.8 開始爬，
 * **15.0 才滿高**。第一版把斜坡壓在 13.85…14.60（0.75 m）而且前段只有
 * 0.22 寬，看起來就是「一個矮胖的方塊立在尾樑上」，而不是由機背一路長上來
 * 的整流。專案負責人：「我看他是向前延伸將近半個垂直尾翼耶」。現在斜坡拉到
 * 13.80…14.92（1.1 m），半寬一開始就給 0.16 → 0.30。
 *
 * 【剖面：圓角矩形】`roundness` 3.0、半寬 0.33、頂 1.97。半寬取 0.33 是由
 * x0.32 那一條帶反推的 —— 0.35 配 3.2 在那裡會做到 1.84，量到的是 1.70。
 *
 * 【為什麼不烘進環裡】它比尾樑高 0.5、只有 0.66 寬 —— 烘進去等於把整圈撐胖
 * （與發動機艙、機腹吊艙同一個判準，見 skill 第 5b 步）。
 */
const HOOD_FWD: LoftPart = {
  roundness: 3.0,
  segments: 16,
  sections: [
    { z: 13.80, halfWidth: 0.16, halfHeight: 0.10, centerY: 1.44 },
    { z: 14.35, halfWidth: 0.30, halfHeight: 0.16, centerY: 1.47 },
    { z: 14.80, halfWidth: 0.33, halfHeight: 0.26, centerY: 1.55 },
    { z: 14.92, halfWidth: 0.33, halfHeight: 0.31, centerY: 1.60 },
  ],
}

/**
 * 射手的玻璃艙。
 *
 * 【兩段都封口，而且**疊 0.02**】原本是「前段不封後、後段不封前」，兩個
 * 開口的殼在 z ≈ 15 —— `geometry.test.ts` 的帶符號體積是對原點取的，開口
 * 的殼在那裡是一個很大的負數（見 `fuselage.ts` 封口那一段的註解）。
 *
 * 兩段各自封口、玻璃的首站往前挪 0.02 疊進金屬裡：玻璃的前封蓋埋在金屬
 * 之內看不到，金屬的後封蓋透過玻璃看得到 —— 那正是射手前面的隔框。
 */
const HOOD_GLASS: LoftPart = {
  roundness: 3.0,
  segments: 16,
  sections: [
    { z: 14.90, halfWidth: 0.33, halfHeight: 0.30, centerY: 1.60 },
    { z: 15.10, halfWidth: 0.33, halfHeight: 0.34, centerY: 1.61 },
    { z: 15.30, halfWidth: 0.33, halfHeight: 0.36, centerY: 1.61 },
    { z: 15.70, halfWidth: 0.33, halfHeight: 0.36, centerY: 1.61 },
    { z: 15.88, halfWidth: 0.29, halfHeight: 0.33, centerY: 1.61 },
    { z: 15.98, halfWidth: 0.17, halfHeight: 0.20, centerY: 1.55 },
  ],
}

/**
 * 玻璃艙裡的暗艙。**不能省** —— 45% 的玻璃後面沒有東西擋的話，從一側看穿到
 * 另一側就是天空（坑 24）。
 *
 * 比罩子小一號、而且**前後各縮進去一節**：尾端與罩子同站的話，由正後方看是
 * 「玻璃的封蓋 + 暗殼的封蓋」兩個同心的圓角矩形，中間那一圈黑邊看起來像一個
 * 洞。前端也縮，讓 `HOOD_FWD` 的金屬把它擋住。
 */
const HOOD_DARK: LoftPart = {
  roundness: 3.0,
  segments: 16,
  sections: [
    { z: 14.96, halfWidth: 0.26, halfHeight: 0.24, centerY: 1.60 },
    { z: 15.30, halfWidth: 0.28, halfHeight: 0.31, centerY: 1.61 },
    { z: 15.70, halfWidth: 0.28, halfHeight: 0.31, centerY: 1.61 },
    { z: 15.86, halfWidth: 0.23, halfHeight: 0.26, centerY: 1.60 },
  ],
}

/**
 * 三座砲塔。**位置與尺寸都是量到的**，不是照片讀的（坑 22）：
 *
 * ```
 *   下巴（Bendix）  Z −5.48…−4.48  腹線量到 −1.056（旁邊各站是 −0.60）
 *   上部（Sperry）  Z −1.68…−0.88  背線量到  2.372（旁邊各站是  1.56）
 *   球形（Sperry）  Z  4.52… 5.72  腹線量到 −1.263（旁邊各站是 −0.65）
 * ```
 *
 * 尾砲塔（Cheyenne）在機體 Z 13.97 之後，那一段機身環整圈打不到 —— 由
 * `b17g.hull.ts` 最後五環的手工尾錐代表，不另外貼球。
 */
const TURRETS: readonly Blister[] = [
  /**
   * 【導航員的圓頂】機身那一層特意把它丟掉（`b17g.hull.ts` 的背線可信區間
   * 排掉量測 −6.18…−5.28），因為留在裡面等於把它抹進整條蒙皮。量到的是
   * 機體 z −5.00…−4.20 有一塊**只在中線附近**的凸起：中線那條 X 帶讀
   * 1.574，而 x 0.3～0.5 的帶同一站只有 1.18～1.38 —— 高出蒙皮 0.36、
   * 寬度不到 0.6、長 0.8。
   */
  { x: 0, y: 1.30, z: -4.60, width: 0.52, height: 0.56, length: 0.85,
    round: true, bodyColor: true },
  { x: 0, y: -0.55, z: -4.98, width: 0.95, height: 1.00, length: 1.05, round: true },
  { x: 0, y: 1.95, z: -1.28, width: 0.90, height: 0.85, length: 0.90, round: true },
  { x: 0, y: -0.68, z: 5.12, width: 1.10, height: 1.15, length: 1.10, round: true },
]

/**
 * 玻璃。**這張表是量的不是讀照片的**（坑 22）——同一組射線打兩次，一次不
 * 過濾、一次只打材質透明的那一片（`Object_50`），兩者相等就是那一格最外面
 * 是玻璃。機體座標：
 *
 * ```
 *   −6.16…−5.66  整圈       投彈手的機首罩 → `GLASS_SPLIT_Z`
 *   −5.28…−5.03  正上與 30°  領航員的頰窗
 *   −4.53…−4.16  正上與 30°  天文導航罩那一帶
 *    2.72… 3.72  只有正上    無線電艙的頂窗
 *    8.22        只有 150°   腰部槍窗（左右**交錯**，B-17G 本來就是）
 *   15.72        只有正上    尾槍手
 * ```
 *
 * ── ⚠ 座艙玻璃是判斷，不是量到的 ────────────────────────
 *
 * 機體 Z −3.4…−2.0（座艙）整段量到「**連凹進去的玻璃都沒有**」——把判準
 * 從「玻璃在最外」放寬成「有沒有打到玻璃」重跑，那一段仍然全是蒙皮。
 * 這台參考模型的座艙玻璃不是獨立的透明 mesh。
 *
 * 所以 `COCKPIT` 那一片的範圍是**人工定的**，錨在量到的機身站位上。這一條
 * 照實記著：He 111 的機腹就是敗在「從照片讀來的前提沒有進過任何量測表」，
 * 六次修正每一次都合理、每一次都不對（坑 22）。這裡至少知道自己在猜。
 */
const GLASS_SPLIT_Z = -5.66
const GLASS_PATCHES: readonly GlassPatch[] = [
  /**
   * 【領航員的頰窗】0.125 m 細取樣的表說機體 −5.28…−5.03 與 −4.53…−4.16
   * 有玻璃，而且只在 **90° 與 30°** 這兩個方向 —— 60° 與 0° 整段是蒙皮。
   *
   * 第一版寫成 `i0: 0, i1: 4`（90° 一路到 42° 連續），於是機首上方整片變成
   * 一大塊圓弧玻璃，而參考模型那裡是**光蒙皮加一個小圓頂**。這是坑 15 的
   * 反面：量到的是「兩個孤立的方向」，照抄成連續區間就是憑空補了中間那段。
   *
   * 90° 那一格由 `TURRETS` 裡的圓頂代表（機體 −4.60），這裡只留頰窗。
   */
  { from: -5.35, to: -4.10, i0: 6, i1: 8, recess: true },
  /**
   * 【風擋不能開 `recess`】`recess` 把整片往內沉到半徑的 0.94，而風擋在中線
   * 上 —— 沉下去之後**最外面的那一層就是玻璃**，逐站對切量到的「背線」因此
   * 整段低 0.09（機體 −2.98…−2.58 的 1.849 對真值 1.931）。機身環本身是對的，
   * 錯的是蓋在上面的東西。真機的風擋本來就與蒙皮齊平。
   *
   * 側窗與腰部槍窗保留 `recess` —— 那兩處真的是嵌進框裡的。
   */
  /**
   * ── 俯視要是一個向前的尖角 ──────────────────────────
   *
   * `GlassPatch` 是「z 範圍 × 環索引範圍」的**矩形**，貼出來是一條橫的方帶。
   * 而參考模型從正上方看，風擋是一個向前的尖角 —— 機殼的稜已經做出來了
   * （見 `b17-ref.measure.ts` 的 `ringOf`），玻璃卻還是方的，於是那個角在
   * 算圖上看不見。人工回報連續三輪都指這裡。
   *
   * 【玻璃的形狀由**機殼**決定，補丁只是換材質】
   *
   * `GlassPatch` 是「z 範圍 × 環索引範圍」的矩形，做不出斜的邊界 —— 試過拆
   * 成三片、也試過加一個 `sweep` 欄位；前者的暗色襯裡逐片取弦會裂開，後者的
   * 邊界只能落在四邊形的格線上，斜邊是四階的階梯。兩種都比方帶醜，都退掉了。
   *
   * 現在風擋是**烘焙時就先訂成一個四邊形**的：四個角由參考模型的玻璃 mesh
   * 量出來，那一段的站位加密到 0.06，索引 1 與 2 分別坐在四邊形的上緣與前緣
   * 上（見 `b17-ref.measure.ts`）。所以這裡只要指名那一格。
   *
   * 【z 範圍收在「四邊形完整成形」的那一段】頭尾兩個角是銳角楔形，那幾站
   * 索引 1、2 會擠在同一個 x 上，所以烘焙時用漸變讓它們留在自然位置 ——
   * 那幾格不是玻璃。收成 −3.17…−2.70 之後，貼出來的四個角剛好落在四邊形
   * 自己的兩條緣線上，是一個**真正的四邊形**，只是頭尾各修掉一個銳角。
   *
   * 中線那一格（`i0-1`）留成金屬 —— 真機的風擋本來就有一根中框，而且這樣
   * 俯視才看得出是**兩片**。
   */
  /**
   * 【風擋的形狀由 `WINDSCREEN` 那四個角決定，這兩片只負責**把蒙皮挖掉**】
   * 沒有它們的話，四個角那片玻璃底下是金屬，45% 的玻璃透出來一片死白。
   *
   * 兩片的 z 範圍是逐站驗過的：那幾格**整格落在四邊形之內**，所以它們參差
   * 的邊界會被上面那片四邊形完整蓋住，看不到。
   */
  /**
   * 【範圍是逐帶驗出來的，不是估的】改成挖洞之後，挖到四邊形外面就是一個
   * 看穿到天空的破口。逐「站位對」量四個角離四邊形多遠：
   *
   * ```
   *   帶（機體 z）        索引 1…2        索引 2…3
   *   −3.26…−3.20          −0.178          −0.239
   *   −3.20…−3.14          −0.074          −0.126   ← 漸變帶，索引還沒走到緣上
   *   −3.14…−3.08          +0.030          −0.026
   *   −3.08…−3.02          +0.011           0
   *   −3.02…−2.96          −0.056           0       ← 索引 1 上了艙頂的簷
   *   −2.60…−2.54            —             −0.193   ← 四邊形結束
   * ```
   *
   * 索引 2、3 在 −3.08…−2.60 這一段**正好坐在四邊形的上緣與外緣上**（烘焙
   * 時就是照 `paneSpan` 擺的），所以那一段整段可挖。索引 1 只有前面兩帶在
   * 裡面 —— 再往後它是艙頂的簷，在玻璃之上。
   */
  { from: -3.20, to: -3.04, i0: 1, i1: 2, hole: true, bare: true },
  { from: -3.19, to: -2.60, i0: 2, i1: 3, hole: true, bare: true },
  // 座艙側窗：在方盒**之下**的圓管肩上，不是盒壁
  { from: -2.40, to: -2.05, i0: 5, i1: 6, recess: true },
  /**
   * ── 機背的**兩片**頂窗：這裡只挖蒙皮 ──────────────────────
   *
   * 舊版是一條 `2.47…4.34 × 索引 0…3` 的方帶，三件事都錯：
   *
   * ```
   *   數量  參考模型在 2.80…3.23 之間**是金屬**，那是兩片窗不是一片
   *   寬度  索引 3 在那一段是 x 0.50…0.55，而量到的玻璃只到 x 0.44
   *   收尾  後窗的尾端是收尖的，方帶做出來是一刀切齊
   * ```
   *
   * 形狀改由 `ROOF_PANES` 訂死（量測表寫在那裡），這兩片補丁退回**只負責
   * 把蒙皮挖掉**，`bare` 連襯裡都不生 —— 暗艙由 `h.bowl` 生。
   *
   * 【挖到索引 2 為止，只有尾段退回索引 1】專案負責人：「黑碗也是要連接在
   * 玻璃邊框」—— 只挖到索引 1（x 0.265…0.312）的話，0.92 m 寬的窗子兩側各
   * 留 0.15 的蒙皮，碗離玻璃的邊還有一大段。索引 2 是 x 0.386…0.427，對上
   * 外框的 0.44～0.46 只差 3～5 cm，那才是「框」該有的寬度。
   *
   * 後窗尾端（4.11 之後）外框收到 0.30 以下，索引 2 就超出去了，那一段退回
   * 索引 1。只有落在外框**之內**的格子才能挖，挖到外面就是看穿到天空的洞。
   *
   * 【z 的兩端要落在**站位之間**】補丁是逐「站位對」判斷的（`patchAt` 吃的
   * 是兩站的中點），逐帶量餘裕之後可挖的是：
   *
   * ```
   *   前窗  2.5354…2.7101  索引 0…2  +0.047
   *   後窗  3.4093…3.9336  索引 0…2  +0.048 +0.043 +0.008
   *         3.9336…4.1084  索引 0…1  +0.039（索引 2 在這裡是 −0.058）
   * ```
   *
   * 兩端的帶都是負的：前窗 ±0.11、後窗前端 −0.039（3.2345 那一站外框只有
   * 0.21 而索引 2 在 0.401）、後端 −0.064。那幾格留成蒙皮就是艙口的前後框。
   *
   * 剩下的那一圈蒙皮就是艙口的框，真機那裡本來就是。
   */
  { from: 2.55, to: 2.70, i0: 0, i1: 2, hole: true, bare: true },
  { from: 3.45, to: 3.90, i0: 0, i1: 2, hole: true, bare: true },
  { from: 3.95, to: 4.10, i0: 0, i1: 1, hole: true, bare: true },
  /**
   * 腰部與無線電艙的側窗。細取樣量到三段，左右**交錯**（B-17G 本來就是）：
   * 機體 5.97…6.59（30°）、7.34…8.22（150°）、9.34…9.59（30°）。
   * 這一檔的環是左右鏡像的，所以三段都做成同一側的 z（坑 20b：寧可少）。
   */
  { from: 5.97, to: 6.59, i0: 6, i1: 8, recess: true },
  { from: 7.34, to: 8.22, i0: 6, i1: 8, recess: true },
  { from: 9.34, to: 9.59, i0: 6, i1: 8, recess: true },
]

/**
 * 機首罩的結構條。B-17G 的投彈手罩是分格的，沒有骨架的話那一段從外面看
 * 就是一顆光滑的玻璃球。
 *
 * 【材質是雙面的】窄帶只有一個朝向，全玻璃的罩子看得到對側骨架的**背面**
 * ——不加雙面材質那幾條從背面看整個消失（坑 25）。`h.frames` 自己用
 * `bothSides`。
 */
const FRAMES: FrameSpec = {
  hoops: [-6.00, -5.83, GLASS_SPLIT_Z],
  rails: [0, 6, 10],
  from: -6.03, to: GLASS_SPLIT_Z,
  width: 0.030, out: 0.012,
}

export /**
 * ── 風擋：**四個角直接訂死**，不由網格湊 ──────────────
 *
 * 四邊形由參考模型的玻璃 mesh 量出（見 `b17-ref.measure.ts` 的檔頭）：平面
 * `y = 4.9107 − 0.8469·|x| + 1.0124·z`，殘差 RMS 0.0102；上緣是一條水平線
 * `y = 1.812`（x ≤ 0.18 那九條帶量到的值一模一樣）；前緣 `z = −3.2758 +
 * 0.6345·x`；外緣 0.60（艙頂的冠降到上緣高度的那個 x）。
 *
 * ```
 *   內下 A (0.03, 1.5881, −3.2568)   內上 B (0.03, 1.8120, −3.0356)
 *   外上 C (0.60, 1.8120, −2.5588)   外下 D (0.60, 1.4716, −2.8951)
 * ```
 *
 * 【內側縮 0.03，z 要跟著走】中線留一根中框 —— 真機本來就有，而且俯視才
 * 看得出是兩片。第一版只把 x 由 0 縮到 0.03、z 卻留在 x = 0 的值，於是上緣
 * 的斜率變成 0.8805 而烘焙用的是 0.8365：玻璃整片微微轉了一點，蒙皮上該
 * 落在上緣的那一排頂點（索引 2）**整段落在玻璃外面 0.003…0.013**。
 * 補丁只是換材質時看不出來；改成挖洞之後那就是一條會透光的縫。
 *
 * 現在兩個內角的 z 各自落在自己的緣線上：
 * `A.z = FZ0 + FZK × 0.03`、`B.z = UZ0 + UZK × 0.03`。
 *
 * 【蒙皮不挖】那一段的環在烘焙時就壓在同一個平面上（逐站離面 ±0.1 mm），
 * 玻璃浮 4 mm 蓋上去。蒙皮留成金屬 —— 它是玻璃後面的結構。
 */
const WINDSCREEN: readonly (readonly (readonly [number, number, number])[])[] = [
  [[0.03, 1.5881, -3.2568], [0.03, 1.8120, -3.0356],
    [0.60, 1.8120, -2.5588], [0.60, 1.4716, -2.8951]],
  [[-0.03, 1.5881, -3.2568], [-0.60, 1.4716, -2.8951],
    [-0.60, 1.8120, -2.5588], [-0.03, 1.8120, -3.0356]],
]

/**
 * ── 機背的兩片頂窗：**先訂外框，蒙皮再遷就** ────────────────
 *
 * 與風擋同一條原則（見 `WINDSCREEN`），只是這裡的窗不是平的 —— 它就是艙頂
 * 本身。所以訂死的是**外框**（逐 z 的半寬），y 由烘好的蒙皮取值。
 *
 * 【量測】只打參考模型的透明 mesh（`Object_50`），x 每 0.03 一帶、z 每
 * 0.025 一刀，讀該帶有玻璃的 z 區間：
 *
 * ```
 *   x     前段 z             後段 z
 *   0.00  2.425 … 2.800     3.225 … 4.375
 *   0.21  2.425 … 2.800     3.225 … 4.300
 *   0.30  2.425 … 2.800     3.300 … 4.200
 *   0.36  2.475 … 2.775     3.225 … 4.075
 *   0.42  2.450 … 2.750     3.250 … 3.900
 *   0.45      —             3.275 … 3.775
 *   0.48      —                 —
 * ```
 *
 * 半寬取 0.44（前窗）與 0.46（後窗）—— x 0.45 那一帶還有、0.48 沒有，所以
 * 真值落在 0.45～0.48 之間；取下緣那一側（坑 20b：寧可少）。
 *
 * 三件事一次讀出來：**2.80…3.23 是金屬**（兩片窗不是一片）、半寬到 0.44～
 * 0.46 為止（舊版的索引 3 是 0.50…0.55，太寬）、後窗的尾端是**收尖的**。
 *
 * 【平面殘差】前窗配平面 RMS 0.0053、最大 0.0123 —— 它是平的。後窗 RMS
 * 0.0208、最大 0.0557，因為它有 1.15 m 長、艙頂在那一段一路往下彎。所以
 * 後窗不能像風擋那樣訂成一片平板，得貼著蒙皮走。
 */
const ROOF_FRONT: readonly (readonly [number, number])[] = [
  [2.425, 0.12], [2.470, 0.36], [2.510, 0.44],
  [2.740, 0.44], [2.780, 0.36], [2.800, 0.12],
]

const ROOF_REAR: readonly (readonly [number, number])[] = [
  [3.225, 0.15], [3.260, 0.36], [3.300, 0.46], [3.850, 0.46], [3.900, 0.44],
  [4.000, 0.41], [4.075, 0.38], [4.125, 0.34], [4.200, 0.30], [4.300, 0.21],
  [4.375, 0.02],
]

/** 某一站的半剖面折線上，x 處的 y（折線在 x ≤ 0.55 這一段是單調的）。 */
function ringY(half: readonly (readonly number[])[], x: number): number {
  for (let i = 1; i < half.length; i++) {
    const a = half[i - 1]!, b = half[i]!
    if (x <= b[0]! || i === half.length - 1) {
      const d = b[0]! - a[0]!
      return d === 0 ? a[1]! : a[1]! + (b[1]! - a[1]!) * ((x - a[0]!) / d)
    }
  }
  return half[0]![1]!
}

/** 烘好的蒙皮在 (x, z) 的 y。站位之間線性內插。 */
function skinY(z: number, x: number): number {
  const R = B17G_HULL
  let k = 1
  while (k < R.length - 1 && R[k]!.z < z) k++
  const a = R[k - 1]!, b = R[k]!
  const t = (z - a.z) / (b.z - a.z)
  return ringY(a.half, Math.abs(x)) * (1 - t) + ringY(b.half, Math.abs(x)) * t
}

/**
 * 四個進氣口的**碗狀凹槽**。專案負責人：「引擎與螺旋槳目前可以看穿，是不是
 * 要補上一個黑內碗填補空洞?」
 *
 * 【看穿的機制】兩具艙都是 `caps: { front: false }` —— 罩口是開的（封起來
 * 那裡就是一片機身色的平板，He 111 踩過）。而整流罩只有半徑 0.30、罩口是
 * 0.65：中間那一圈是空的，看進去是艙的**內側**，內側是背面、被剔除，於是
 * 一路看到天空。
 *
 * 【為什麼不照 He 111 那樣用一段 loft】He 111 的 `NAC_COWL_DARK` 是「朝前
 * 開口、比罩口大 2% 的暗色管子」，第一版照抄過來 —— 結果 `geometry.test.ts`
 * 的「每一個網格都是法線朝外」當場紅四片。開口朝 −Z 又整段坐在 z ≈ −3.0
 * 的殼，帶符號體積本來就是負的；He 111 沒紅只是因為**它還沒被加進那條護欄
 * 的機種清單**（skill 檢查清單的最後一項）。
 *
 * 改用 `assembly.bowl`：緣照罩口那一圈超橢圓逐點放大 2%（同 `roundness`、
 * 同 16 段，兩者的邊逐點對齊，罩口不會有鋸齒），法線朝**前**、離原點更遠，
 * 帶符號體積是正的。多出來的那 2% 從外面看就是進氣口的厚度。
 *
 * ```
 *   內艙  z −3.18  0.648 × 0.643  中心 +0.002
 *   外艙  z −2.78  0.643 × 0.549  中心 +0.257
 * ```
 */
function cowlRim(
  x: number, cy: number, z: number, hw: number, hh: number,
  grow = 1.02, seg = 16,
): (readonly [number, number, number])[] {
  const e = 2 / 2.3
  const shape = (v: number): number => Math.sign(v) * Math.abs(v) ** e
  const out: (readonly [number, number, number])[] = []
  for (let i = 0; i < seg; i++) {
    // 由 +Z 看是順時針 → Newell 法線朝 −Z → 碗往機尾凹（而不是往機首凸）
    const t = (-i / seg) * Math.PI * 2
    out.push([x + shape(Math.cos(t)) * hw * grow,
      cy + shape(Math.sin(t)) * hh * grow, z])
  }
  return out
}

/** 索引 `i` 那條稜在 z 的 x。站位之間線性內插 —— 那正是網格的邊。 */
function ringX(z: number, i: number): number {
  const R = B17G_HULL
  let k = 1
  while (k < R.length - 1 && R[k]!.z < z) k++
  const a = R[k - 1]!, b = R[k]!
  const t = (z - a.z) / (b.z - a.z)
  return a.half[i]![0]! * (1 - t) + b.half[i]![0]! * t
}

/**
 * 照外框在蒙皮上鋪一片玻璃。
 *
 * 【格線要與蒙皮對齊，不能自己等分】第一版橫向只切兩格（那一段的艙頂是平
 * 的，看起來切再細也一樣）。錯在**外框比平頂寬**：平頂只到索引 2（x 0.386…
 * 0.427），而外框到 0.44，中間那一段蒙皮已經在往下斜。一格由中線直接拉到
 * 0.44 的四邊形，於是整片**沉在平頂之下**幾 mm —— 算圖上是玻璃被蒙皮咬出
 * 一圈缺口。加大浮高治不了，那是斜率不對不是高度不對。
 *
 * 現在兩個方向都取齊：
 *
 * ```
 *   站位  外框自己的 z ∪ 這一段的所有環 z
 *   橫向  0 → 索引 2 那條稜 → 外框
 * ```
 *
 * 每一格於是整格落在蒙皮的同一片上，只差三角化的對角線那幾 mm。
 *
 * 【纏繞方向】外側 → 內側 → 內側（下一站）→ 外側（下一站），法線才朝上；
 * 反過來的話正面被剔除，從機外看整片消失。
 */
function roofPane(
  outline: readonly (readonly [number, number])[],
): (readonly [number, number, number])[][] {
  const z0 = outline[0]![0]!, z1 = outline[outline.length - 1]![0]!
  const zs = new Set(outline.map((o) => o[0]))
  for (const r of B17G_HULL) if (r.z > z0 && r.z < z1) zs.add(r.z)
  const list = [...zs].sort((a, b) => a - b)
  const wAt = (z: number): number => {
    for (let i = 1; i < outline.length; i++) {
      const [za, wa] = outline[i - 1]!, [zb, wb] = outline[i]!
      if (z <= zb) return wa + (wb - wa) * ((z - za) / (zb - za))
    }
    return outline[outline.length - 1]![1]!
  }
  /**
   * 兩條稜都要斷：索引 1、索引 2。機背 z 3.4 之後甲板一路收窄，最外的那條
   * 平頂的邊界由索引 2 換成索引 1（z 3.93 的索引 1 已經比中線低 0.19）——
   * 只在索引 2 斷的話，跨過索引 1 的那一格又會沉下去。
   *
   * 稜落在外框之外時夾住：兩格都在同一片蒙皮上，只是白切一刀。
   */
  const kAt = (z: number, i: number, lo: number, hi: number): number =>
    Math.max(lo, Math.min(hi, ringX(z, i) / wAt(z)))
  const at = (z: number, u: number): readonly [number, number, number] => {
    const x = wAt(z) * u
    return [x, skinY(z, x), z]
  }
  const colsAt = (z: number): number[] => {
    const k1 = kAt(z, 1, 0.05, 0.95)
    const k2 = kAt(z, 2, k1 + 0.02, 0.98)
    return [0, k1, k2, 1]
  }
  /**
   * 【左半是右半的鏡像，不是自己再跑一次】兩邊各自由 −1 排到 +1 的話，四個
   * 角一樣、**對角線卻反向** —— `flatGlass` 的三角化是固定的 q0–q2，所以
   * 同一格在左右兩邊被切成不同的兩個三角形。四個角不共面（z 不同、蒙皮的 y
   * 也不同），於是兩邊的內部各差 1～2 mm：算圖上左右的亮暗不一樣，人工回報
   * 「一邊破碎、另一邊沒有」。
   *
   * 鏡像 = x 取負 + 順序反轉。反射本身翻一次定向、反轉再翻一次，法線的 y
   * 分量因此保持朝上。
   */
  const mirror = (q: (readonly [number, number, number])[]):
  (readonly [number, number, number])[] =>
    [q[3]!, q[2]!, q[1]!, q[0]!].map((v) => [-v[0], v[1], v[2]] as const)
  const out: (readonly [number, number, number])[][] = []
  for (let i = 1; i < list.length; i++) {
    const za = list[i - 1]!, zb = list[i]!
    const ca = colsAt(za), cb = colsAt(zb)
    for (let j = 0; j + 1 < ca.length; j++) {
      const q = [
        at(za, ca[j + 1]!), at(za, ca[j]!), at(zb, cb[j]!), at(zb, cb[j + 1]!),
      ]
      out.push(q, mirror(q))
    }
  }
  return out
}

/**
 * 頂窗後面的暗艙。緣取外框自己（少取幾個點就夠 —— 緣與艙口之間隔著一圈
 * 蒙皮，差一點看不到），逆時針繞才會讓碗**往下**凹。
 */
function roofBowl(
  outline: readonly (readonly [number, number])[],
): (readonly [number, number, number])[] {
  const rim: (readonly [number, number, number])[] = []
  for (const [z, w] of outline) rim.push([-w, skinY(z, -w), z])
  for (let i = outline.length - 1; i >= 0; i--) {
    const [z, w] = outline[i]!
    rim.push([w, skinY(z, w), z])
  }
  return rim
}

export const B17G_BODY_COLOR = 0x8d9299

export function buildB17G(): AircraftModel {
  const h = createHull({
    bodyColor: B17G_BODY_COLOR,
    accentColor: 0x3c4147,
    realLength: 22.66,
    // 平移已經烘進 b17g.hull.ts 的站位裡（量測系 +1.1212）
    offsetZ: 0,
    /**
     * 眼點：正駕駛在座艙的**左**座。z −2.9 落在座艙段中間，y 1.05 在該站
     * 背線（1.57）之下、腹線（−0.78）之上。
     */
    eyePoint: new Vector3(-0.38, 1.05, -2.90),
    /**
     * 右翼尖弦的中點，由 `WING` 推出：
     *   x = halfSpan                              = 15.810
     *   y = rootY + tan(3.87°) × 15.81            = −0.10 + 1.0693 = 0.969
     *   翼尖前緣 = rootZ + tan(7.53°) × 15.81      = −1.5045 + 2.0906 = 0.586
     *   翼尖弦  = tipChord × tipFactor(1, 0.45)    = 2.413 × 0.45 = 1.086
     *   前緣   = 0.586 + (2.413 − 1.086) × 0.3     = 0.984   （TIP_ANCHOR）
     *   z     = 前緣 + 弦/2                        = 1.527
     */
    wingTip: new Vector3(15.810, 0.969, 1.527),
  })

  // 機首罩：外殼切成兩截，前段玻璃、後段機身色
  h.glazedNose(B17G_HULL, GLASS_SPLIT_Z, GLASS_PATCHES)
  h.flatGlass(WINDSCREEN)
  /**
   * 【浮 12 mm 不是預設的 4】玻璃取的 y 是「兩站之間線性內插」，而蒙皮那一格
   * 實際上是兩個三角形 —— 對角線那一側會比內插值低幾 mm。4 mm 推不過去，
   * 算圖上是一圈被蒙皮咬出來的缺口。
   */
  h.flatGlass([...roofPane(ROOF_FRONT), ...roofPane(ROOF_REAR)], 0.012)
  /**
   * 玻璃後面的四個暗艙。
   *
   * 【為什麼不沿用 `glassBackGeometry`】那一份是「補丁那串頂點搬到兩端的
   * 弦上」。風擋只跨兩格，弦幾乎就是蒙皮 —— 做出來是一片跟著機殼隆起的
   * 薄板，下緣還從蒙皮戳出去。專案負責人：「駕駛玻璃內的黑碗應該要內凹」。
   * 所以那幾片補丁掛了 `bare`（連襯裡都不生），暗艙改由這裡按玻璃的框生。
   */
  for (const q of WINDSCREEN) h.bowl(q, 0.18)
  h.bowl(roofBowl(ROOF_FRONT), 0.14)
  h.bowl(roofBowl(ROOF_REAR), 0.20)
  h.frames(B17G_HULL, FRAMES)

  /**
   * 四具發動機艙。機身色而不是強調色 —— 它們是蒙皮的一部分。
   *
   * 【內外各一串截面】兩者的高度差 0.28（機翼上反角）、而且只有內艙有主輪
   * 艙的鼓包。壓進同一串截面就會整圈一起跳（He 111 的 `NACELLE` 檔頭記過
   * 這一條：底線該掉的那一步把頂線也憑空抬了 0.045）。
   */
  for (const sx of [1, -1]) {
    h.loft(NACELLE_INNER, h.body).position.x = sx * NAC_X_INNER
    h.loft(NACELLE_OUTER, h.body).position.x = sx * NAC_X_OUTER
    // 四個進氣口的暗碗，見 cowlRim
    h.bowl(cowlRim(sx * NAC_X_INNER, +0.002, -3.186, 0.648, 0.643), 0.24)
    h.bowl(cowlRim(sx * NAC_X_OUTER, +0.257, -2.786, 0.643, 0.549), 0.24)
  }

  h.wingPair(WING)
  h.wingPair(TAILPLANE)
  // 主垂尾的頂不圓化（頂蓋接在上面）；頂蓋自己收圓
  h.upright(FIN, 0.621, undefined, 0.122)

  h.blisters(TURRETS)

  // 尾艙罩：前段金屬、後段玻璃，玻璃裡一層暗艙
  h.loft(HOOD_FWD, h.body)
  h.loft(HOOD_DARK, h.dark)
  h.loft(HOOD_GLASS, h.glass)

  /**
   * 四具螺旋槳。
   *
   * 【`noseZ` 是整流罩的**底座**不是尖端】`assembly.propeller` 把圓錐擺在
   * `noseZ − spinnerLength / 2`、頂點朝 −Z，所以錐體佔 `noseZ − length` 到
   * `noseZ`。He 111 那次照「機首尖端」填，整具整流罩前伸了 0.55 m，而艙的
   * loft 又從同一點長一個錐尖接上去，接縫看不出來、只是引擎長了半公尺
   * （坑 27）。
   *
   * 這裡的 `noseZ` 直接取每一具艙的**首站**：內艙 −3.18、外艙 −2.78。
   *
   * 【`spinnerY` 要跟著艙心走，不是一律 0】外艙整個高 0.25（機翼上反角），
   * 首站的中心在 **+0.257**；整流罩留在 0 的話它偏在罩口下緣，補上進氣口的
   * 暗碗之後一眼就看得出來 —— 碗是照罩口生的，槳轂卻不在碗心。內艙的首站
   * 中心是 +0.002，所以那一對本來就對。
   *
   * 【槳盤半徑 1.765】真機三葉槳直徑 11 ft 7 in = 3.53 m。注意
   * `specs/b17g.ts` 的 `prop.diameter` 是**等效單槳盤**（四具的合計面積換算
   * 成一個），與這裡的視覺尺寸是兩件事。
   */
  for (const sx of [1, -1]) {
    for (const [x, noseZ, y] of [
      [sx * NAC_X_INNER, -3.18, 0.002], [sx * NAC_X_OUTER, -2.78, 0.257],
    ] as const) {
      h.propeller({
        x, noseZ, spinnerRadius: 0.30, spinnerLength: 0.42, spinnerY: y,
        blades: 3, propZ: noseZ - 0.18, propRadius: 1.765,
      }, B17G_HULL[0]!.z)
    }
  }

  return h.finish()
}
