import { Vector3 } from 'three'
import type { GlbAircraft } from './glb'

/**
 * F6F-5 Hellcat 的外型 —— **這一台不是量出來的，是畫出來的。**
 *
 * 【座艙是盒切的】玻璃不是自己一片面，而是**機身本身的那片面**：機身的背線先
 * 抬到罩頂（`build_f6f2.py` 的 `lift_to_canopy`），再用一個六面盒切 —— 玻璃 =
 * 機身∩盒、機身 = 機身−盒。罩子與龜背因此是同一條 loft，接縫的落差恆等於零
 * （舊的「玻璃先行」版在罩子後緣 40 mm 之內掉 42 mm、下一站又回升 5 mm）。
 *
 * 代價是量到的溫室骨架軟化了：四個站位都測到「肩在 |x| 0.187、比中線低 0.093」
 * 的那道稜，斜率變化由 4.6 倍降到 2.1 倍，罩子也寬了約 12%（艙緣半寬 0.455 →
 * 0.509）。專案負責人 2026-09-07 看過兩版的算圖後裁定接受。
 *
 * 幾何全部在 Blender 裡建（來源 `tools/blender/f6f5.blend`；建模腳本
 * `tools/blender/build_f6f2.py` 是它的出處），直接匯出
 * `public/models/f6f5.glb`，2,978 個三角形（P-51D 是 2,703）。所以這裡沒有
 * `.hull.ts` 錨點表 —— GLB 就是那份資料。
 *
 * 【GLB 的座標系正好就是機體座標】Blender 匯出 glTF 時 +Y up、−Z forward，
 * 而機體座標是 X 翼展、Y 上、Z 機尾。實測包圍盒：
 *
 *     X −6.530 … 6.530   Y −1.046 … 2.167   Z −2.932 … 7.190
 *
 * X 對稱於 0（翼展 13.06 m，真機 13.06）、Z 的原點落在**機翼四分之一弦線**
 * （翼根前緣 −0.809 + 弦長 3.235 的 1/4 = 0），與另外四台的約定相同，不必
 * 平移。
 *
 * 【外型的量測與驗收】對照參考模型 `ref/f6f.glb`：
 *   剖面 RMS（純機身區段）  Zb −2.20…−0.96  0.016–0.045 m
 *                          Zb  2.45… 5.24  0.009–0.026 m
 *   機翼平面形              x 2.0…6.0 每站前後緣與參考完全相同
 *   翼面高度                誤差 ≤ 0.006 m
 *   槳盤位置                −2.545…−2.495（參考 −2.552…−2.492）
 */
export const F6F5_MODEL: GlbAircraft = {
  url: '/models/f6f5.glb',
  realLength: 10.24,

  /**
   * 眼點。艙緣（玻璃下緣）量到是水平的 y = 0.812，取其上 0.29 當肩→眼；
   * z 0.90 落在風擋前框（0.300）後方 0.60 m，與 P-51D 同一個訂法。
   * 罩頂在 z 0.90 是 1.50，頭部餘裕 0.40。
   */
  eyePoint: new Vector3(0, 1.10, 0.90),

  /**
   * 右翼尖弦的中點，由 Blender 的 `WING` 推出（最外側站位 u = 1）：
   *   x = halfSpan                                        = 6.530
   *   y = rootY + tan(1.31°)×2.00 + tan(7.10°)×4.53       = 0.227
   *   翼尖弦 = tipChord × tipRound = 1.513 × 0.12          = 0.182
   *   前緣  = rootZ + tan(5.244°)×6.53 + (1.513−0.182)×¼   = 0.123
   *   z    = 前緣 + 弦/2                                   = 0.214
   */
  wingTip: new Vector3(6.530, 0.227, 0.214),

  /** 掛不了彈 */
  bombPoint: null,

  /**
   * Glossy Sea Blue（ANA 623）—— 1944 年之後的美國海軍艦載機塗裝，F6F-5
   * 全機單色。比另外四台都暗，但那是真的；而且它讓敵我在空中好分辨。
   */
  bodyColor: 0x3f5266,
  accentColor: 0x232a31,

  /** Blender 材質名 → 遊戲材質 */
  materials: {
    F6F_Body: 'body',
    F6F_Accent: 'accent',
    F6F_Glass: 'glass',
  },

  /**
   * 螺旋槳。轉軸高度取整流罩首環的 cy（0.038）；槳盤 Z 是量參考模型的
   * `Object_44` 得到的 −2.52（把頂點按 Z 分箱、每箱取最大半徑，只有
   * −2.55…−2.49 那幾箱衝到 1.93）。半徑照史實：Hamilton Standard
   * 13 ft 1 in = 3.987 m。
   */
  props: [{ node: 'F6F_Prop', hubY: 0.038, hubZ: -2.52, radius: 1.995 }],
}
