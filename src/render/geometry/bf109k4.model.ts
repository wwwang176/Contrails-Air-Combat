import { Vector3 } from 'three'
import type { GlbAircraft } from './glb'

/**
 * Bf 109（E-4 外型 + K-4 性能）的 GLB 外型。
 *
 * **來源是 `tools/blender/bf109k4.blend`，GLB 是它匯出的產物。** 這一台
 * 走的是「用 GLB 畫 GLB」：在 Blender 裡把 `ref/bf109e4.glb` 匯進來當量尺，
 * 機身沿 39 個站位切剖面（逐站取寬度剖面、剔除座艙內裝與鉚扣等 400 多個
 * 小連通塊）loft 成乾淨的低多邊形殼（水滴體 + 機鼻圓錐，蛋形剖面）；
 * 座艙玻璃用切割盒布林做出（見下方【玻璃是盒切的】）；其餘零件是簡單
 * 基本體：
 *
 *   機翼        一側一整片平板（無活動舵面），平面形量自參考模型
 *   水平尾翼    史實參數的平板（參考模型的安定面缺升降舵、支撐桿減面成
 *               尖刺，整組棄用；參數沿用舊程序版的 TAILPLANE）
 *   螺旋槳      三個矩形盒（照 P-51D 的做法）
 *   排氣管      每側六個黑色小方塊貼在引擎罩側面
 *   整流罩      10 邊形鈍卵 loft
 *   垂尾        史實參數的平板（參考模型的垂尾 −25%，不可信，見
 *               bf109e.ts 檔頭與技能坑 21：史實優先）；機身 loft 的尾段
 *               背線照 ref 扣掉垂尾後的射線值收斂（−3.3 站 0.757 →
 *               −4.75 站 0.586 → 尾柱 0.505），垂尾只存在 Fin 平板這一份
 *   機鼻凸包    引擎罩兩側各一顆縮扁的 8×4 UV 球（NoseBulgeR/L 左右
 *               獨立物件、各自正向建構——鏡射建構會反轉纏繞；位置手調）
 *   下巴進氣    整流罩後下方一個暗色盒（y 1.70..2.20、z −0.20..0.03）
 *   翼下散熱器  RadiatorR/L 左右獨立物件，各繞 y 軸傾斜 ±8.07°（實測
 *               翼下表面局部上反角）貼合翼下，頂面均勻埋入 0.03
 *   尾撐桿      TailStrutR/L：機尾 (±0.10,−5.15,0.03) 斜上到尾翼下
 *               (±0.60,−5.42,0.52) 的細長盒，兩端各多埋 4–5 cm
 *
 * 全機 1,123 個頂點、2,108 個三角形（預算是 4,000 頂點）。機翼沿展向有
 * 5 條環線（每 0.8 m）—— 命中盒要切得下去，正後方的放大倍數才壓得到
 * 2.05×（見 `specs/bf109k4.ts` 的 `hitBoxes`）。
 *
 * ── 重出 GLB 的設定（會靜靜弄丟東西，寫下來）────────────────
 *
 * ```
 *   use_selection=True    只選 BF109_* —— UserGlassBox 是切玻璃用的輔助盒
 *                         （hide_render），Camera / Light / RefCamOb 也不該進去
 *   export_yup=True       機體座標是 X 翼展、Y 上、Z 機尾
 *   export_extras=True    **預設是 False。** 少了它，`BF109_Wing` 的自訂屬性
 *                         `part = 'wing0'` 不會寫進 glTF extras，`glb.ts` 於是
 *                         標不出主翼，`geometry.test.ts` 的「四分之一弦線落在
 *                         原點」量不到任何 mesh、算出 NaN
 * ```
 *
 * 驗收方式：重出之後把新舊 GLB 的每個節點逐頂點比對，**只有你動到的那一個
 * 物件可以不同**。
 *
 * 【玻璃是盒切的】新機種照這個流程做：機身先做完整（含
 * 罩體體積），用手調的切割盒（後緣斜面）對機身做布林——玻璃件
 * = 機身 ∩ 盒、機身 = 機身 − 盒，外觀不變、接縫天生吻合。之後獨立整
 * 形玻璃（平頂一個平面、平側板、斜風擋），**接縫頂點鎖死不動**；玻璃
 * 與機身的接觸面不建玻璃面（刪掉布林封蓋），機身側的接觸面塗黑
 * （BF109_Cockpit）；玻璃半透明（Alpha 0.45，抄 P-51D）。左半手調後
 * 用 Symmetrize（−X→+X）鏡像到右半。
 *
 * 【對齊】翼展縮放（8.288 → 9.87）、翼根四分之一弦線在原點，螺旋槳軸高
 * 0.36。實測烘出來的關鍵座標：
 *
 *   機身 z −2.17…5.80、方向舵尾端 6.32
 *   整流罩尖端 z −2.578
 *   翼根前緣 −0.518、翼尖 x ±4.934
 *
 * 座標是機體座標：X 翼展、Y 上、Z 機尾，原點在主翼四分之一弦線。
 */
export const BF109K4_MODEL: GlbAircraft = {
  url: '/models/bf109k4.glb',
  realLength: 8.64,

  // 眼點：艙緣（甲板切線）0.547 上方 0.26；z 0.95 落在風擋前緣（0.36）
  // 後方 0.6 m，與 P-51D / F6F-5 同一個訂法
  eyePoint: new Vector3(0, 0.80, 0.95),

  // 右翼尖弦的中點：翼尖站位 x 4.935（= spec 半翼展）、前緣 −0.196、
  // 後緣 0.594（Blender 裡訂的收尖站），y 是量測上反角線到翼尖的高度
  wingTip: new Vector3(4.935, 0.225, 0.199),

  /** 掛不了彈 */
  bombPoint: null,

  /** 與程序版相同的塗裝（`bf109e.ts` 的 BF109_BODY_COLOR / accent）。 */
  bodyColor: 0x7e8a73,
  accentColor: 0x33403a,
  // 貼圖由 tools/livery/bf109k4.py 畫。UV 在載入時算，GLB 不動
  livery: { url: '/textures/bf109k4.png', scale: 100, planZ: 1.87, sideY: 0.70 },

  /** Blender 材質名 → 遊戲材質 */
  materials: {
    BF109_Body: 'body',
    BF109_Accent: 'accent',
    BF109_Glass: 'glass',
    BF109_Cockpit: 'cockpit',
  },

  /**
   * 螺旋槳。轉軸高度 = 整流罩軸心 0.36（對齊舊程序版的 spinnerY）；
   * 槳盤 z 取參考模型槳葉的位置（−2.63…−2.41 的中點換算後為 −2.29）；
   * 半徑照史實 VDM 螺旋槳直徑 3.10 m（槳葉盒也縮到同一半徑）。
   */
  props: [{ node: 'BF109_Prop', hubY: 0.36, hubZ: -2.29, radius: 1.55 }],
}
