import { Vector3 } from 'three'
import { DEG } from '../../core/math'
import { createHull, type AircraftModel, type LoftPart, type FinParams } from './assembly'
import type { FrameSpec } from './canopy'
import type { GlassPatch } from './hull'
import { HE111_HULL } from './he111.hull'
import type { WingParams } from './wing'

/**
 * Heinkel He 111 H-6 的程序化外型 —— 真機全長 16.40 m、翼展 22.60 m、
 * 兩具 VDM 三葉槳各 3.50 m。
 *
 * 照片上最能認出它的四件事：
 *   1. **全玻璃的不對稱機首** —— 沒有階梯式風擋，整個機首是玻璃；駕駛座偏左、
 *      機槍座偏右
 *   2. 兩具掛在機翼上的發動機艙，後端拉得很長（主輪收進去）
 *   3. 機腹的吊艙（Bola）—— 機身下方那一塊突出的機槍座
 *   4. 大而圓的垂尾，配上細長的後機身
 *
 * 【全部座標量自參考模型】機身、機翼、尾翼、發動機艙都是同一次三軸切片量出來
 * 的，量測系整體 −3.0889 換成機體座標（見 `he111.hull.ts`）。
 *
 * ── 零件的分法，以及每一條為什麼非這樣不可 ─────────────────
 *
 * **發動機艙是三個零件**（`NACELLE`／`NAC_INTAKE_LOW`／`NAC_INTAKE_TOP`），
 * 不是一串截面。參考模型是一個圓錐體加上下兩個進氣罩，而 `buildFuselage`
 * 的截面是**整圈一起縮放**的 —— 壓進同一串的話，底線該掉的那一步會把頂線
 * 也憑空抬 0.045，側視看是一段沒有理由的起伏。
 *
 * **暗色凹槽不是「補一塊暗色的東西」。** 凹槽要有開口、要有朝內的壁、
 * 而且壁要接到緣上：罩子 `caps: { front: false }` 真的開口，內壁是朝前
 * 開口、比罩口大 2% 的雙面暗管。做成一根縮小一號的封閉塞子的話，看得到的
 * 只有它的封蓋，側壁法線朝外會被剔除，而且中間一圈是空的。
 * 見 `NAC_INTAKE_LOW_DARK`／`NAC_INTAKE_TOP_DARK`／`NAC_COWL_DARK`／
 * `NAC_INTAKE_LOW_EXIT`。
 *
 * **機首玻璃內是一片整剖面黑板**（`splitZ` 前 1 cm 的前隔框），不是一層
 * 內縮的殼 —— 內縮的殼從外面看是罩子裡飄著一台小飛機。
 *
 * **機背襯裡取補丁那一段的弦**（`hull.ts` 的 `lidAt`）。跟著玻璃等比內縮
 * 的話，隆起的罩子會生出一個同樣隆起的襯裡，不是凹槽。
 *
 * **`LID_SINK` 對側窗與機背是兩個值。** 機背的玻璃與蒙皮共面，側窗是
 * **凹槽**、玻璃沉到 0.94 —— 碗底沉 0.985 會落在玻璃**外面**，整片黑板
 * 蓋住那扇窗，看起來是一張黑貼紙。
 *
 * **玻璃骨架用 `assembly` 的 `bothSides` 材質。** 窄帶只有一個朝向，而
 * 全玻璃機首看得到對側骨架的背面 —— 單面材質下整條機首是通的。
 *
 * **機腹吊艙是金屬的，只有前後兩片窗**（`bolahole` 的實測表在
 * `BOLA_PARTS` 上面）。照片上讀起來像「整個凸起都是玻璃」，量下去不是。
 *
 * **原點在翼根四分之一弦線上**，而翼根弦是 4.149（`WING.kink` 之後的值），
 * 不是外段外推回來的 5.954。
 */

/**
 * 主翼。
 *
 * 【外段（X 3.4…10.2）的量測值，機體座標】前後緣各配一條直線：
 *
 * ```
 *   前緣  −0.983 → +0.713   後掠 14.0°（dz/dx = 0.24941）
 *   後緣  +3.870 → +3.365   前掠  4.3°（dz/dx = −0.07427）
 *   厚弦比 18.2% → 10.9%    真機 NACA 23018 → 23010
 * ```
 *
 * 【內段（X 1.4…3.4）是另一條線】見 `WingParams.kink` 的檔頭 —— 前緣幾乎
 * 不後掠（0.9°）、後緣後掠 12.6°，所以弦長是**往外變長**的 4.149 → 4.853。
 *
 * 【翼根弦 4.1492、前緣 −1.0373】內段兩條線外推到 X = 0。而 −1.0373 正好是
 * −4.1492/4 —— 四分之一弦線壓在原點，那不是巧合，是 `he111.hull.ts` 的平移量
 * 就是這樣定出來的（`geometry.test.ts` 有一條護欄在守）。
 *
 * ── 交叉驗證：翼面積（坑 16）──────────────────────────
 *
 * ```
 *   內段 X 0→3.4     弦 4.149 → 4.853    15.30 m²
 *   外段 X 3.4→11.3  弦 4.853 → 2.296    28.24 m²
 *                               全機 87.1 m²   史實 86.5   +0.7%
 * ```
 *
 * **不可以把外段那條線一路外推到翼根**：那會把凹陷填平，梯形面積 93.2 m²、
 * 比史實大 7.8%，而且那個誤差會被誤讀成「參考模型自己的比例問題」。
 *
 * ── ⚠ 還沒量準的一個 ──────────────────────────────────
 *
 * **上反角 6°** 是史實值不是量測值。切片量到的「X 外緣隨切面高度外移」給的
 * 是 10.2°，但那個量混了翼型厚度沿翼展的收縮，不是中厚線的斜率。要量準得
 * 逐站取 (vMin+vMax)/2 再配線。
 *
 * **翼根高度 −0.30** 是由機身剖面推的（機翼接在機身腰線略下方），沒有直接量。
 */
const WING: WingParams = {
  rootChord: 4.1492, tipChord: 2.296, halfSpan: 11.30, rootZ: -1.0373,
  kink: { at: 3.40 / 11.30, chord: 4.853, lead: -0.9829 },
  sweep: 14.0 * DEG, dihedral: 6 * DEG, rootY: -0.30,
  /**
   * 【厚度配的是「看得見的那一段」】量到的厚度不是直線：X 1.4～1.8 平在
   * 0.935，X 3.4 起才以 −0.0879/m 掉下去。`thickness`／`tipThickness` 之間
   * 只能是一條直線，所以取對 X ≥ 1.4 誤差最小的那一條 —— 逐站差
   * ＋0.041（1.4）／−0.066（3.4）／−0.025（7.6）／＋0.010（翼尖）。
   *
   * 【tipThickness 0.200 而不是按弦長等比】逐站對切：X 9.1 起等比版本偏厚
   * 0.03~0.06。翼根的 1.084 對得很好，所以要修的是沿翼展的收縮率不是
   * 絕對值。
   */
  thickness: 1.084, tipThickness: 0.200,
  /**
   * 【tipRound 0.45 是取捨不是定值】逐站對切的兩組實測：
   *
   * ```
   *   X        10.00  10.30  10.60  10.90  11.20
   *   0.55 差  −0.12  −0.20  −0.19  −0.09  +0.36
   *   0.38 差  −0.15  −0.26  −0.28  −0.27  +0.02
   * ```
   *
   * **`tipRound` 只控制收多少，控制不了從哪裡開始收。** 而參考模型是
   * 「撐住弦長到 96% 半翼展才急收」（1.94 → 1.05 發生在最外側 4% 之內），
   * 那正是坑 18 描述的圓翼尖。單一參數配不出那條折線，所以 0.55 在外段好、
   * 0.38 在翼尖好，取 0.45 讓兩端都在 0.25 m 之內。
   */
  tipRound: 0.45,
}

/**
 * 翼根整流罩 —— **只有後緣那一段**。
 *
 * 【量法】沿 X 切，只留 |x| ≥ 0.95（機身最寬 0.90，所以那些切面裡只剩機翼），
 * `uWindow` 把螺旋槳擋掉。自家與參考模型逐站對照（參考模型帶著 +0.126 的
 * 剛體位移，下表已扣掉）：
 *
 * ```
 *   X      我的後緣   參考後緣   差
 *   0.95     3.324      4.284    +0.96
 *   1.15     3.371      4.284    +0.91
 *   1.25     3.394      4.284    +0.89
 *   1.34     3.415      3.434    +0.02   ← 到這裡就沒了
 *   4.00     3.826      3.824    −0.00
 * ```
 *
 * 前緣則**完全對得上**（x 1.05 起逐站差 0.014 以內）—— 參考模型的翼根在
 * 前緣沒有整流罩，只有後緣有。所以這一片不往前伸。
 *
 * 【為什麼是獨立的一片】與 P-51D 的 `WING_FILLET` 同一組理由：主翼的翼根弦
 * 是**氣動參考弦**，四分之一弦線必須落在原點（`geometry.test.ts` 有護欄）；
 * 整流罩不算在氣動弦裡。而它只跨 11.7% 半翼展，測試判斷「哪一片是主翼」的
 * 門檻是 80%，獨立成片自然被排除。
 *
 * 【為什麼不會 z-fighting】厚度取主翼同站位的 **0.97 倍**，弦長卻長 28%。
 * 同一個 z 上整流罩的弦長比例比主翼小，翼型最厚處在 30% 弦長 —— 所以在
 * 主翼的前半段整流罩比較薄（縮在裡面），到主翼後緣附近主翼薄到 0 而整流罩
 * 還在 76% 弦長，就露出來了。露出來的正好是後緣那一塊。
 *
 * 【參考模型的後緣是一條**平的**橫線（x 0.95～1.25 都是 4.284），而梯形只能
 * 線性收】x = 1.1 這一帶因此比參考少 0.4 m。坑 20b：寧可少也不要多 ——
 * 少的地方看起來只是整流罩小一點，多的地方是憑空長出參考模型沒有的東西。
 */
const WING_FILLET: WingParams = {
  rootChord: 5.321, tipChord: 4.422, halfSpan: 1.32, rootZ: -1.0373,
  sweep: 0.92 * DEG, dihedral: 6 * DEG, rootY: -0.30,
  thickness: 1.051, tipThickness: 0.951,
}

/**
 * 水平尾翼。量測值（機體座標，量測系 −3.0889）：
 *
 * ```
 *   X 0.60 → 3.80   前緣 7.214 → 8.294（後掠 18.6°）
 *                   後緣 10.016 → 9.148（前掠 15.2°）
 *                   弦長  2.801 →  0.853   厚弦比 ~10%
 *   半翼展 3.95（`align` 那一趟量到全展 7.889，真機 7.60，+3.8%）
 * ```
 *
 * 【後掠 11.1° 而不是量到的 18.6°】逐站對切：參考模型幾乎等弦長到 X = 3.0
 * 才急收，直線配的 18.6° 讓內段整段偏窄。最大差 0.51 → 0.25。
 *
 * 【rootY 0.0】平尾裝在機身中線附近。沒有直接量到，由該段的機身剖面推的。
 */
const TAILPLANE: WingParams = {
  rootChord: 3.01, tipChord: 1.63, halfSpan: 3.95,
  sweep: 11.1 * DEG, dihedral: 0, thickness: 0.320, tipThickness: 0.160,
  rootZ: 9.968, rootY: 0.0, tipRound: 0.30,
}

/**
 * 垂尾 —— 前緣弧線的**下段**。
 *
 * 【rootY −0.10 必須低於整段機尾背線】坑 17：取「前緣所在站位的背線」很直覺，
 * 但背線一路降到尾錐的 0.06 —— 垂尾後半會整個懸空，側視看得到底下透出背景。
 * 取 −0.10 讓虛擬翼根低於最後一個機身環，多出來的部分埋進機身反正看不到。
 */
const FIN: FinParams = {
  chordRoot: 3.02, chordTip: 1.64, height: 2.20, sweep: 23.8 * DEG, z: 9.888, rootY: -0.10,
}

/**
 * 垂尾頂蓋 —— 前緣弧線的**上半**。
 *
 * 【為什麼需要它】逐站對切，參考模型的垂尾前緣（機體座標）：
 *
 * ```
 *   z    10.26  10.46  10.66  10.86  11.06  11.26  11.46  11.66
 *   上緣  0.747  1.401  1.830  2.111  2.309  2.453  2.555  2.608
 * ```
 *
 * 逐段斜率：**下段 dy/dz = 2.27（後掠 23.8°）、上段 0.62（後掠 58°）**。
 * 也就是這片圓垂尾的前緣**下面陡、上面平**。
 *
 * 我第一版把補的那一片放在**下半**、後掠取 12°（更陡），方向剛好相反 ——
 * 結果是下半凸出一塊 1.50 的平台、上半仍然差 0.6。單一梯形的最大差是
 * 1.110，加了反向的那一片之後只降到 0.622。**改對方向之後才收得下來（0.247）。**
 *
 * 【厚度 0.14，比主垂尾薄】它整片縮在主垂尾的厚度之內，只有前緣那一小塊
 * 露出來 —— 與 P-51D 翼根整流罩「厚度取 0.97 倍、弦長更長」是同一個
 * 不共面的手法。
 */
const FIN_CAP: FinParams = {
  chordRoot: 1.72, chordTip: 0.41, height: 0.58, sweep: 58.0 * DEG, z: 10.808, rootY: 2.05,
}

/**
 * 發動機艙。**截面串是烘出來的，不是手打的** —— 量測腳本的 `nacbake` 那一格
 * 直接吐出這段字面值（與機身外殼同一個做法）。射線原點橫移到 x = 2.6、
 * y = 0（`sliceRef` 的 `axisU`）：從機身軸心射出去會先穿過機身與機翼，取到的
 * 「最外側」是翼尖不是發動機艙。
 *
 * ── 量它的三個前提 ───────────────────────────────────────
 *
 * **截面一律烘焙，不要手抄。** 從 0.4 m 的表格手抄成 16 個截面會帶進曲率
 * 反轉（halfHeight 0.668→0.675、centerY −0.041→−0.033），而且沒有任何東西
 * 會提醒你。烘焙之後寬度的曲率變號是 1/24。
 *
 * **`maxRadius` 不可以貼著量測值。** 下緣量到 0.994，用 1.0 當上限時超出的
 * 射線回 0、被當成量不到，看不出來 —— 等於邊界值的參數就是垃圾資料。
 * 1.3 之下最深處是 1.003～1.012。
 *
 * **`segments` 是 20，不是 12。** 直徑 1.3 m 的艙用 12 邊形等於 30° 一個
 * 面，比 30 邊形的機身還粗。
 *
 * ── 為什麼是三個零件 ─────────────────────────────────────
 *
 * 參考模型是**一個圓錐體的引擎，加上下方進氣罩與上方進氣罩**。三樣壓進
 * **同一串截面**的話，`buildFuselage` 的截面是整圈一起縮放的 —— 烘出來的
 * 表格自己就露餡：
 *
 * ```
 *      z      halfWidth   halfHeight   centerY      底線      頂線
 *   −1.289      0.613       0.573      −0.140     −0.713     0.433
 *   −1.279      0.619       0.730      −0.252     −0.982     0.478   ← 一步
 * ```
 *
 * 半寬是乾淨的圓錐（0.61 → 0.65 峰 → 收），**半高與 centerY 卻在這裡各跳
 * 0.157／0.112**。底線該掉 0.27 沒錯 —— 那是進氣罩的前壁；但同一步把**頂線
 * 也憑空抬了 0.045**，而艙頂那個位置實際上什麼都沒有。後壁（−0.489／
 * −0.479）同一個病。憑空多出來的那一圈，打光之下就是一段沒有理由的起伏。
 *
 * 【拆件的實證】`nacparts` 用五個 X 原點各打一次朝上／朝下的射線，判讀規則
 * 是「五欄一起動 → 那是機翼；只有內側幾欄凸出去 → 那是掛在艙上的罩子」。
 * `nacsection` 再把整條截面輪廓逐點印出來定寬與定中心。結論：
 *
 * ```
 *   主體    底線 −0.400 → −0.688 → −0.98，**全程單調平順、一道階都沒有**
 *   上方罩  機體 −2.16…−1.42，u −0.28…+0.24（對稱於艙心），高出錐面 0.12
 *   下方罩  機體 −1.26…−0.46，u −0.45…+0.62，掛在錐面下 0.24，前壁 0.31
 * ```
 *
 * 下方那個不是「一顆小凸起」，是**整片艙腹往下讓出一個平底槽**（散熱器
 * 浴缸）。所以它做成 halfWidth 0.63 的獨立 loft，上緣埋進錐體裡。
 *
 * 【主體從 −2.590 起，不要再往前】−3.089…−2.629 那一段射線量到的是**整流罩
 * 與槳葉**，而整流罩另外畫。把那幾站收進來等於同一段錐體畫兩次：艙的 loft
 * 會比整流罩粗，在槳轂根部圍出一圈突出的領子。現在整流罩底座（−2.560）埋
 * 在主體裡，第一站的端面封蓋正好是唇環。
 *
 * 【`propeller` 的 `noseZ` 是底座不是尖端】當成尖端傳的話整具整流罩會前伸
 * 0.55 m —— 見 `h.propeller` 那一段。
 *
 * ── 一個量不到、手工接的地方（坑 15）──────────────────────
 *
 * 【艙尾在 2.11 收乾淨】`nactail` 那一格用**三個射線原點**分辨過：
 *
 * ```
 *   機體Z   x=2.0   x=2.6   x=3.2   判讀
 *    1.71   0.636   0.768   0.497   ← 艙（2.6 明顯深）
 *    1.81   0.624   0.735   0.486   三欄開始收斂
 *    2.41   0.541   0.495   0.416   ← 機翼下表面，不是艙
 * ```
 *
 * 艙只在 x ≈ 2.6 附近深，機翼是一整片 —— 所以艙在 2.1 附近收掉，不要跟著
 * 機翼一路拖下去。
 *
 * 【艙寬在機翼覆蓋的那一段量不到】艙與機翼在參考模型裡是**同一個實體**，
 * 中間沒有面可以打。y = 0 的射線會穿出艙、打到機翼的表面（實測某一站回
 * 1.087，鄰站是 0.66／0.69）。烘焙時「外側比內側大 0.20 以上就只採內側」，
 * 但這一條沒有辦法用更好的量法解決，只能標出來。
 *
 * 【為什麼是 LoftPart 而不是 Blister】`Blister` 是「貼在主體上的一小塊」
 * （進氣口、排氣管），尺度是 0.1～0.5 m。發動機艙長 5.2 m、直徑 1.3 m，是
 * 主要結構 —— 用盒子做出來會是一根方棍。
 */
const NACELLE: LoftPart = {
  /**
   * 【2.2 → 2.4】截面比橢圓再方一點。實證：機體 −1.44 那一刀的底線，離艙心
   * 0.15／0.30／0.45／0.60 分別只比龍骨高 0.016／0.047／0.117／0.205 —— 等效
   * 曲率半徑 0.88，遠大於半高 0.54。橢圓（2.0）的艙底會太尖。
   */
  roundness: 2.4,
  /**
   * 【12 → 20】機身現在是 30 邊形（16 點的半圈），而艙還是 12 邊形 ——
   * 直徑 1.3 m 的艙用 12 段等於 30° 一個面，稜線在近距離很明顯。
   */
  segments: 20,
  // 艙首不封：整流罩與艙首之間那一圈要是黑的，見 NAC_COWL_DARK
  caps: { front: false },
  /**
   * halfWidth 直接用烘出來的（那一欄是乾淨的），halfHeight 與
   * centerY 由**量到的頂線與底線**反推：hh = (頂−底)/2、cy = (頂+底)/2。
   * 兩條線都在 `nacparts` 那一格，兩個罩子的區段從錐面平順內插補上。
   */
  sections: [
    // 唇環：端面封蓋 = 整流罩底座外面那一圈朝前的環（底座在 −2.560、半徑 0.32）
    { z: -2.590, halfWidth: 0.375, halfHeight: 0.350, centerY: -0.020 },
    { z: -2.490, halfWidth: 0.446, halfHeight: 0.422, centerY: -0.041 },
    { z: -2.390, halfWidth: 0.473, halfHeight: 0.458, centerY: -0.049 },
    { z: -2.239, halfWidth: 0.513, halfHeight: 0.477, centerY: -0.071 },
    { z: -2.039, halfWidth: 0.553, halfHeight: 0.504, centerY: -0.084 },
    { z: -1.839, halfWidth: 0.581, halfHeight: 0.534, centerY: -0.092 },
    { z: -1.639, halfWidth: 0.601, halfHeight: 0.556, centerY: -0.100 },
    { z: -1.439, halfWidth: 0.612, halfHeight: 0.574, centerY: -0.108 },
    { z: -1.239, halfWidth: 0.617, halfHeight: 0.593, centerY: -0.112 },
    { z: -1.039, halfWidth: 0.622, halfHeight: 0.608, centerY: -0.129 },
    { z: -0.839, halfWidth: 0.623, halfHeight: 0.622, centerY: -0.153 },
    { z: -0.639, halfWidth: 0.634, halfHeight: 0.637, centerY: -0.177 },
    { z: -0.439, halfWidth: 0.648, halfHeight: 0.652, centerY: -0.202 },
    { z: -0.239, halfWidth: 0.648, halfHeight: 0.659, centerY: -0.227 },
    { z: -0.039, halfWidth: 0.630, halfHeight: 0.669, centerY: -0.258 },
    { z: 0.161, halfWidth: 0.604, halfHeight: 0.674, centerY: -0.286 },
    { z: 0.361, halfWidth: 0.575, halfHeight: 0.669, centerY: -0.307 },
    { z: 0.561, halfWidth: 0.543, halfHeight: 0.655, centerY: -0.322 },
    { z: 0.761, halfWidth: 0.506, halfHeight: 0.633, centerY: -0.331 },
    { z: 0.961, halfWidth: 0.464, halfHeight: 0.603, centerY: -0.337 },
    { z: 1.161, halfWidth: 0.414, halfHeight: 0.569, centerY: -0.337 },
    { z: 1.361, halfWidth: 0.352, halfHeight: 0.532, centerY: -0.331 },
    { z: 1.561, halfWidth: 0.279, halfHeight: 0.492, centerY: -0.319 },
    { z: 2.110, halfWidth: 0.060, halfHeight: 0.221, centerY: -0.319 },
  ],
}

/**
 * 下方進氣罩（散熱器浴缸）。**獨立零件**，與 `NACELLE` 同一個 X 位移。
 *
 * 【垂直的前後壁不用配對截面】`buildFuselage` 對首尾兩站各生成一片朝外的
 * 封蓋。首站給滿尺寸的截面，端面就是一片垂直牆 —— 坑 10 的「兩站相距
 * 0.01 m」是為了**中段**的垂直面，端點本來就有。
 *
 * 【龍骨走勢】−0.985 →（−0.890 處最深 −1.012）→ −0.938，一條平順的淺弧。
 * 上緣固定在龍骨 +0.49，比錐體的底線（−0.69…−0.85）高出 0.20 以上，整條
 * 埋在錐體裡不會有縫。
 *
 * 【為什麼比錐體還寬】它在 y ≈ −0.77 那個高度是 1.26 m 寬，而錐體在同高度
 * 只有 0.6 m —— 實測 −0.89 那一刀，離艙心 0.60 的地方蒙皮在 −0.880，錐面
 * 在那裡是 −0.62。浴缸的側壁本來就凸在錐體外面，這是真的形狀。
 */
const NAC_INTAKE_LOW: LoftPart = {
  roundness: 2.6,
  segments: 20,
  // 前後都不封：兩端各是一個碗，見 NAC_INTAKE_LOW_DARK／NAC_INTAKE_LOW_EXIT
  caps: { front: false, back: false },
  sections: [
    { z: -1.270, halfWidth: 0.630, halfHeight: 0.245, centerY: -0.740 },
    { z: -1.190, halfWidth: 0.630, halfHeight: 0.245, centerY: -0.747 },
    { z: -1.090, halfWidth: 0.630, halfHeight: 0.245, centerY: -0.758 },
    { z: -0.990, halfWidth: 0.630, halfHeight: 0.245, centerY: -0.765 },
    { z: -0.890, halfWidth: 0.630, halfHeight: 0.245, centerY: -0.767 },
    { z: -0.790, halfWidth: 0.630, halfHeight: 0.245, centerY: -0.763 },
    { z: -0.690, halfWidth: 0.630, halfHeight: 0.245, centerY: -0.749 },
    { z: -0.590, halfWidth: 0.630, halfHeight: 0.245, centerY: -0.728 },
    { z: -0.490, halfWidth: 0.630, halfHeight: 0.245, centerY: -0.702 },
    { z: -0.455, halfWidth: 0.630, halfHeight: 0.245, centerY: -0.693 },
  ],
}

/**
 * 上方進氣罩（增壓器進氣口）。**獨立零件**，與 `NACELLE` 同一個 X 位移。
 *
 * 【中心確實在艙心】`nacsection` 逐點印機體 −1.89 那一刀的上緣：罩子由
 * u = −0.28 起、到 u = +0.24 止，中點 −0.02。真機 Jumo 211 的進氣口偏一側，
 * 但這個參考模型上是對稱的 —— 這正是為什麼要印整條輪廓而不是只掃外側。
 *
 * 【下緣 0.30 是埋進去用的】罩子只有頂上那 0.12 露在錐面外，下緣壓進錐體
 * 裡 0.10 以上。半寬 0.26 處錐面在 0.42，所以 0.30 兩側都埋得住。
 */
const NAC_INTAKE_TOP: LoftPart = {
  roundness: 2.6,
  segments: 16,
  caps: { front: false },
  sections: [
    { z: -2.180, halfWidth: 0.240, halfHeight: 0.106, centerY: 0.406 },
    { z: -2.090, halfWidth: 0.250, halfHeight: 0.109, centerY: 0.409 },
    { z: -1.990, halfWidth: 0.258, halfHeight: 0.111, centerY: 0.411 },
    { z: -1.890, halfWidth: 0.262, halfHeight: 0.112, centerY: 0.412 },
    { z: -1.790, halfWidth: 0.260, halfHeight: 0.111, centerY: 0.411 },
    { z: -1.690, halfWidth: 0.252, halfHeight: 0.108, centerY: 0.408 },
    { z: -1.590, halfWidth: 0.243, halfHeight: 0.104, centerY: 0.404 },
    { z: -1.490, halfWidth: 0.228, halfHeight: 0.096, centerY: 0.396 },
    { z: -1.420, halfWidth: 0.213, halfHeight: 0.089, centerY: 0.389 },
    // 尾端這三站是**埋進錐體裡收掉的**，不是量到的形狀。罩子量到的終點在
    // −1.42；若就此打住，尾端封蓋會在錐面上留一片外露的三角楔（渲染看得到）。
    // 錐頂在 −1.34／−1.24／−1.14 分別是 0.472／0.481／0.480，所以這三站的
    // 頂緣壓在它下面 0.02～0.08，封蓋整片藏起來，外觀正好收在 −1.42。
    { z: -1.340, halfWidth: 0.190, halfHeight: 0.078, centerY: 0.377 },
    { z: -1.240, halfWidth: 0.160, halfHeight: 0.065, centerY: 0.365 },
    { z: -1.140, halfWidth: 0.120, halfHeight: 0.053, centerY: 0.352 },
  ],
}

/**
 * 兩個進氣口裡面的**碗狀凹槽** —— 進氣口內要是黑的，而且是碗（內壁黑），
 * 不是一片擋板。
 *
 * ── 為什麼不能做成塞子 ───────────────────────────────────
 *
 * 罩子是封閉的 loft，`buildFuselage` 會替首站生成一片朝前的封蓋 —— 一面
 * **機身色的平板**。在它前面塞一根縮小一號的暗色管子的話，看到的是「機身色
 * 的環 + 暗管的封蓋」，兩個毛病：
 *
 *   側面內壁不見了
 *        塞子只有正面（封蓋）看得到，那就是底面；側壁的法線朝外，從凹槽
 *        裡面看是背面，被剔除 —— 等於沒有內壁。
 *   進氣口與內壁沒有連上
 *        塞子比罩口小 0.10，中間那一圈是空的。斜著看得到罩子的內側，而那
 *        也是背面 —— 一路穿出去。
 *
 * ── 現在的做法：真的把碗挖出來 ───────────────────────────
 *
 *   罩子    `caps: { front: false }` —— 罩口是**開的**，不再有那片平板
 *   內壁    朝前開口的暗色管子，材質 `darkInner`（暗色 + 雙面）。從開口看
 *           進去看到的是它的內側，雙面才畫得出來。
 *   接合    內壁在罩口那一站比罩子**大 2%**、而且往前多伸 0.006 ——
 *           進氣口本身不動，只擴大內壁。多出來的那一圈從外面看就是進氣口
 *           的**厚度**。
 *
 * 【剖面沿用罩子的】內壁與罩子同 `roundness`、同 `segments`，等比放大，
 * 兩者的邊才會逐點對齊，不會在罩口出現鋸齒。
 */
const NAC_INTAKE_LOW_DARK: LoftPart = {
  roundness: 2.6,
  segments: 20,
  caps: { front: false },
  sections: [
    { z: -1.276, halfWidth: 0.643, halfHeight: 0.250, centerY: -0.740 },
    { z: -1.190, halfWidth: 0.600, halfHeight: 0.232, centerY: -0.749 },
    { z: -1.090, halfWidth: 0.520, halfHeight: 0.200, centerY: -0.760 },
    { z: -1.010, halfWidth: 0.380, halfHeight: 0.145, centerY: -0.768 },
    { z: -0.960, halfWidth: 0.150, halfHeight: 0.060, centerY: -0.772 },
  ],
}

const NAC_INTAKE_TOP_DARK: LoftPart = {
  roundness: 2.6,
  segments: 16,
  caps: { front: false },
  sections: [
    { z: -2.186, halfWidth: 0.245, halfHeight: 0.108, centerY: 0.406 },
    { z: -2.120, halfWidth: 0.228, halfHeight: 0.100, centerY: 0.408 },
    { z: -2.050, halfWidth: 0.190, halfHeight: 0.082, centerY: 0.410 },
    { z: -2.000, halfWidth: 0.120, halfHeight: 0.052, centerY: 0.410 },
    { z: -1.970, halfWidth: 0.050, halfHeight: 0.022, centerY: 0.410 },
  ],
}

/**
 * 散熱器的**出口**。
 *
 * 進氣口是一根管子，前面挖了碗、後面卻還是一片機身色的平板 —— 從後方看
 * 就是「一道與蒙皮同色的橫牆」，而真機那裡是散熱器的出風口，是黑的。
 *
 * 做法與前面的碗完全對稱：罩子的後端也不封（`caps.back: false`），這一根
 * 朝**後**開口、比罩尾大 2%。前後兩個碗的封蓋把管子中段封死，所以不會從
 * 前面看穿到後面。
 */
const NAC_INTAKE_LOW_EXIT: LoftPart = {
  roundness: 2.6,
  segments: 20,
  caps: { back: false },
  sections: [
    { z: -0.620, halfWidth: 0.150, halfHeight: 0.058, centerY: -0.722 },
    { z: -0.560, halfWidth: 0.380, halfHeight: 0.148, centerY: -0.713 },
    { z: -0.505, halfWidth: 0.560, halfHeight: 0.218, centerY: -0.703 },
    { z: -0.453, halfWidth: 0.643, halfHeight: 0.250, centerY: -0.693 },
  ],
}

/**
 * 整流罩與艙首之間那一圈。
 *
 * 【那裡是什麼】整流罩是半徑 0.32 的圓錐（−3.090…−2.560），艙首在
 * −2.590 是 0.375 × 0.350 —— 兩者互相穿插，其實沒有破洞。露出來的是艙首
 * 那片**朝前的機身色封蓋**，在整流罩四周圍出一圈同色的環。真機那裡是
 * 整流罩與發動機罩之間的縫，是暗的。
 *
 * 所以艙首改成不封，前面補一個朝前開口的暗碗；整流罩的底座（−2.560）落在
 * 碗裡面，碗自己在 −2.440 收口封死。
 */

const NAC_COWL_DARK: LoftPart = {
  roundness: 2.4,
  segments: 20,
  caps: { front: false },
  sections: [
    { z: -2.592, halfWidth: 0.383, halfHeight: 0.357, centerY: -0.020 },
    { z: -2.520, halfWidth: 0.356, halfHeight: 0.333, centerY: -0.024 },
    { z: -2.470, halfWidth: 0.281, halfHeight: 0.263, centerY: -0.028 },
    { z: -2.440, halfWidth: 0.131, halfHeight: 0.123, centerY: -0.030 },
  ],
}

/** 發動機艙的中心。量測值：`wing` 那一趟的翼厚在 X 2.2～3.2 由 0.9 暴增到 1.54，峰值 2.6 */
const NACELLE_X = 2.6

/**
 * 機腹吊艙（Bola）—— **獨立零件，整個用玻璃材質**。
 *
 * 【為什麼不能烘進機身外殼】外殼一圈只有 16 點、相鄰差 12°。吊艙的半寬只有
 * 0.32 而它掛在 r ≈ 1.5 的地方 —— 偏 12° 的那一點橫向已經 0.31，**早就在
 * 吊艙外面**。硬烘進來做出的是一根沿中線的尖刺，不是一顆圓凸起。
 *
 * 這與發動機艙是同一個判斷：**尺度夠大、又不在剖面取樣解析度之內的東西，
 * 要另外做成零件。**
 *
 * ── 量它的時候不可以把射線原點放在吊艙裡面 ─────────────────
 *
 * 原點放在 (0, −1.05) 朝下打的話，`castRay` 取的是 maxRadius 之內**最遠**
 * 的一次命中 —— 射線穿過艙底之後還會走 0.9 m，而**中線上掛著兩根細桿**
 * （參考模型的實體渲染看得到，一根長的在前、一根短的在後）。量到的
 * 「艙底」會從頭到尾是桿子的下端，而深度、寬度、尾段三個一起錯。
 *
 * 那張表自己就露了餡：半寬 0 的那幾站（量測 5.5～5.8、8.1～8.4）艙底仍然讀得
 * 出 −1.09…−1.29，而且與艙身接成一條**連續的 V**。真正的艙在那裡不存在，
 * 能連成一條線的只有一路貫穿的東西。**這是「最遠命中 ＋ 中線上有細長的東西」
 * 這個坑的第三次**（前兩次是機背開口被天線鋼索騙、垂尾被量成懸空）。
 *
 * 新的量法：原點放在機身**裡面**（y = +0.30），朝下打，maxRadius 2.0，而且
 * 原點沿 X 橫掃 0.10…0.60 —— 起點一定在實體內，最遠命中就是該站的最低外
 * 表面；橫移 0.10 就打不到桿子了。
 *
 * ── 三個定義 ─────────────────────────────────────────────
 *
 * 【`centerY` 取半寬處的蒙皮高度】`loft` 產生的是一根管子，它在
 * |x| = halfWidth 的地方剛好是 `centerY`。把上緣釘在**中線的腹線**上的話
 * centerY ≈ −1.04 而該處的機身蒙皮在 −0.78 —— 管子的肩膀整條露在機身外面，
 * 側視是一道從頭到尾的稜線，看起來像「機腹玻璃外推太多」。取半寬處之後
 * 肩膀埋進機身，露出來的只有下半。
 *
 * 【寬度是「朝下的射線比同一個 x 的蒙皮低 0.03 的最外側 x」】從艙裡往兩側
 * 打量到的是**中段半寬**，不是最寬處（0.32 對 0.47）。
 *
 * 【尾段是平順收上去的，不是一刀切】逐站對切（`belly`，已扣掉 −0.105 的
 * 剛體位移）：Z 4.80 參考還在 −1.154、5.00 還有 −1.010，一刀切的版本在
 * 4.80 就收起來，短了 0.4 m。收尖的兩站寬度與高度一起收 —— 只收高度會在
 * 尾端留下一片寬扁的楔子。
 *
 * 【中段的深度不要再加深】Z 3.4～4.6 這一段比參考**淺** 0.03～0.05，
 * 「外推」的觀感來自肩膀露出來，不是艙太深。
 *
 * 【roundness 2.45 是反解的，不是猜的】超橢圓指數對「半寬一半處的深度比」
 * 做最小平方，25 個樣本 RMS 0.069。2.0（正橢圓）底部太圓。
 */
const BOLA: LoftPart = {
  roundness: 2.45,
  /**
   * 【14 → 20】前後兩片窗要靠 `arc` 從這一圈上切出來，而 `arc` 只能切在
   * **頂點之間**。14 段等於一格 25.7°，量到的 215°…325° 會被吃成 231°…308°；
   * 20 段一格 18°，216°…324° 剛好落在格線上。
   */
  segments: 20,
  sections: [
    { z: 2.511, halfWidth: 0.040, halfHeight: 0.040, centerY: -0.870 },
    { z: 2.611, halfWidth: 0.103, halfHeight: 0.031, centerY: -0.870 },
    { z: 2.711, halfWidth: 0.283, halfHeight: 0.124, centerY: -0.842 },
    { z: 2.811, halfWidth: 0.367, halfHeight: 0.226, centerY: -0.805 },
    { z: 2.911, halfWidth: 0.416, halfHeight: 0.319, centerY: -0.773 },
    { z: 3.011, halfWidth: 0.443, halfHeight: 0.394, centerY: -0.754 },
    { z: 3.111, halfWidth: 0.456, halfHeight: 0.447, centerY: -0.744 },
    { z: 3.211, halfWidth: 0.462, halfHeight: 0.478, centerY: -0.738 },
    { z: 3.311, halfWidth: 0.466, halfHeight: 0.497, centerY: -0.734 },
    { z: 3.411, halfWidth: 0.467, halfHeight: 0.508, centerY: -0.730 },
    { z: 3.511, halfWidth: 0.466, halfHeight: 0.516, centerY: -0.728 },
    { z: 3.611, halfWidth: 0.465, halfHeight: 0.522, centerY: -0.727 },
    { z: 3.711, halfWidth: 0.462, halfHeight: 0.528, centerY: -0.726 },
    { z: 3.811, halfWidth: 0.459, halfHeight: 0.532, centerY: -0.726 },
    { z: 3.911, halfWidth: 0.455, halfHeight: 0.536, centerY: -0.726 },
    { z: 4.011, halfWidth: 0.450, halfHeight: 0.540, centerY: -0.726 },
    { z: 4.111, halfWidth: 0.447, halfHeight: 0.544, centerY: -0.727 },
    { z: 4.211, halfWidth: 0.443, halfHeight: 0.547, centerY: -0.727 },
    { z: 4.311, halfWidth: 0.440, halfHeight: 0.549, centerY: -0.728 },
    { z: 4.411, halfWidth: 0.437, halfHeight: 0.552, centerY: -0.728 },
    { z: 4.511, halfWidth: 0.433, halfHeight: 0.554, centerY: -0.729 },
    { z: 4.611, halfWidth: 0.430, halfHeight: 0.554, centerY: -0.730 },
    { z: 4.711, halfWidth: 0.426, halfHeight: 0.503, centerY: -0.730 },
    { z: 4.811, halfWidth: 0.423, halfHeight: 0.432, centerY: -0.731 },
    { z: 4.911, halfWidth: 0.420, halfHeight: 0.345, centerY: -0.732 },
    { z: 5.011, halfWidth: 0.417, halfHeight: 0.252, centerY: -0.732 },
    { z: 5.111, halfWidth: 0.405, halfHeight: 0.165, centerY: -0.741 },
    { z: 5.211, halfWidth: 0.223, halfHeight: 0.082, centerY: -0.741 },
    { z: 5.311, halfWidth: 0.040, halfHeight: 0.030, centerY: -0.741 },
  ],
}


/**
 * 從 `BOLA` 的截面切出 [z0, z1] 那一段，並可整體縮放、指定角度範圍。
 *
 * 吊艙要拆成「蒙皮 + 兩片窗 + 兩片暗艙」共七塊，每一塊都是同一組截面的
 * 一段，只差在 z 範圍、比例、角度。逐塊手打表格會有七份會漂開的座標。
 */
function bolaSeg(z0: number, z1: number, o: {
  scale?: number
  arc?: { fromDeg: number; toDeg: number }
  caps?: { front?: boolean; back?: boolean }
} = {}): LoftPart {
  const k = o.scale ?? 1
  return {
    roundness: BOLA.roundness,
    segments: BOLA.segments,
    ...(o.arc ? { arc: o.arc } : {}),
    ...(o.caps ? { caps: o.caps } : {}),
    sections: BOLA.sections
      .filter((s) => s.z >= z0 - 1e-6 && s.z <= z1 + 1e-6)
      .map((s) => ({
        z: s.z,
        halfWidth: s.halfWidth * k,
        halfHeight: s.halfHeight * k,
        centerY: s.centerY,
      })),
  }
}

/**
 * 機腹吊艙拆成七塊。**參考模型量出來的：吊艙是金屬的，只有前後兩片窗。**
 *
 * ── 為什麼不是「整個凸起都是玻璃」 ───────────────────────
 *
 * `bolahole` 那一格同一組射線切兩次
 * （`only: 'hull'` 與 `only: 'windows'`），射線原點放在吊艙自己的剖面中心：
 *
 * ```
 *   機體Z        190° 215° 245° 270° 295° 325° 350°
 *   2.51…2.61     蒙  蒙   蒙   蒙   蒙   蒙   蒙
 *   2.71…3.21     蒙  玻   玻   玻   玻   玻   蒙   ← 前窗
 *   3.31…4.41     蒙  蒙   蒙   蒙   蒙   蒙   蒙   ← 中間是金屬
 *   4.51…5.51     玻  玻   玻   玻   玻   玻   玻   ← 後窗
 * ```
 *
 * 而且**吊艙的底就是 `hull` mesh 本身**（機體 3.41 的正下方打到蒙皮，
 * −1.241）—— 參考模型的機身外殼直接鼓成吊艙，沒有任何洞。
 *
 * 所以做法是前窗、中間機體、後窗三段。照片上讀起來像「整個凸起都是玻璃」，
 * 量下去不是。
 *
 * ── 七塊 ─────────────────────────────────────────────
 *
 *   蒙皮  首段（尖端，前端封口）／前窗那一段的**補角**／中段整圈／
 *         後窗那一段的補角
 *   玻璃  前窗 216°…324°、後窗 198°…342°，往外 0.4% 避免與蒙皮共面
 *   暗艙  兩片窗各一片，往內 8%，z 與角度都比窗各多一格 —— 從窗看進去
 *         看到的是它，而且斜著看也不會從窗緣繞過去
 *
 * 【角度為什麼要跨過 360】`arc` 的兩端是索引，超過 360 會繞回去。蒙皮要的是
 * 窗的**補角**（例如 324°→216°），寫成 `324…576` 就是繞過頂部那一段。
 */
const BOLA_PARTS: readonly { part: LoftPart; mat: 'body' | 'glass' | 'dark' }[] = [
  // 蒙皮：尖端 → 前窗前緣。前端封口（那一站已經收成 0.04 的點）
  { part: bolaSeg(2.511, 2.711, { caps: { back: false } }), mat: 'body' },
  // 蒙皮：前窗那一段的補角
  { part: bolaSeg(2.711, 3.211, { arc: { fromDeg: 324, toDeg: 576 } }), mat: 'body' },
  // 蒙皮：中段，整圈金屬
  { part: bolaSeg(3.211, 4.511, { caps: { front: false, back: false } }), mat: 'body' },
  // 蒙皮：後窗那一段的補角
  { part: bolaSeg(4.511, 5.311, { arc: { fromDeg: 342, toDeg: 558 } }), mat: 'body' },
  // 兩片窗的暗艙（先畫，玻璃蓋上去）
  { part: bolaSeg(2.611, 3.311, { scale: 0.92, arc: { fromDeg: 198, toDeg: 342 } }), mat: 'dark' },
  { part: bolaSeg(4.411, 5.311, { scale: 0.92, arc: { fromDeg: 180, toDeg: 360 } }), mat: 'dark' },
  // 兩片窗
  { part: bolaSeg(2.711, 3.211, { scale: 1.004, arc: { fromDeg: 216, toDeg: 324 } }), mat: 'glass' },
  { part: bolaSeg(4.511, 5.311, { scale: 1.004, arc: { fromDeg: 198, toDeg: 342 } }), mat: 'glass' },
]

/**
 * 全玻璃機首的後界 —— 機身外殼在這一站切成兩截。
 *
 * 【量出來的，不是照片上抓的】只切參考模型的 `windows` mesh，逐站看玻璃
 * 佔剖面的比例：
 *
 * ```
 *   機體Z    玻璃佔剖面   蒙皮腹底   玻璃腹底
 *   −3.09      93.1%       0.077      —（前端）
 *   −2.89      97.2%      −0.058     −0.058   ← 一模一樣
 *   −2.49      75.0%      −0.329     −0.329
 *   −2.09      62.5%      −0.491     −0.491
 *   −1.69      54.2%      −0.616      —
 *   −1.49       8.3%                          ← 玻璃結束
 * ```
 *
 * 玻璃的下緣**逐字等於蒙皮的下緣** —— 那一段機身沒有蒙皮，玻璃就是外殼。
 *
 * 【1.64 這一刀讓最後 0.25 m 略微過度玻璃化】−2.09 到 −1.69 的玻璃只佔
 * 剖面六成（底下三分之一是蒙皮），切在 −1.64 等於把那一段整圈當玻璃。
 * 換來的是「機首是透明的」這個第一眼特徵，而外殼分兩截是這個產生器唯一
 * 做得到「一段機身用另一種材質」的辦法。
 */
const GLASS_SPLIT_Z = -1.64

/**
 * 機首以外的三塊玻璃 —— 機背與機腹也有玻璃區域。
 *
 * 【`windows` mesh 要往後看完，不是只用來定機首玻璃的後界】`glass` 那一格
 * 逐站印出玻璃落在**第幾號輸出角**（索引與 `he111.hull.ts` 的十六點一致）：
 *
 * ```
 *   機體Z    0123456789012345
 *   +0.71    ##..............   機背機槍座（前緣）
 *   +1.11    ####.##.........   ← 機背 0~3，另外 5~6 是座艙側窗
 *   +1.51    ####............
 *   +1.91    ..##............   機背機槍座（後緣）
 *   +2.71    ..............##   機腹吊艙（Bola）起
 *   +3.11    .....###......##   ← 側窗 5~7 與 Bola 同時在
 *   +4.51    ............####
 *   +5.91    ..............##   Bola 止
 *   +6.11    ................
 * ```
 *
 * 【機背玻璃與機身那個「量不到的開口」不是同一段】玻璃在 0.71…1.91，而
 * 正上方射線穿進去的那五站在 1.91…2.91（見 `he111.hull.ts`）。真機的 H-6
 * 就是這樣：前面是有蓋的玻璃座、後面是機槍的開口。
 *
 * 【側窗 5~7 只取連續的那一段】3.11…4.31 之間逐站都在（6.80 那一站漏一格，
 * 射線角度的取樣問題）。更前面 0.11／0.31／0.91～1.31 也有零星的 5~6，
 * 但中間斷開 —— 硬連起來會做出一條 4 m 長的玻璃帶，那不是真機的樣子。
 *
 * ── 機背與機腹是**整個凸起都是玻璃**，而且都不凹 ──────────
 *
 * 【凸起從哪到哪：拿機身資料自己量】背線減掉不受影響的**肩線**（第 3 點）、
 * 腹線減掉**腰線**（第 12 點），超出遠場值的那一段就是凸起：
 *
 * ```
 *   機體z    背凸           腹凸
 *    0.21   0.297  基準    0.107  基準
 *    0.41   0.309  ← 起    0.106
 *    1.41   0.494  峰      0.121
 *    2.41   0.408          0.212
 *    3.01   0.250  ← 止    0.456  峰（投彈窗）
 *    3.41   0.229  遠場    0.400
 *    4.81   0.214          0.455  第二峰（機槍座）
 *    5.41   0.200          0.295  ← 止
 *    6.01   0.175          0.287  遠場
 * ```
 *
 * **玻璃要蓋滿整段凸起。** 機背只蓋 0.71…1.91（隆起的前半）的話後半是機身
 * 色，看起來是「圓頂做到一半」；機腹由 2.71 起算也會漏掉前面那一段。
 *
 * 【兩塊都不凹】它們是**凸出來的**，玻璃就是那塊凸起的外殼本身 —— 與全玻璃
 * 機首同理。沉下去等於把真機凸的東西做成凹的。凹槽只留給真正嵌在平面蒙皮上
 * 的機身側窗。
 *
 * 【與參考模型的 windows mesh 有一處不同】它在機腹只有第 14～15 點連續有
 * 玻璃，第 13 點僅在機體 4.51 那一站出現。這裡取 13～15，讓整個凸起都是
 * 玻璃。
 */
const GLASS_PATCHES: readonly GlassPatch[] = [
  // 機背機槍座的玻璃圓頂 —— 整個隆起，與蒙皮共面
  { from: 0.41, to: 3.01, i0: 0, i1: 3 },
  // 機身側窗 —— 嵌在平面蒙皮上，凹槽
  { from: 3.11, to: 4.31, i0: 5, i1: 7, recess: true },
]

/**
 * 玻璃的骨架。
 *
 * 【為什麼一定要有】一大片沒有分割線的透明曲面在遊戲距離下讀不出「那是
 * 玻璃」，只讀得出「那裡破了一個洞」。He 111 的機首在照片上最搶眼的就是
 * 那張金屬網格。
 *
 * 【站位取整數格】隔框放在機身環的整數站位上（0.2 m 一站），前後各留一格
 * 給尖端與接縫。桁條取剖面的第 0（正上方）、4（+42°）、8（−6°）、
 * 12（−54°）點 —— 上、肩、腰、腹各一條，左右鏡像（正上方那一條在中線上，
 * 只畫一次）。
 */
const FRAMES: FrameSpec = {
  hoops: [-2.99, -2.59, -2.19, -1.79, GLASS_SPLIT_Z],
  rails: [0, 4, 8, 12],
  from: -3.19, to: GLASS_SPLIT_Z,
  width: 0.024,
  out: 0.012,
}

/**
 * 機身色。
 *
 * 【為什麼是匯出的常數】零件（`render/debris.ts`）要用同一個值 —— 打爆的
 * 飛機掉下來的碎片必須跟機身同色。
 *
 * 【選色】德軍轟炸機的上表面是 RLM 70/71 的墨綠。取偏暗的灰綠，與 109 的
 * 淺灰藍分得開 —— 空戰裡「那是轟炸機還是戰鬥機」要一眼認得出來。
 */
export const HE111_BODY_COLOR = 0x5a6350

export function buildHe111(): AircraftModel {
  const h = createHull({
    bodyColor: HE111_BODY_COLOR,
    accentColor: 0x2b3128,
    realLength: 16.40,
    // 平移已經烘進 he111.hull.ts 的站位裡（量測系 −3.0889）
    offsetZ: 0,
    // 眼點：駕駛座在玻璃機首的後上方、**偏左**（真機的駕駛座偏左，
    // 機首機槍座偏右）。z −1.95 落在玻璃段的後段，y 0.85 在該站背線
    // （1.26）之下
    eyePoint: new Vector3(-0.25, 0.85, -1.95),
    // 右翼尖弦的中點，由 WING 推出：
    //   x = halfSpan                          = 11.300
    //   y = rootY + tan(6°) × 11.30           = −0.30 + 1.1876 = 0.888
    //   翼尖前緣 = kink.lead + tan(14°)×7.9    = −0.9829 + 1.9703 = 0.987
    //   翼尖弦 = tipChord × tipFactor(1, 0.45) = 2.296 × 0.45  = 1.033
    //   前緣  = 0.987 + (2.296 − 1.033)×TIP_ANCHOR             = 1.366
    //   z    = 前緣 + 弦/2                                     = 1.883
    wingTip: new Vector3(11.300, 0.888, 1.883),
  })

  // 全玻璃機首：外殼切成兩截，前段玻璃、後段機身色（見 GLASS_SPLIT_Z）
  h.glazedNose(HE111_HULL, GLASS_SPLIT_Z, GLASS_PATCHES)
  h.frames(HE111_HULL, FRAMES)

  // 兩具發動機艙。機身色而不是強調色 —— 它們是蒙皮的一部分。
  //
  // 【為什麼是位移 Mesh 而不是給截面一個 centerX】`FuselageSection` 只有
  // `centerY`（單發機的機身與導管都在中線上，橫向偏移從來不需要）。加一個
  // 欄位要動 `buildFuselage` 的內圈迴圈；`loft` 本來就回傳 Mesh，位移它
  // 是同一件事而且不碰共用的產生器。
  //
  // 【三個零件而不是一串截面】主體是平順的圓錐，兩個進氣罩各自獨立 ——
  // 壓進同一串截面就會整圈一起跳（見 NACELLE 的註解）。
  for (const sx of [1, -1]) {
    for (const part of [NACELLE, NAC_INTAKE_LOW, NAC_INTAKE_TOP]) {
      h.loft(part, h.body).position.x = sx * NACELLE_X
    }
    // 四個碗狀凹槽：整流罩接縫、上下進氣口、散熱器出口。材質是雙面的 ——
    // 從開口看進去看到的是內壁的背面
    for (const part of [
      NAC_COWL_DARK, NAC_INTAKE_LOW_DARK, NAC_INTAKE_LOW_EXIT, NAC_INTAKE_TOP_DARK,
    ]) {
      h.loft(part, h.darkInner).position.x = sx * NACELLE_X
    }
  }

  /**
   * 機腹吊艙 —— **金屬艙 + 前後兩片窗**，見 `BOLA_PARTS`。
   *
   * 【這些做法都不對】縮小的實心殼（兩次）、平天花板、上半圈的內面、下半圈
   * 的外面、機身挖洞 —— 前六次都在假設「整個吊艙是玻璃」，而參考模型的吊艙
   * 本來就是金屬的，只有兩片窗。**問題從頭到尾不在暗色怎麼補。**
   */
  for (const { part, mat } of BOLA_PARTS) {
    h.loft(part, mat === 'body' ? h.body : mat === 'glass' ? h.glass : h.dark)
  }

  h.wingPair(WING)
  // 翼根整流罩：只補後緣那一塊，前緣參考模型本來就沒有（見 WING_FILLET）
  h.wingPair(WING_FILLET)
  h.wingPair(TAILPLANE)
  // 主垂尾的頂不圓化（頂蓋接在上面）；頂蓋自己收圓
  h.upright(FIN, 0.16)
  h.upright(FIN_CAP, 0.14, 0.80)

  /**
   * 兩具螺旋槳。**`propeller` 可以呼叫多次** —— 若它每次呼叫都把
   * `setPropSpin` 整個換掉，雙發時左邊那具永遠不轉。
   *
   * 【`noseZ` 是整流罩的**底座**不是尖端】`assembly.propeller` 把圓錐擺在
   * `noseZ − spinnerLength / 2`、頂點朝 −Z —— 所以錐體佔 `noseZ − length`
   * 到 `noseZ`。傳 −3.089（尖端）的話整具整流罩落在
   * −3.639…−3.089，**比參考模型的位置整整前伸 0.55 m**；艙的 loft 再從
   * −3.089 自己長一個錐尖接上去，所以接縫看不出來，只是引擎前端長了半公尺。
   *
   * 量到的：下緣在 −3.04 首次命中 0.117、到 −2.59 長到 0.327，−2.54 跳到
   * 0.405（那已經是艙）。所以整流罩是 **−3.09 尖、−2.56 底、半徑 0.32**。
   *
   * 【與艙的接法】艙的第一站在 −2.590，比底座前 0.03 —— 底座埋進艙裡，艙的
   * 端面封蓋就是唇環（0.375 − 0.30 ≈ 0.07 寬），與參考模型一致。
   *
   * 【槳盤半徑 1.75】真機每具 VDM 三葉槳直徑 3.50 m。注意 `specs/he111.ts`
   * 的 `prop.diameter` 是 4.95 —— 那是**等效單槳盤**，給動量理論用的，
   * 與這裡的視覺尺寸是兩件事（推導見那一支的檔頭）。
   */
  for (const sx of [1, -1]) {
    h.propeller({
      x: sx * NACELLE_X, noseZ: -2.560,
      spinnerRadius: 0.32, spinnerLength: 0.53, spinnerY: -0.01,
      blades: 3, propZ: -2.64, propRadius: 1.75,
    }, HE111_HULL[0]!.z)
  }

  return h.finish()
}
