# AI 能量紀律與機種打法：讓 AI 知道自己的甜蜜區

**日期** 2026-08-11
**狀態** 設計，待專案負責人審核
**前置** `2026-08-11-turn-radius-record.md`（本設計的動機全部來自那一輪的量測）

---

## 1. 要解決什麼

兩個問題，同一個根。

### 1.1 AI 會把自己拉爆

2026-08-11 拿掉 `limiters.ts` 的飛行員過載硬夾之後（6.5 G → 結構極限 8 / 7.5 G），
AI 在破防時會直接要到 8 G，誘導阻力暴增、速度崩掉，於是閃不掉。實測：

```
  ai-defence 正後方 400 m      挨打 0.084 s → 0.518 s（惡化 6 倍）
  ai-visible-evasion 700 m     中彈率 0.130（門檻 0.08）
  ai-duel-matrix 高能量開局     3213 m 的先手 → 200 秒僵局，紅 34 : 藍 42
```

專案負責人裁定「AI 拉爆自己這件事本身合理，接受」，四條護欄已按此重新定值
（局部例外，見前置文件）。**但那個裁定接受的是「物理上合理」，不是「這樣好玩」**
—— 一個好的飛行員會管理能量，現在的 AI 是菜鳥。

### 1.2 兩台飛機打起來一模一樣

`grep spec.id` 掃遍 `src/ai/`、`src/control/` —— 零命中。AI 不知道自己開哪一台。
唯一的機體比較 `airframeTurnAdvantage` 用的是**速度無關**的
`bestSustainedTurnRateCached`，而兩台在 4000 m 是 29.5 對 29.3°/s，
差 0.0035 rad/s，決策門檻 `turnEnter` 是 0.02 —— **差距比門檻小 6 倍**。

`rules.ts` 的 `turnEnter` 註解自己記著這件事：

> 【對目前兩台等於休眠，這是預期中的】…這兩台本來就旗鼓相當…
> 門檻是為了**日後加的機種**而存在。

**這個判斷在當時是對的，現在不對了。** 兩台的差異真實存在，只是不在那個軸上：

```
  4000 m 的分水嶺 420 km/h
    以下   109 迴旋 +1.2°/s、比余功率 +2.0 m/s
    以上   P-51 全面佔優，而且差距爆炸性擴大
    700 km/h 時 P-51 滾轉快 2.7 倍（154 對 57°/s）
```

**架構不是壞掉，是量錯了軸。** 目標不是推翻湧現，是補上一把分辨得出來的尺。

### 1.3 目標

專案負責人的兩句話：

- 「AI 會用不同的策略開飛機」
- 「應該是讓 AI 知道說，偏好的選擇？」——偏好的對象是**速度帶 + 高度帶**

---

## 2. 量測：甜蜜區長什麼樣

`test/tools/advantage-map.probe.ts`，套 `GAME_FEEL`，正 = P-51 佔優。

### 2.1 持續迴旋率（°/s）

```
高度\速度   300    350    400    450    500    550
   0 m    -1.5    0.8    1.9    3.9    8.4   13.8
2000 m    -1.3    1.4    3.7    4.6    6.5   12.5
4000 m    -1.0   -1.2    1.1    2.3    4.1    7.1
6000 m    -0.8   -1.0   -0.2    0.2    0.9    2.0
```

### 2.2 1G 比余功率（m/s）

```
高度\速度   300    350    400    450    500    550
   0 m    -2.5   -1.1    1.3    4.8    9.6   15.7
2000 m     3.4    4.0    5.3    7.4   10.3   14.2   ← 全列為正
4000 m    -3.3   -2.0    0.1    3.1    7.2   12.4
6000 m    -3.7   -3.7   -3.0   -1.6    0.5    3.3
```

**2000 m 整列為正**是 P-51 一級增壓器的甜蜜點 —— 它的框不是單純的「高而快」，
低空有一個鼓包。這種非直覺的形狀正是「不要手寫框」的理由。

### 2.3 翻轉速度隨高度非單調

```
     0 m  321 km/h      6000 m  427 km/h
  2000 m  343 km/h      8000 m  392 km/h
  4000 m  366 km/h     10000 m  385 km/h
```

6000 m 是 109 窗口最寬的地方。

### 2.4 **關鍵發現：滾轉率不能進甜蜜區**

三個量的正負號只有 **31/48（65%）** 一致。不一致的 17 格裡 14 格是同一個模式：

```
  低速 300–350   迴旋 109 優、能量 109 優、滾轉 P-51 優
```

**P-51 的滾轉在任何速度、任何高度都贏**（連 300 km/h 都 +2.3°/s）。合成一個
「優勢分數」會把 109 的低速優勢洗掉，兩台又變成一樣。

滾轉是另一類東西：它不是「我在哪裡打得好」，是「我能贏哪一種動作」。
本設計**不使用**它；它屬於未來的機動層（`steer.ts` 的反轉是唯一的既有消費端）。

### 2.5 量測本身的兩個瑕疵（引用時注意）

- `sustainedTurnRate` 撐不住時回傳 0，所以海平面 600–650 那兩格的「差 0.0」
  是**兩台都撐不住**，不是打平
- 探針的 4G 表在 4000 m/300 出現 `-Infinity`，是把過載夾在剛好等於 CLmax 的
  邊界上造成的數值假象，那一列不採信

---

## 3. 架構：兩個分開的單元

量測直接指出架構。**兩者的定義域不同**，這是分開的硬理由。

### 3.1 `sweetSpot` —— 甜蜜區（相對，需要對手）

「在這個 (高度, 速度) 我打不打得贏他。」

- **輸入** `(selfSpec, targetSpec, altitude, tas)`
- **輸出**
  - `advantage`：優勢純量，**單位 rad/s**（持續迴旋率之差），正 = 我佔優。
    用既有單位而不是無因次分數，理由是它能直接與 `turnEnter`（0.02 rad/s）
    這個既有門檻比較 —— 同一把尺，不必再發明一個標度
  - `pitchBias`：建議的**航跡角偏置**，rad。正 = 該抬頭（換高度）、
    負 = 該低頭（換速度）

**【原稿的 `dTas` / `dAlt` 兩個方向量在撰寫計畫時被合併成一個】**
俯仰只能在高度與速度之間**交換**，不能同時增加兩者 —— 同時增加是油門的事，
而 AI 平常就在 WEP。所以「該加速」與「該爬」不是兩個獨立方向，是同一根桿的
兩端，硬拆成兩個量會在「又低又慢」時互相打架而無解。

還有一個硬性理由：見 4.3 的方位不變量 —— 偏置**必須**表達成俯仰，
任意的 3D 偏移會被指揮儀讀成滾轉需求。

偏置的大小由表格上的梯度求得（往優勢上升最快的方向投影到俯仰軸），
不是手寫規則。
- **用迴旋 + 能量，不含滾轉**（見 2.4）
- **純函數**，快取每個「機種對」一張表，首次使用一次填滿、格間線性內插
  —— 沿用 `envelope.ts` 的 `bestTurnTable` 既有作法（WeakMap、避免逐格填充
  造成的 AI 步 p999 惡化）
- **鏡像對戰自動歸零**：P-51 對 P-51 時優勢處處為 0，沒有偏好，行為與今天
  相同。這是正確的，不是缺陷

### 3.2 `gDiscipline` —— 拉桿紀律（絕對，不需要對手）

「不要把自己拉到掉出可用包絡。」

- **輸入** `(selfSpec, altitude, tas)` 與當前的比能量狀態
- **輸出** 此刻允許的最大轉彎率
- 判準用飛機自己的量（角落速度、可持續轉彎率），與 `steer.ts` 既有的
  `cornerRatio` 同一族 —— 全是比值，分母是飛機自己的性能，所以手感倍率
  一動分母同步跟著走（`feel.ts` 記載的自我校正性質）

### 3.3 為什麼不合成一個

鏡像對戰時甜蜜區處處為 0。若把拉桿紀律綁在甜蜜區上，**同機種對打會完全
失去紀律** —— 而 `ai-duel-matrix` 留紅的那一場正是 P-51 對 P-51。

---

## 4. 資料流

現行節拍：

```
assess（看態勢）→ rules.arbitrate（決定意圖）→ steer（怎麼飛）→ safety（撞海保命）
```

**兩個新量都算在 `assess`，消費在 `steer`。本期完全不動 `rules.arbitrate`。**

### 4.1 為什麼不動意圖仲裁

`arbitrate` 是一串經過裁決的閂鎖與優先序，是整個 AI 最脆弱的地方。而且如果
甜蜜區有效，打法會**自己浮現**：P-51 為了回到高而快的框，自然變成爬走、
俯衝、打一次再爬走；109 待在低而慢的框裡，自然變成留下來繞圈。
**不需要一張「誰該用什麼打法」的表。**

若量測顯示浮現不足，再談要不要進意圖層 —— 那是下一期的決定。

### 4.2 拉桿紀律的消費點

`steer` 在下達期望轉彎率時取 `min(既有需求, gDiscipline 的上限)`。

**這個限制必須放在 AI 層，不放進 `control/limiters.ts`。** 飛行模型是玩家與
AI 共用的；放進去等於再次把玩家夾住，而 2026-08-11 才剛刻意拆掉一個夾子。
**玩家要拉爆自己是他的自由，AI 不該。**

### 4.3 甜蜜區的消費點

`steer` 把方向建議當成**瞄準點的偏移**：太慢就把瞄準點壓低（換速度），
太低就抬高（換高度）。

**是偏移不是覆寫。** 它不搶走目前的意圖 —— 追擊仍然是追擊，只是路徑偏向
自己舒服的高度與速度。偏移量要有上界，否則會與意圖打架。

#### 硬性不變量：方位絕對不能動

`steerCommand` 的結構是「先算瞄準點，再套一疊**不改方位**的後處理」：

```
  算 aimWorld（依 mode / intent）
    → shrinkTowardNose(unloadPull(stallMargin))   少拉一點，方位不動
    → applyFloor(floorPitchAngle(離地餘裕))        抬航跡角，方位不動、只抬不壓
```

兩個既有後處理的註解都記著同一個教訓：舊版用 `unloadAim(self, 0)` 表達卸載，
在橫向產生約 14° 的偏移，**指揮儀讀成轉向需求 —— 滾轉指令由 2–3° 暴增到
27–29°、副翼打到滿舵**。

**本設計的兩個單元就是這一疊的另外兩層：**

- **拉桿紀律 = 再一個 `shrinkTowardNose`**，觸發條件由「快失速」換成
  「快沒能量」。既有的 `unload` 其實已經是一種拉桿紀律 —— 它防的是失速，
  新的防的是能量。兩者取較小的拉桿係數即可，共用同一個呼叫點
- **甜蜜區 = 一個能上下的 `applyFloor`**（`applyFloor` 只抬不壓，甜蜜區需要
  雙向，所以是它的姊妹函式而不是它本身）

**不新增機制** —— 兩者都復用既有、已經吃過教訓的原語。這也讓「方位不動」
這件事天然成立，不必再驗一次。

**上界的定值方法**：掃描偏移上界（以瞄準點的角度偏移表示，候選 5° / 10° /
15° / 20°），主判準是 5.2 的四筆能回幾筆，護欄是 `ai-targeting` 的
`onNose`（瞄準點是否還指著敵人）與 `ai-manoeuvre` 既有五條指標不得退化。
偏移過大時 AI 會為了顧自己的框而放掉射擊解 —— 那個症狀會先出現在 `onNose`。

### 4.4 與指揮層的位階關係

**這一節是專案負責人在設計審核時提出的，不是原稿有的。**

指揮層（`ai/command.ts`、`rally.ts`、`AiController` 裡的兩行覆寫）會下三種
命令。它對本設計的干涉點如下：

```
  有命令且沒目標   rallyCommand 直接寫 aimWorld，然後 return —— 整條 steer 都跳過
  有命令且有目標   intent 被強制設成 'rally'（除非 defendLatch）
  僚機站位        stationCommand 直接寫，也是 return
```

`rules.ts` 的註解寫得很清楚：**`rally` 不由 `arbitrate` 產生，它是指揮層的
外部覆寫。**

#### 兩個真實的衝突

**一、集合／側翼途中的甜蜜區偏移。** 指揮官說「去那個點」，甜蜜區說「你太低
了，爬」——兩者互相拉扯，飛機不會直線飛到集合點。代價很具體：
`ai-command-channel` 的「命令期間編隊收攏」**在 main 上就已經是紅的**。

**二、僚機站位。** 僚機各自往自己的甜蜜區飄，編隊會散。這一項較輕 —— 同一
分隊機種相同，目標若也相同則甜蜜區相同、大家一起飄；但長機與僚機目標不同時
會分岔。

#### 裁定：甜蜜區讓位給指揮，拉桿紀律不讓位

兩者的**位階不同**：

- **甜蜜區是戰術偏好** —— 指揮官的位階比它高。「我想飛高一點」不該蓋過
  「去那個點集合」
- **拉桿紀律是不要弄壞自己** —— **沒有任何命令是「把自己拉爆」**，所以它在
  任何時候都生效，包括執行命令途中

#### 實作上剛好很乾淨

`rallyCommand` 與 `stationCommand` 都是**直接寫 `aimWorld` 然後 return、
根本不經過 `steer`**。而甜蜜區偏移放在 `steer` 裡，所以它**自動就不會影響**
那兩條路徑 —— 不需要額外的守衛。

只有「有命令且有目標」那一條（`intent` 被設成 `'rally'`）需要明確排除：
`steer` 在 `intent === 'rally'` 時不套甜蜜區偏移。

拉桿紀律則相反 —— 它必須連 `rallyCommand` 那條早退路徑也涵蓋，否則「飛去
集合點」的途中仍然可以把自己拉爆。這是兩者實作位置不同的直接後果，
**計畫必須把它寫成一個獨立的任務**，不能假設「放在 steer 就好」。

#### 驗收上的要求

`ai-command-channel` 的兩條與 `ai-command-tactics` 的側翼那條 **main 上本來
就紅**，所以不能只看紅不紅 —— 必須**量它們的數值有沒有變差**，開／關對照
各記一次。

---

## 5. 驗收

### 5.1 主判準：把 2026-08-11 放寬的四筆改回去

```
  ai-defence 正後方 400 m 的 budgetSeconds: 0.55   → 整筆刪掉（回到共用的 0.3125）
  ai-visible-evasion SHOOTABLE_LIMIT[700]: 0.14    → 回到 0.08
  ai-targeting holdMedian: 1.35                    → 回到 1.5
  ai-duel-matrix「能量優勢要換得到東西」            → 由紅轉綠
```

**不發明新指標。** 做對了就改得回去，改不回去就是沒做到。

這也是當初堅持做成局部例外而非統一放寬的理由 —— 統一放寬會讓這個驗收
永遠做不到。

**四筆能改回幾筆由專案負責人裁定**，不是全有全無：若三筆回得去、一筆回不去，
那一筆要有量測支持的解釋，而不是再放寬一次。

### 5.2 副判準：打法真的看得出來

新增量測探針：一場 P-51 對 Bf 109，記錄兩邊的**速度分布與高度分布**。

- 做對了 → 兩個分布分開（P-51 偏高偏快、109 偏低偏慢）
- 沒做到 → 兩個分布重疊

判準值待實測後回填（先量開／關對照的差，再定門檻）。

### 5.3 配置的陷阱

**主判準與副判準必須在不同配置下量。**

`ai-defence`、`ai-visible-evasion`、`ai-duel-matrix` 用 `new Aircraft(P51D, …)`，
走**史實 spec**，不經過手感層 —— 它們量的不是玩家飛的那台飛機。
主判準跟著它們走史實 spec；**副判準必須套 `GAME_FEEL`**，否則量的是另一台。

### 5.4 開關對照

照專案慣例做開／關對照，確認效果來自本改動。

**指揮層的三條既有紅字要另外記數值**（見 4.4）：`ai-command-channel` 的
編隊收攏與命令佔時比例、`ai-command-tactics` 的側翼方位角。它們在 main 上
就是紅的，所以判準是「數值不得比開關前更差」，不是「轉綠」。

### 5.5 效能

`assess` 在 AI 的決策節拍上（10 Hz、每次兩台），而 `bestSustainedTurnRateCached`
的註解記載過原函數 1,076 µs/次會造成看得見的頓挫。甜蜜區表必須沿用同一個
作法（首次一次填滿、之後查表）。**新增的每步成本要量，門檻沿用
`perf-gate.test.ts`。**

---

## 6. 不做什麼（YAGNI）

- **不動 `rules.arbitrate`** —— 見 4.1
- **不寫機種打法表** —— `p51d: 'energy'` 這種明寫的行為表會失去 `feel.ts`
  倍率一動就自動校正的性質，而這個專案反覆吃過「值的來歷過期」的虧
- **不使用滾轉率** —— 見 2.4；它屬於未來的機動層
- **不新增意圖** —— `defend | merge | extend | engage | approach | rally` 不變
- **不碰飛行模型** —— `limiters.ts` 不再動，玩家的操縱權限不受影響

---

## 7. 未決事項的結案（2026-08-11 實作後回填）

### 7.1 拉桿紀律的餘裕 —— 結案

`energyMinPull` 由起手的 0.35 定到 **0.65**，掃描表在 `ai/doctrine.ts` 的
`DEFAULT_DOCTRINE` 註解。起手值本身就落在自己註解警告的「完全鬆桿的 AI
是靶子」區域：`ai-defence` 後下方 400 m 的掉血 0 → 605。

`energyFreeRatio` 1.0、`energyFloorRatio` 0.70 維持起手值 —— 掃描顯示
0.70 → 0.50 對結果一格不動（藍機一直在下限以下，斜坡調不到）。

### 7.2 甜蜜區偏移量的上界 —— **掃了，但掃不出來**

`sweetSpotMaxPitch` 留在起手的 10°。0 / 5° / 10° / 15° / 20° 五個值對每一個
判準都**非單調**（表在下方），現有測試組沒有一個指標解析得出這個尺度的
改動。唯一一致的訊號是 20° 的 `fireShare` 與 `onNose` 同時低於關閉基準
（0.0226 / 0.1618 vs 0.0363 / 0.1733）—— 偏太多會開始放掉射擊解，與設計
預期相符，所以 20° 排除。

| 上界 | 中位TAS差 | 中位高度差 | hold | rear | fire | onNose | 高能量開局 |
|---|---|---|---|---|---|---|---|
| 0（關） | 40 | 47 | 1.400 | 0.338 | 0.0363 | 0.1733 | blue 120.4 s |
| 5° | 51 | −19 | 1.800 | 0.294 | 0.0300 | 0.1878 | timeout 254:0 |
| **10°** | 35 | 61 | 1.600 | 0.344 | 0.0300 | 0.1800 | **blue 28.1 s** |
| 15° | 29 | 22 | 1.600 | 0.381 | 0.0333 | 0.2082 | blue 61.8 s |
| 20° | 10 | 77 | 1.400 | 0.348 | 0.0226 | 0.1618 | timeout 131:0 |

### 7.3 護欄的解析度 —— **本次最重要的方法學發現**

用一個**與打法完全無關的等量擾動**（無條件把拉桿係數乘 0.99）跑同一場，
量它造成的偏移，就知道該護欄解析得出多小的改動：

| 護欄 | 0.99 無意義擾動 | 本功能 | 判讀 |
|---|---|---|---|
| `ai-defence` 後下方 400 m | 0（= 基準） | 605 → 修正後 0 | **解析得出來** |
| `ai-targeting` rearShare | 0.3640（破 0.35 門檻） | 0.3615 | 解析不出來 |

`rearShare` 的門檻 2026-08-07 已由 0.30 放寬到 0.35，現在坐在刀鋒上 ——
**任何 AI 改動都會讓它隨機翻紅**。這支探針應該變成專案的標準工具：
定值之前先問「這個護欄分辨得出我要量的東西嗎」。

### 7.4 主判準：四筆回去三筆

```
  ai-visible-evasion SHOOTABLE_LIMIT[700] 0.14 → 0.08   ✓ 回去了（例外整筆撤銷）
  ai-targeting holdMedian 1.35 → 1.5（實測 1.60）        ✓ 回去了，比放寬前還高
  ai-duel-matrix「能量優勢要換得到東西」                  ✓ 由紅轉綠（28.1 s 擊落、1000:0）
  ai-defence 正後方 400 m budgetSeconds 0.55             ✗ 回不去（0.518 → 0.530 s）
```

**留下的那一筆有量測支持的解釋**：它是唯一沒有被能量紀律改善的幾何，而且
一格不動（+2%，在該場景對無意義擾動的解析度之內）。0.55 這個數字沒有被
調高過。下一輪該查的方向寫在該筆註解裡（破防軸退化，不在能量層）。

### 7.5 副判準：看得出來

`test/tools/doctrine-visible.probe.ts`（套 `GAME_FEEL`）。甜蜜區關 → 開：
P-51 的 TAS p10 由 324 升到 358 km/h（不再讓自己變慢）、109 的 p90 由 618
降到 593（不再飛太快）、中位高度差由 −6 m 變成 +61 m。

**門檻不定。** 見 7.2 與 7.3：這一組量對參數非單調，定門檻等於擬合單一顆
種子的軌跡。要有門檻得先讓探針跑多個**開局**（不是多顆種子 —— 種子只決定
飛行員名字，不進入物理路徑）。

### 7.6 仍然未決

- **`steer.ts` 四個能量判準的重掃**（`brakeCornerRatio` / `unloadMargin` /
  `speedRecoverMargin` / `extendPitchAngle`）**沒有做**。理由不是忘記：7.3
  顯示現有判準對這個尺度的改動解析不出來，硬掃只會生出四張雜訊表。
  **先修量測工具（多開局的探針），再掃。**
- `ai-duel-matrix`「能量優勢方仍有喘息」由綠轉紅（0.343 → 0.909）。實測
  同一場仗由 90 秒打不完變成 28 秒解決，藍方花掉的能量**更少**（4816 →
  3261 m）、打出 45 倍傷害、自己零損。那條護欄量的是「深度耗能佔**時間**
  的比例」，分母隨戰鬥長度變 —— 前提被本功能否證。**護欄重新定值是專案
  負責人的決定**，本次不動。
- 指揮層三條既有紅字：編隊收攏由紅轉綠、命令佔時比例 0.0000 → 0.0130
  （仍紅）、側翼方位角 −0.0993 → +0.0910（**變差**）。樣本數 n=0 / n=1，
  事件計數型指標，解析度問題與 7.3 同一族。待裁定。
- 若浮現不足以讓打法看得出來，是否進意圖層 —— 下一期的決定（本次的副判準
  顯示浮現是**看得見的**，暫時不需要）
