# 給 Claude 的工作規則

專案負責人反覆交代過的事。**worktree 也適用** —— 每一個 worktree 都是同一個
repo 的簽出，這個檔案在每一份裡面都在。

## 1. 註解只寫事實，不寫思考過程

註解寫的是**現在的狀況與它的理由**，不是「我當初考慮過 A、後來選了 B」。

不要寫：

- 討論過程、比較過的方案、被否決的做法
- 沿革（「本來是 1 倍，後來改成 2 倍」）—— git log 已經有了
- 出處與裁決紀錄（「負責人 2026-09-07 裁定」、「Codex 審查 C7」）
- 自己的推理步驟

要寫：

- 這個值／這段程式在做什麼
- 為什麼非這樣不可（**壞掉會怎樣**，尤其是不會報錯的那種）
- 呼叫端要遵守的約定

可調參數就地改掉，不要在註解裡疊一行歷史。

## 2. 改檔案直接編輯

用編輯工具改檔案。**不要寫 Python／Node 腳本去做字串取代。**

例外：一次要改幾十處的機械式改寫，而且改完會逐一驗證。單純為了「省事」
而繞道腳本不算例外。

## 3. 這個專案的其他既定紀律

- 流程：SPEC → PLAN → Codex 審查 → TDD → Codex → Playwright
- `npx tsc --noEmit` 有一批既存錯誤（`test/tools/` 的探針與 e2e 的 `process`）。
  **動工前先量一次當基準**，改完比對行數有沒有增加 —— 不要把某個數字寫死
- Codex 跑法：`codex exec -s danger-full-access`，prompt 走 stdin，**背景執行**
- 提交訊息的 trailer 只留 `Co-Authored-By`，不加 session 網址
- 不要 `git add -A`
- 護欄要能被殺死才算數：先驗紅，或做變異測試
- 護欄重新定值是負責人的決定，不是實作者的
- 熱路徑（240 Hz）不配置記憶體
- 全部註解與文件用繁體中文
